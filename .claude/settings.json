{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.cpp|*.cc|*.cxx|*.h|*.hpp|*.c) command -v clang-format >/dev/null && clang-format -i \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "clang-format"
          }
        ]
      }
    ]
  }
}
