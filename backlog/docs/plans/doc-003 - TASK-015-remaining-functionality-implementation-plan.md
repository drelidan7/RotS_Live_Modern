---
id: doc-003
title: TASK-015 remaining functionality implementation plan
type: specification
created_date: '2026-09-07 13:47'
updated_date: '2026-09-07 17:03'
---
# TASK-015 remaining functionality implementation plan

## Purpose and dependencies
Finish the approved migration design doc-001 on the current branch. Batch B is verified in doc-002. This plan covers all remaining selected functionality, including local uaf-port, and the full combined validation gate. TASK-015 owns recorded work state.

## Global Constraints
- Source release-frodo is pinned to e0458069453f362e544a0c12b2655879e2da6979. Local uaf-port source is /Users/drelidan/Projects/GitHub/RotS_Live/.claude/worktrees/uaf-port at 1d242d46b10a2462cb538d789fb23a6047e687dd. Read committed source only; never modify those checkouts.
- Current branch fix/task-025-summon-dark-ok at c88186b5 remains unchanged in Git. Preserve existing batch B edits. No commits, pushes, merges, deployment or live-server access. Local test servers and disposable test fixtures are authorized.
- Preserve nine-library architecture, Placement, first-null string-view boundaries, modern caster snapshots/identity ownership, FP determinism, portable socket APIs and legacy binary ABI. No whole-file imports over modern implementations.
- Tests first, meaningful regressions and positive controls. Controller owns all shared builds, dependency restore, census/ledger integration, boot/smoke and finalization commands. Implementers own only assigned source/test edits and report exact validation needs. No parallel implementation subagents or same-file writers.
- Task reviewers are read-only and independent of implementation. Inherit the user's current model. Managed Backlog changes remain controller-owned via CLI; scratch briefs/reports are disposable execution artifacts, not a work board.
- Native/rots64 build+full CTest+boot goldens per coherent production batch; macOS ASan+UBSan for test changes. Account smoke for account/network paths. All three census checks retain ratchets/proofs. Final local i386 battery plus available six CI checks; do not push merely to trigger CI, and do not falsely complete TASK-015 if required external checks are unavailable.

## Class responsibilities
Existing descriptor owns connection/session state; AccountData owns persisted authentication/roster preferences; new roster summary/cache owns persisted display snapshots without character pointers. Existing battle_mage_handler owns interruption decisions. Poison classification owns punishment policy independently of kill attribution and snapshots callback-sensitive engagement before ON_DIE. Validated movement is a synchronous app-owned operation exposed through the existing combat hook direction. No reversed library dependencies.

## Task 1: Local uaf-port U1-U4
Read doc-001's Additional approved source section and corresponding committed source deltas. Implement squared summon distance, death-room XP with remote/unplaced credit and remote-target exclusion, poison punishment/attribution separation, and remove_char_exists registry bounds. Audit residual source overlap with modern TASK-018/019/020/021/025/026. Preserve three-argument die/raw_kill entry points. Capture classification/name ownership before ON_DIE; no unchecked borrowed opponent after callbacks. Extend mage/fight credit/registry tests and introduce focused death classification tests if needed. Test same-room controls, equal/negative/positive coordinates, PC-vs-mob poisoner with every engagement direction, pets/orcfriends, missing poisoner, non-poison/NPC controls, exploit naming and actual die/raw_kill penalty integration, ON_DIE extraction. Controller wires new TUs and updates census proofs without weakening gates.

## Task 2: Networking and MSDP N/M
Port all selected N/M commits in doc-001 using final source behavior. Keep portable rots_net APIs for would-block/socket options, explicit partial-write failure policy, retry-safe prompt ordering, bounded reads and pre-login deadlines. Complete charset negotiation, reconnect parity, room guard/descriptor consistency, once-only flush, persistent dirty flags, server/weight/professions/specialization/group fields. Keep output seam and Placement. Adapt room-resolution ledger and overlap TASK-030 based on actual corrected tests. Extend socket/output/protocol/menu and account smoke coverage; test invalid actors, switched reconnects, disabled channels, sanitization and group filtering. Run proxy tests. Shared descriptor changes coordinated with account task.

## Task 3: Movement and delays V/D
Implement synchronous checked movement hook through combat_hooks; private app helper accepts call-local validation state tied to direction, invalidated by haze changes. Flee/windblast validate once; no global skip pointer. Preserve callback revalidation and actor-location re-reads. Test veto/relocation/riding/special-return/no-exit and later unrelated moves. Preserve newly queued delay after complete_delay in both WAIT_STATE macros. Battle mages resist bash cast interruption through current battle_mage_handler. Add deterministic regression/control tests; census new dispatch or caller spellings require real proof and registry updates.

## Task 4: Account lifecycle, recovery and roster A/P/R
Apply final release behavior into existing persist identity/presentation/storage/assets modules and app nanny. Logout closes after Goodbye; deletion archives/unlinks account ownership before index retirement with failure consistency. Password recovery includes notices, code expiry/resend/attempt/replay controls, mail/write failures and absolute deadlines. Add roster_cache under persist with default disabled state, boot enable, successful-write/delete invalidation and owned normalized keys. Raise cap200; share ordered filtered indices across display/numeric/name selection; stable ties, persisted sort/session filter and labelled side output. Preserve source outputs and all current portable/storage contracts. Preserve final upstream name selection intentionally bypassing the active filter through the same sorted/capped roster with RosterFilter::None; numeric selection uses the active filter. Add the three recovery input states to secret-input classification so reset codes/passwords bypass history and snoop. Close account-deletion cache invalidation omitted by upstream. Distinguish pre-commit failure from post-account-unlink cleanup failure, retaining archives and index/cache consistency instead of deleting the recovery archive after an already-committed unlink. Preserve any preexisting archive on failures. Extend existing and upstream-adapted tests, add all new TUs to three build lists through controller. Account smoke covers complete paths.

## Task 5: Colours, product help and local operations C/O
Add mob colour slot15 with reserved_15 input compatibility in existing JSON schema/layout, NPC/mount/player rendering controls and deliberate JSON golden-key change. Port selected help_tbl/shap_tbl edits without overwriting current unrelated help. Adapt host-user Docker operation to current private named volumes/services, without destructive ownership repair. Ignore local autorun. Validate compose config, shell syntax and disposable ownership write probes.

## Task 6: Whole-port reconciliation and finalization
Compare all72 release and all38 supplemental commits against resulting behavior, retain explicit skips/overlaps and per-slice proof. Run combined native/Linux/sanitizer suites, both boot goldens, account smoke/proxy tests and all censuses; verify tests execute rather than only skip. Run canonical i386 battery sequentially and reconcile monolithic count. Inspect available six CI results for exact proposed changes; no remote publishing is authorized, so report any unavailable final requirement accurately. Independent broad final review plus fixes and scoped re-review. Update TASK-015/linked docs with actual evidence; mark Done only when every acceptance criterion is fulfilled.

## Execution sequencing clarification
U/C/O are the first coherent remaining batch. Account A/P/R may execute before N/M after U review because it can use existing portable connection APIs; the N/M implementer must consume its added descriptor states and preserve secret-input classification/deadlines. This changes scheduling, not the source contracts. Controller V/D work uses disjoint movement/delay files; any boot registration edit in account-owned app files must be handed to that implementer instead of concurrent same-file writes. Existing direct Compose workflows retain root-default private build caches; the host-user launcher uses separate per-UID/GID build volumes and initializes only empty volume roots. This explicitly differs from upstream's default1000 because otherwise current root-owned caches become unwritable.

## Execution evidence: U/C/O coherent batch
- U1-U4 implemented in the existing combat/entity owners and existing test TUs. Independent spec/quality review passed with no introduced findings. Source overlap with TASK-018/019/020/021/025/026 audited; modern snapshot/lifetime and Placement architecture retained.
- C and O independently reviewed and locally implemented. Mob colour uses slot15 and writes the mob JSON key while accepting reserved_15; selected help changes are ported. Both host-user Docker services passed disposable501:20 bind/volume write probes. A deliberately mismatched nonempty disposable volume refused safely; original root-owned caches were unchanged.
- Combined native full CTest discovered2019 and passed with platform skips. Linux and macOS ASan full suites had only one stale colour-menu expected-list failure; its corrected expectation passed native full recheck and Linux/ASan focused rechecks. Native and Linux boot logs match goldens. All three census checks passed at the U/C/O batch boundary. Current evidence logs: /tmp/task015-uc-native-recheck.log, /tmp/task015-uc-linux-recheck.log, /tmp/task015-uc-asan-recheck.log, /tmp/task015-uc-native-boot.log, /tmp/task015-uc-linux-boot.log.
- D delay/bash regressions reproduced before changes and13 focused tests passed afterwards. Independent static review found no production defect and one low test-style item, corrected. Full combined validation follows A/P/R and V integration.
- A/P/R and V implementation underway. These entries do not mark TASK-015 complete; mandatory lifecycle smoke, whole-port reconciliation and finalization remain outstanding.

## Execution evidence: A/P/R and V/D coherent batch

2026-09-07 A/P/R and V/D coherent batch verified locally. Full native and ASan+UBSan: 2155 discovered, 2079 passed, 76 platform skips; Linux x64: 2155 discovered, 2077 passed, 78 skips; zero failures. Both boot goldens match. All three census checks and the location/room-resolve self-tests pass (string-view has no self-test command). Isolated account/proxy lifecycle smoke passes after narrowly porting logout reconnect and roster prompt expectations. Movement review findings reproduced (null mount after early follower callback; replacement registry-slot passenger moved without its own check), fixed and independently re-reviewed PASS. Actual heap-free mount/rider/windblast tests pass ASan; registry generation/ABA redesign remains outside this port. Account archive rollback/symlink and pipelined recovery secret fixes reviewed; Linux mail-failure fixture now drains stdin before reporting failure. Source-parity audit accounts for all72 release and38 supplemental rows; no missing selected production behavior found outside pending N/M. U1-U4 and modern UAF overlap satisfy AC6. Next: N/M regressions and implementation, then full finalization; task remains In Progress.

Evidence: /tmp/task015-av-closure-native-full.log, /tmp/task015-av-closure-linux-full.log, /tmp/task015-av-closure-asan-full.log, /tmp/task015-av-native-boot-recheck.log, /tmp/task015-av-linux-boot.log and /tmp/task015-av-account-smoke-recheck.log. Tests initially exposed four sandbox-only socket failures on each Mac configuration; all passed with local socket permission before the clean final full reruns. The sanitizer mount red log and recycled-passenger red log are /tmp/task015-v-review-red2.log and /tmp/task015-v-recycled-red.log. The room-resolve ceiling fell573 from576 through resolver hoists; the added checked-movement dispatch is pinned by its own guard, bare handler token and counted registration exemption.

## Execution evidence: N/M and final implementation reconciliation

2026-09-07: all selected functionality from the pinned 72 release commits and 38 supplemental commits is implemented locally. doc-001 has a final disposition for every exact commit ID, with no temporary Port rows. Independent source-parity review covered B/U/C/O/A/P/R/V/D and a separate N/M review covered all 9 N and 10 M source commits, including the mixed-commit halves. Architecture and style review findings are closed. The final two account log-event locals use string_view; native/Linux/ASan rebuilds and all 124 InterpreAccountMenu tests pass after that final correction. Rustfmt is unavailable on the installed toolchain, so no Rust formatter certification is claimed.

N/M retains the portable socket layer, no-data would-block retry with owned buffers, the upstream fatal partial-write policy, output-before-prompt ordering, eight-read quota and pre-login idle cleanup. MSDP now advertises Latin1, retains undeliverable dirty values, initializes SERVER_ID and zero-valued fields, sends room/time once, reports only placed playing actors and serializes weight/professions/specialization/group. Switched and legacy reconnects preserve existing protocol objects and send actual negotiation/room packets. The corrected room update stays behind the output seam and validates actor/exit bounds through Placement. No raw-location or flat-layout import was needed. The room-resolve ceiling dropped from573 to565 through hoists and two guarded rows; its self-test pin follows the measured count, with no relaxed floor or exemption.

| Slice | Actual regression/characterization owners |
| --- | --- |
| B | skill_timer_tests.cpp, script_tests.cpp, act_wiz_format_tests.cpp, db_loader_tests.cpp; shifted timer, nested block, OB and bounded description regressions |
| U | mage_tests.cpp, fight_credit_tests.cpp and caster_snapshot_tests.cpp; summon coordinate outcomes, local XP/remote credit, DeathClassification/PoisonDeathPunishment actual die/raw_kill integration and heap-extracting ON_DIE, registry bounds; existing dark-room and room-affect lifetime suites retained |
| V/D | act_move_tests.cpp, act_offe_tests.cpp and comm_delay_tests.cpp; actual flee/windblast/rider callbacks, freed mounts/passengers, veto/haze/relocation, both reentrant delay macros and battle-mage interruption resistance |
| A/P/R | account_management_tests.cpp, interpre_account_menu_tests.cpp and roster_cache_tests.cpp; archive rollback, recovery/deadlines/secrets, cache ownership/invalidation, ordering/filtering/name selection/caps, plus isolated lifecycle smoke |
| C/O | color_tests.cpp, act_info_format_tests.cpp and character_json_tests.cpp; disclosed reserved_15-to-mob golden key change and legacy input; exact upstream product-help comparison; both disposable501:20 Docker write probes and nonempty-volume refusal |
| N/M | comm_output_tests.cpp, startup_options_tests.cpp, protocol_tests.cpp, interpre_account_menu_tests.cpp and proxy/src/main.rs; real buffer saturation, deterministic prompt/write paths, accept/read/idle, room seam, dirty/serialization, null/existing reconnect protocol with real wire bytes, TCP and WebSocket round trips |

Combined native macOS and ASan+UBSan each discovered2194 tests:2118 passed and76 platform skips. Linux x64 discovered2194:2116 passed and78 skips. All three completed with zero failures; all nine layer acyclicity checks and converter builds are included. Both final boot goldens match byte-for-byte. Account/proxy lifecycle smoke passes against a temporary copied runtime and the final native binary; it includes recovery, logout/reconnect, create/play/save/color, legacy link and deletion/roster consistency. The Python smoke harness has54 passing unit tests; Rust proxy has3 passing tests, including TCP and WebSocket. The strengthened final reconnect/protocol/output/accept set passed245 native tests before the last type-only correction. All three census checks and location/room-resolve self-tests exit0; the scanned source tree contains328 files. String-view has no self-test command. Whitespace diff check is clean with the repository's CRLF policy.

Evidence: /tmp/task015-final-closure-{native,linux,asan}-full.log; /tmp/task015-last-style-{native,linux,asan}.log; /tmp/task015-final-closure-{native,linux}-boot.log; /tmp/task015-final-closure-account-smoke.log; /tmp/task015-final-proxy-ws.log; /tmp/task015-account-smoke-unit.log; /tmp/task015-final-{location-check,location-self,resolve-check,resolve-self,string-check}.log. Independent reviews are the task reports under .superpowers/sdd/doc-003 - TASK-015-remaining-functionality-implementation-plan/. The exact implementation/build/data file hash manifest is /tmp/task015-final-source-manifest.json.

At the start of finalization, the canonical i386 battery was run sequentially using explicit step numbers so dirty work at unchanged HEAD could not be skipped by stale completion markers. It has now completed successfully; final results follow below. Six exact-change remote CI jobs are unavailable for this uncommitted local diff: Linux i386 legacy, Linux x64, Linux x64 ASan+UBSan, macOS arm64, macOS arm64 ASan+UBSan and Windows MSVC. Their names are verified in .github/workflows/ci.yml. No commit, push or remote publication is authorized; existing HEAD CI cannot certify these changes. TASK-015 remains In Progress until its complete finalization criterion is met. TASK-030's overlapping guard defect has its own history, real-seam regression and boot/census evidence and can close independently.

Additional finalization evidence: the native single-process runner passed all2181 GoogleTest cases (2105 passed,76 platform skips; zero failures) in14.244seconds. Its count plus the13 separately registered gates (nine layer checks and four location/room-resolve checks/self-tests) equals CTest2194. Evidence: /tmp/task015-final-native-monolithic.log and /tmp/task015-final-native-test-inventory.json. All74 frozen implementation/build/data hashes and both source pins remain unchanged. The four unused TASK-015 Docker probe volumes, empty probe network, copied smoke runtime and temporary bind probe were removed; original build volumes are retained.

## Final local validation and remaining external gate

2026-09-07: all canonical i386 steps completed in sequence with exit0. Step0 removed only generated CMake metadata inside the private i386 build volume. Step1 clean CMake game/test/layer builds and CTest passed:2194 discovered,2187 passed,7 skipped, zero failures (856.48seconds of CTest). Step2 clean legacy Makefile compiled all221 production/test/helper objects, linked the monolithic runner, passed its census checks/self-tests, and ran2181 tests in one process:2157 passed,24 skipped, zero failures or crashes (62.793seconds). Step3 boot log matches the golden byte-for-byte.

The count reconciliation is exact:2181 GoogleTest cases +13 standalone CTest gates =2194. Monolithic skips are the same7 platform/environment skips plus17 ConvertEquivalence cases, because the flat Makefile does not supply the CMake-only rots_convert executable path. All17 converter cases passed in this battery's CMake stage; no conversion behavior is left untested by the combined battery. The seven shared skips are FormatsOutOfRangeSummaryTimestampsAsInvalid, LeavesStoredObjectPathUnchangedWhenCanonicalObjectWriteFails, MigrationFailsClosedWhenLegacyFileRetirementFails, MigrationRestoresRetiredFilesWhenExploitRetirementFails, DbLoader.FailsClosedWhenAccountNativeObjectOrExploitJsonCannotBeRead, LocationBenchmark.DumpsReportWhenRequestedByEnvironment and OlogHaiHelpers.HeavyFightingAndRidingAdjustOverrunDamage. These explicit skips are not counted as executed passes.

Battery logs: log/i386-battery/step1-20260907T160431Z.log, step2-20260907T163233Z.log and step3-20260907T170205Z.log; controller capture copies /tmp/task015-final-i386-step{0,1,2,3}.log. All74 frozen implementation/build/data hashes remained unchanged through the battery. Current checkout is still fix/task-025-summon-dark-ok at c88186b5f0c4d643d850ced7e14b6b45c197f757; both selected source pins remain unchanged. All implementation and local review/validation work is complete, with no commits, pushes, merges or production access.

TASK-015 remains In Progress solely because AC5 also requires six remote CI jobs for the exact changes. Current uncommitted work cannot have those results. No automatic publication or CI waiver is inferred. TASK-030 is Done based on its separate completed criteria. All other TASK-015 criteria are checked. A later authorized publication should run the six required jobs and close any platform-specific finding before completing TASK-015; no source migration slice remains pending.
