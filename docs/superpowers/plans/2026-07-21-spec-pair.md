# Spec-Pair Wave Implementation Plan (`spec_pro.cpp` → `spec_ass.cpp`, combat DEFER → 0)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the combat row's last two TUs — `spec_pro.cpp` then `spec_ass.cpp` (joint only if
a genuine cycle forces it) — into the tier T0's census adjudicates, taking combat DEFER 5-wave
program to **0**.

**Architecture:** Census first (T0: the tier ruling — `rots_combat` L3 vs `rots_script` L4, the
mobact fresh-closure precedent; the pair-structure ruling; the pre-authorized 3-edge spec-proc
registrar family design), seams consumer-free (T1), per-TU conversions (T2 spec_pro, T3
spec_ass), membership commit(s) per the pair ruling (T4), docs + the Wave-A hygiene batch + the
Wave-A i386 fold-in (T5a), finalization with the pre-authorized merge (T5b). Spec:
`docs/superpowers/specs/2026-07-21-spec-pair-design.md`. Recipe: the playbook. Branch:
`arch/spec-pair` off master @`01d1dc8`.

**Tech Stack:** C++20, CMake presets + flat Makefiles, GoogleTest, `nm` censuses, Python 3
byte-edits, existing seam headers + ONE pre-authorized new registrar hook family (home per T0 —
expected `script_hooks.h` or a small standalone header per the owning-taxonomy rule).

## Global Constraints

- **Flat-Makefile parity BINDING, same-commit** (owner rule, carried): new test file →
  `src/tests/Makefile` SRCS; new production TU (none expected) → both flat lists; new linkcheck
  (not anticipated; a forced new library = auto-STOP) → root `Makefile` test-target list.
- **Wave-A lesson, MANDATORY in T2/T3 briefs:** every upward edge of a promoting TU converts in
  its conversion task — "defer to the membership task" is an error by construction.
- Byte-verbatim moves; per-file CRLF measurement (`spec_pro.cpp`/`spec_ass.cpp` unmapped);
  Python byte-edits for all `.cpp`/`.h`. Conversions/relocations are the only semantic edits.
- Layer order `platform < core < entity < persist < world < combat < pathfind < script < olc <
  app`, unidirectional links only. The registrar family's unregistered-lookup default is a LOUD
  tripwire (never a silent null fn-ptr). Rider gate: ONE slot (2 of ≤3); second new same-shape
  edge or any different-shape = auto-STOP. A 4th registrar edge or different coupling class =
  auto-STOP. An irreducible cross-tier bidirectional pair = auto-STOP.
- Per-task gates (both hosts): macOS arm64 + `rots64` builds + full ctest, both boot goldens,
  all nine linkchecks, `ConvertEquivalence` 17/17, `python3 tools/string_view_census.py
  --check` exit 0; ASan on any test-file change. i386 battery finalization-only. Goldens
  unchanged — drift = bug. No death tests; `rots_rng` only; `std::format`; `-Werror` clean.
- STOP-and-adjudicate per spec §Risks (the named auto-STOPs go to the OWNER despite the
  autonomous grant). Docker synchronous in subagents. Baseline tests **1487**; deltas per task,
  reconciled T5a. Implementers Sonnet; census/heavy reviews Opus; whole-branch Fable. Scratch
  prefix `sp-`, never committed. **Owner grants: merge-when-green; autonomous-spec-gate (this
  plan itself rides it).**

---

### Task 0: Census + tier adjudication + registrar-family design (read-only)

**Files:** Create `.superpowers/sdd/sp-census.md` (gitignored).

**Interfaces:** Produces the tier ruling (both TUs), the pair-structure ruling (joint vs
sequential), the registrar-family design (key scheme, hook home, default), and the full
per-edge disposition tables for T1-T4.

- [ ] **Step 1:** Build objects (`cmake --preset macos-arm64` + build). Confirm base: 15-TU
  `rots_combat`, 29 cells, nine libraries, master @`01d1dc8` + spec/plan commits.
- [ ] **Step 2:** `nm -u` census on `spec_pro.cpp.o`/`spec_ass.cpp.o`, library-first resolution
  over the nine lib dirs + `ageland.dir`; reconcile against the playbook rows (spec_pro
  combat-peer=17 pre-Wave-A, spec_ass=39) listing what Wave A dissolved.
- [ ] **Step 3:** **Tier adjudication** — run the closure check under BOTH candidate homes
  (`rots_combat`, `rots_script`) for each TU: which produces zero upward edges after the T1
  seam set? Evidence per the spec's cuts (the `virt_*` setter direction; `find_first_step`;
  `_cmd_info`/`command_interpreter`; who dispatches the procs). Also rule the pair structure:
  enumerate spec_pro↔spec_ass edges both directions (joint only on a genuine cycle).
- [ ] **Step 4:** **Registrar-family design** — body-read the three fn-ptr references
  (`gen_board`/`postmaster`/`receptionist` — find the exact reference sites in spec_ass.cpp and
  each owner's definition); design ONE lookup hook family (key scheme e.g. an enum or the
  virtual number; setter per owning TU registered at boot both paths; loud-tripwire
  unregistered default), home per the owning-taxonomy rule. Sweep BOTH TUs for a 4th registrar
  edge or different-class coupling (auto-STOP) and for any new `void*` dispatcher shape
  (rider-gate last slot vs auto-STOP).
- [ ] **Step 5:** Disposition everything else: `_cmd_info` (RELOCATE/accessor/existing-hook —
  auto-STOP if none fits), the `do_*` sweep vs 29 cells (new cells only on confirmed shapes),
  `_buf`/scratch globals, `add_exploit_record`/`waiting_list`/`find_first_step` confirmations,
  dead decls. Record the membership plan (exact CMake moves, flat zero-change greps, T1 seam
  list, T2/T3 conversion lists with the NO-DEFERRAL rule stamped on every upward edge).

---

### Task 1: Seams (consumer-free)

**Files:** The registrar-family hook (home per T0) + its three boot registrations
(boards.cpp/mail.cpp/objsave.cpp) both paths; any T0-surfaced cells/accessors; tests
(discriminator pair per edge/cell; new test files flat-paired SAME COMMIT).

- [ ] **Step 1:** TDD the registrar family: failing discriminator pairs (registered lookup
  returns the real fn-ptr; unregistered exercises the loud tripwire via the flag pattern), then
  the hook + three registrations. Commit per logical unit.
- [ ] **Step 2:** Remaining T0 seams/relocations (byte-verbatim, `nm` single-definition,
  coverage riders per the gap rule).
- [ ] **Step 3:** Full gates both hosts + ASan + goldens + flat-parity grep evidence.

---

### Task 2: `spec_pro.cpp` conversions (still app-compiled)

- [ ] **Step 1:** Convert EVERY upward edge per T0's list (NO deferral to T4): `do_*` cells,
  `command_interpreter` hook, `_cmd_info` per ruling, `add_exploit_record` dispatch, `_buf`
  retirement, dead-decl deletion. Intra-pair spec_ass calls stay DIRECT only under a T0 joint
  ruling; under sequential, spec_ass→spec_pro edges are the gated direction (spec_pro must not
  call spec_ass — census-verified).
- [ ] **Step 2:** Discriminator audit (genuine gaps only, verified zeros recorded).
- [ ] **Step 3:** Full gates + ASan (if tests changed) + goldens. Commit.

---

### Task 3: `spec_ass.cpp` conversions (still app-compiled)

- [ ] **Step 1:** Convert per T0: the three registrar references → the T1 lookup family;
  re-home its own `virt_*` setter registrations if the tier ruling requires (L3 home makes the
  `script_hooks` setter calls upward — T0 will have ruled the mechanism); `_buf`; dead decls.
- [ ] **Step 2:** Discriminator audit; gates + ASan + goldens. Commit.

---

### Task 4: Membership per the T0 pair ruling

- [ ] **Step 1:** Sequential (default): spec_pro's membership commit (its ruled tier), gates +
  its linkcheck green both hosts; then spec_ass's commit, gates + linkcheck. Joint (cycle-forced):
  ONE commit. House-style comments; flat zero-change verified by grep; linkcheck failure =
  census miss → bisect locally, STOP if undispositioned. **Combat DEFER = 0.**
- [ ] **Step 2:** All nine linkchecks + full ctest + goldens + `ConvertEquivalence` + svc.

---

### Task 5a: Docs + Wave-A hygiene batch + Wave-A i386 fold-in

- [ ] **Step 1:** Docs: BUILD.md (final tier story, DEFER 0, the registrar family), playbook
  (spec_pro/spec_ass rows RESOLVED; the combat row CLOSED — final as-built section), parent
  spec (as-built slice), this wave's spec (As-built), AGENTS.md (chain from 1487; DEFER 0;
  membership counts; i386 pending T5b noted). progress.md controller-owned — untouched.
- [ ] **Step 2:** Hygiene batch (src comments/dead decls, object-code-neutral, nm evidence):
  combat_hooks.h:130 stale "still-app ranger" dismount comment; color.cpp:33 +
  visibility.cpp:1187 stale reader-scan snapshots (spell_pa now in-lib); dead decls
  spell_pa.cpp:34 (`fast_update_list`), spell_pa.cpp:55 (`ACMD(do_flee);`), ranger.cpp:54
  (`old_search_block`). **Wave-A i386 fold-in**: the measured battery numbers from
  `.superpowers/sdd/sf-i386-battery.log` (controller supplies them in the brief) land in
  AGENTS.md's spell-family chain entry, replacing its "pending T5b" note, plus the census
  86-vs-76 clarifying parenthetical. Full gates once (src touched). Commits: docs / hygiene
  separate.

---

### Task 5b: Finalization

- [ ] **Step 1:** i386 battery (sequential, fresh-branch full run; reconciliation exact —
  linkcheck delta stays 9).
- [ ] **Step 2:** Whole-branch review (Fable); doc-only fixes during battery OK; src fixes
  restart it.
- [ ] **Step 3:** Push + PR + six blocking CI jobs → **MERGE WHEN GREEN (owner grant)**;
  ff-merge, delete branch.
- [ ] **Step 4:** Bookkeeping: ledger close (DEFER 0 — THE COMBAT ROW IS DONE), memory update
  (program complete; next-frontier options for the owner: Stage 2 LocationSystem, rots_commands
  census, int→double gate), branch prune. Report the finished program to the owner.
