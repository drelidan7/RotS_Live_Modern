# Room-resolve retirement ledger

Generated by `tools/room_resolve_census.py --generate-ledger`, then hand-refined
per Wave R1's task-3 brief (`.superpowers/sdd/2026-07-31-rr1-census/task-3-brief.md`).
Program design: `docs/superpowers/specs/2026-07-31-room-resolve-retirement-design.md`
(section numbers cited below refer to that document).

## Program contract

This ledger is the proof-obligation table for the room-resolve retirement
program ("RR"): every resolver-reaching site in production code (`room_of(`,
`room_by_id_total(`, `world[`/`::world[`, the `world_room_vnum(`/
`dispatch_room_vnum(` hook-dispatch alias pair, and the eleven-member
resolver-expanding macro family) must carry a row classifying whether its
input id can be proven in-range before `room_data::operator[]` degrades it
(design doc section 1). A row's key is `` `file · function · token` `` (the
middle dot is U+00B7, illegal in a path or C identifier, so the split is
unambiguous); its count must equal the scanner's own count for that exact key
(the anti-inheritance rule, section 3/O-5) -- adding a new site to an
already-classified function fails the gate as a count mismatch rather than
silently inheriting the existing proof.

## The standing rule

`--check` fails closed on: an unclassified scanned site, a stale ledger row
matching nothing in the scan, a per-key count mismatch (either direction), a
per-token total mismatch against the token-counts table below, a `TODO` total
over the checked-in `MAXIMUM_TODO_COUNT` ceiling, and a resolver-reaching
macro whose name is not in the pinned `MACRO_FAMILY` literal. Each drain wave
lowers `MAXIMUM_TODO_COUNT` in the same commit that adds its proofs --
**a new resolver site anywhere in production is therefore a new `TODO` row,
which exceeds the ceiling: unclassified new code is a build failure from R1
onward** (design doc section 4/O-10). A row `Count` must be at least 1 -- a
zero or negative count can mask real sites while some other row's count
keeps a per-key SUM exact (review-1 W-2).

## The closed, pinned classes

Three classes are pinned closed, in addition to `MACRO_FAMILY`'s own pinned
literal (review-1 W-1, closing the dual whole-branch review's BLOCKER):

- **`DECL`** rows must key on a `#`-prefixed name -- either a macro-body key
  (`#NAME`, matching a real `#define`) or the file-scope-unattributable key
  `#decl`. A plain function name can never start with `#`; a `DECL` row
  keyed on one is rejected outright, closing off the demonstrated exploit of
  auto-laundering an unattributable site into the no-proof-required `DECL`
  class by naming it like a real function.
- **`RESOLVER-IMPL`** is closed by a `RESOLVER_IMPL_KEYS` frozenset of
  `(file, function)` pairs (the tool's own module-level constant) -- any
  `RESOLVER-IMPL` row outside that set is a gate error. Extending the
  resolver-impl set is therefore a review-visible SCRIPT edit, never a
  ledger-only one.
- **`.cpp`-file `#decl` keys** are additionally closed by a
  `PINNED_DECL_KEYS` frozenset -- a header's `#decl` key and any `#NAME`
  macro-body key (header OR `.cpp`) stay legal and open, since both are
  re-derivable from a real, named construct; only the `.cpp`-file
  "unattributable, file-scope" bucket is pinned, restoring design doc
  section 4's fail-closed rule that an unattributable production site must
  not silently auto-launder into `DECL` as the tree changes shape around it.

`PROVEN` and `GUARDED` rows must carry a genuinely non-empty `Kind` AND a
genuinely non-empty `Proof` -- the empty marker on either previously
short-circuited every shape check below wholesale (review-1 F-1,
demonstrated live: a real `TODO` row hand-flipped to
`` | PROVEN | — | — | `` passed `--check` unchanged). `PROVEN`'s `Kind` is
held to the closed `PROOF_KINDS` vocabulary above; `GUARDED`'s `Kind` is free
text citing its wave and red-first test (design doc section 3) and is
**not** held to that vocabulary.

## The gate verifies shape, reviews verify semantics

The gate is line-based and performs no reachability analysis of its own: it
checks that every `Class`/`Kind` is a recognized vocabulary word, that
`Kind`-bearing rows carry a `Proof` of at least `MINIMUM_PROOF_TEXT_LENGTH`
characters, and that `entry-guard`/`dominating-resolve`/`caller-contract` rows
cite a `file:line`. Those checks close the *trivially gameable* one-word-row
class -- they cannot tell a semantically correct proof from a
plausible-sounding wrong one. A `PROVEN` row with wrong reasoning is a
**review** finding, not a gate finding (design doc section 3/section 8): the
dual adversarial whole-branch reviews audit the `PROVEN` set by sampling, with
`caller-contract` rows (the hardest kind) checked citation-by-citation.

## Occupant-chain proof caveats

A row proved by the `occupant-chain` kind must state, in its own proof text,
which half of the occupant-chain invariant it relies on (design doc section 3):
the invariant's **contrapositive gives `!= NOWHERE` only** -- it does not by
itself prove the id is *in range*, only that it is not the sentinel. In-range-
ness instead follows from the WRITE side, in the wording ruling R3-C-2 fixes
below ("The in-range half's standing citation"): the location field's only
writers are `char_to_room` and `ScopedRenderLocation`, plus append-only room
allocation (a room id, once valid, stays valid for the process lifetime). The
invariant's second half -- that the occupant-chain-derived id still equals the
character's own stored location field -- does **not** hold inside a
`ScopedRenderLocation` window (the render cursor temporarily diverges from the
stored field by design); an `occupant-chain` proof taken from inside such a
window must say so explicitly, or it is incomplete.

## The location_of() guard proves only the sentinel half

The SAME two-half distinction applies to an `entry-guard` or `caller-contract`
proof built on a `location_of(ch) != NOWHERE` test, not only to
`occupant-chain` proofs (review-1 W-4): the guard establishes only that the
id is not the `NOWHERE` sentinel -- it does not, by itself, establish that the
id is *in range*. A row proved by
`entry-guard`/`caller-contract`/`dispatch-invariant` over a `location_of()`
test must therefore state BOTH halves in its own proof text -- the sentinel
exclusion AND the in-range justification -- not the sentinel half alone.

### The in-range half's standing citation (R3-C-2)

Wave R1/R2 rows spell the in-range half as "in-range via M-1
(`placement.cpp:369-395`) + append-only allocation". **That citation is wrong
about what it points at**: `src/entity/placement.cpp:369-395` is
`ScopedRenderLocation`'s own explanatory banner and precondition discussion,
not an in-range argument. Coordinator ruling R3-C-2 replaces it, from Wave R3
onward, with a citation that is honest about where in-range-ness actually
comes from -- the WRITE side, which this same program drains:

> in-range: the location field's only writers are `char_to_room`
> (`src/entity/placement.cpp:548`, whose own unconditional resolve at `:564`
> is the program's RR-O-1 site and catches every invalid id -- mudlog today,
> abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real
> in `would_break_the_absence_invariant`, `src/entity/placement.cpp:410-420`,
> enforced from the constructor at `:427` and from `retarget` at `:440`, and
> otherwise spoofs only ids its callers supply), plus append-only room
> allocation.

Two notes on this section's own citations. **First**, R3-C-2's drafted wording
cited the `ScopedRenderLocation` half as `:369-391`; every line above was
re-read at this commit, and the refusal is implemented at `:410-420` and
enforced at `:427`/`:440`, so the span is corrected here rather than copied
forward (the wave's standing "every line number re-read before citation"
rule). **Second**, this is an INHERITED proof, not a self-standing one: the
in-range half rests on the write side's own unconditional resolve, which is
exactly the site the program's final flip wave converts from mudlog to abort.
Saying so plainly is the point of the wording.

**R2's landed rows are NOT rewritten.** Every Wave R2 `PROVEN`/`GUARDED` row
still carries the `M-1 (placement.cpp:369-395)` boilerplate in its own `Proof`
cell. Those rows' CONCLUSIONS are correct; only the citation pointed at the
wrong lines, so R3-C-2 fixes the one prose section they all lean on instead of
editing dozens of `Proof` cells -- churn with no reviewable content, and a
diff that would bury the rows a reviewer actually needs to read. **This
section supersedes that boilerplate**: wherever an R2 row says "in-range via
M-1 (`placement.cpp:369-395`)", read the block-quoted wording above instead.
Rows landed from Wave R3 onward cite the new wording directly.

## `dispatch-invariant` proofs and the dispatch-entry registry

`dispatch-invariant` (Wave R3, owner ruling R3-O-1) is the sixth and newest
`PROOF_KINDS` entry, and the first whose evidence lives ENTIRELY OUTSIDE the
row's own function. It covers the pattern that dominates `src/combat/` and
`src/script/`: a body resolves the room of an actor (`ch`/`caster`) it never
validated, because the actor arrived through a dispatch mechanism -- an
`cmd_info[].command_pointer` ACMD dispatch, the `combat_command` table, a
`skills[].spell_pointer` door, one of the two SPECIAL invokers, or the mob-AI
driver. The proof is that the DISPATCHER guards the actor before dispatching:
one tripwire per entry point, on the actor argument, in the
`db_world.cpp:2083` idiom (`if (location_of(ch) == NOWHERE) { mudlog(<globally
unique string>); return; }`).

**A `dispatch-invariant` proof must carry three citation parts. All three, or
the row is not proven:**

1. **The registry entry** -- the `file:line` of the dispatch-entry row whose
   guard covers this row's actor. A `dispatch-invariant` row that cites no
   entry cites nothing; this is also why the kind is in
   `CITATION_REQUIRED_KINDS`.
2. **The actor parameter, by name.** The guard covers ONE argument. A proof
   that does not say which one is unreviewable, and silently over-claims for
   any `victim`/target-derived id -- ruling R3-C-5: the tree's
   `location_of(A) == location_of(B)` equality checks all pass when BOTH are
   NOWHERE, so a target id inherits the caster's status and nothing more.
   Rows whose id is target-derived are classified on their own merits.
3. **The body's exhaustive direct-caller list**, with every door that is NOT
   the registered dispatcher proven separately or the row split. A body called
   directly from elsewhere (`do_mental` from `do_hit`/`perform_violence`,
   `perform_drop` from `script.cpp`) does not inherit the entry's guard on
   that path.

The in-range half is cited exactly as "The in-range half's standing citation"
above states it -- the entry guard, like any `location_of()` test, excludes
only the sentinel.

**The closure.** The kind is sound only while the entry set is CLOSED, so the
entry points are enumerated in the marker-anchored **dispatch-entry registry**
at the end of this file (`<!-- ROOM-RESOLVE-DISPATCH-ENTRIES -->`, exactly
once, outside any fence -- the M10 rule the classification and token-counts
tables already follow). `tools/room_resolve_census.py --check` asserts two
directions over it, the same bidirectional shape
`tools/location_read_census.py`'s location-state registry uses:

- **Downward:** every `GUARDED`/`GUARDED-PRIOR` row names a real file and a
  real, scanner-findable function whose comment/string-MASKED body still
  contains EVERY guard literal that row lists (verbatim up to whitespace
  normalization, since a real guard wraps across physical lines and a table
  cell cannot). Deleting a guard from production is a gate failure, not
  silent proof rot -- and deleting ONE of an entry's two guards is the same
  failure, since a two-tripwire entry is only as strong as its weakest (see
  "An entry may carry MORE THAN ONE guard literal" below).
- **Upward:** every occurrence of a pinned dispatch spelling
  (`command_pointer)(`, `g_command_table[`, `spell_pointer)(`,
  `.spell_pointer(`, `activate_char_special(`, `activate_obj_special(`,
  `shape_center(`) in production code lies inside a registered entry's
  function. An eighth dispatcher is a gate ERROR naming the unregistered
  site, not a free inheritance of the proof. Occurrences attributed to file
  scope are skipped -- those are the dispatch machinery's own declarations
  and definition heads, never calls -- and exactly two real sites are pinned
  as reviewed exemptions in the script's `DISPATCH_TOKEN_EXEMPT_SITES` (see
  below).

`PENDING-T1b` is a transitional status: the entry's guard is SPECIFIED but not
yet LANDED, so the row carries no guard literal and is exempt from the
downward direction only (it must still name a real file and a real function).
`--check` prints a loud WARNING for every `PENDING-T1b` row that remains, so
the wave cannot finish with one standing.

**How a guard literal is SPELLED, and why it looks masked.** The downward
check runs against the function's comment/string-MASKED body, so a
literal that quotes a `mudlog()` call must quote it as the masker leaves
it -- the message's contents (and its quotes) are blanked, so
`mudlog("...", NRM, LEVEL_IMPL, TRUE);` normalizes to `mudlog( , NRM,
LEVEL_IMPL, TRUE);`. Wave R3 Task 1b's nine rows use that spelling
deliberately: a literal of just `if (location_of(ch) == NOWHERE) {` is
satisfied by ANY absence test anywhere in the function, so deleting the
tripwire and leaving some unrelated NOWHERE check behind would pass. The
full statement -- condition, `mudlog()` call, and the exit keyword, which
differs per site (`return` / `return 0` / `continue`) -- pins the
whole shape. (`command_interpreter`'s entry was an `if/else` refusal in Task
1b, because at that placement -- immediately above the ACMD dispatch -- an
early `return` would have skipped the parsed targets' cleanup. Task 1c moved
the guard to the top of the position-check `else` block so it also dominates
`target_parser`, whose `target_from_word` resolves `room_of(ch)`/`EXIT(ch, ..)`
from the same actor (T3d finding O-2); nothing is parsed yet at that point, so
the entry is now an ordinary `return`.) The globally unique message TEXT is the separate half of the
contract: it is what R-final's measured-zero sweep greps for, and it lives
in production, not here.

**An entry may carry MORE THAN ONE guard literal** (Wave R3 Task 1d,
coordinator ruling R3-C-7). One tripwire per entry point is not always
enough. R3-C-7's adjacency rule says a `dispatch-invariant` row inherits an
entry's guard ONLY if no statement between that guard and the dispatch the
row's site is reached through can run code with the actor as a participant --
a `special()` fan-out, a `complete_delay()`, an `affect_total()` APPLY_SPELL
walk, any `command_interpreter` re-entry. The structural way to satisfy it is
ADJACENCY: the tripwire sits IMMEDIATELY before the dispatch statement, with
only scalar/local work in between. But an entry function that must ALSO
resolve or parse the actor EARLIER than its dispatch -- `do_cast`'s
`room_of(ch)` at spell_pa.cpp:505 and its `target_from_word` call at :624,
`command_interpreter`'s `target_parser` at interpre.cpp:1130 -- still needs a
guard THERE, for those sites. So such an entry carries TWO tripwires: an
early one licensing the early sites, and a pre-dispatch one licensing
everything reached through the dispatch. Both share the entry's one globally
unique message string (the sweep counts entry points, not statements).

The `Guard literal` cell then lists both, separated by ` || ` -- spaces
around, and the bars written `\|\|` in the markdown so the row survives cell
splitting -- and `--check` requires EVERY listed literal to be present. The
separator is parsed STRUCTURALLY, anchored on the backticks that wrap each
literal, never by a bare split: `one_mobile_activity`'s single literal
contains a real C `||` operator, and a naive split would tear it into two
halves that each still match, silently weakening the check to nothing.

Because the two guards license DIFFERENT sets of sites, every
`dispatch-invariant` proof cites the specific guard LINE it rests on, not
merely the entry. A row whose site is reached through the dispatch cites the
pre-dispatch line; a row whose site sits above the first relocating call
cites the early one.


**Deliberately NOT an entry: `affect_modify`'s APPLY_SPELL arm**
(`src/entity/entity_lifecycle.cpp:2440`/`:2442`). That arm runs a real
`ASPELL` with `caster == victim == ch` at NOWHERE **by design**, inside the
login/rent-load window (`src/persist/db_players.cpp:1403` sets NOWHERE and
calls `affect_total` on the next line; `src/app/objsave.cpp:506`'s
`Crash_load` runs the same way before placement). A tripwire there would fire
on every login of a character carrying an `APPLY_SPELL` affect. It is pinned
in `DISPATCH_TOKEN_EXEMPT_SITES` with that reason, and every row reachable
through it stays `TODO` under the `APPLY_SPELL-window` category below (owner
ruling R3-O-2). The other pinned exemption is
`src/combat/combat_hooks.cpp:56`'s `g_command_table[i] = handler;` -- a
registration WRITE, at which no actor exists at all.

## Caller-count pins (R3-O-3)

`src/combat/visibility.cpp · CAN_SEE` and `· get_char_room_vis` are the two
scale-flagged rows Wave R3 does NOT drain: both stay `TODO`, waiting for the
same batch treatment `CAN_GO`/`obj_to_room` are queued for. Both are also
id-TAKING in the sense "The caller-contract token-pinning policy" blind spot
below describes -- any future proof of them rests on "every caller is
accounted for" -- and that policy's own remedy is to pin the name as a
SCANNED TOKEN, so a new caller surfaces as its own ledger site.

Here that remedy is unaffordable. The two names have **127 production call
sites** between them; pinning them in `token_patterns()` would mint roughly
that many new ledger rows and raise `MAXIMUM_TODO_COUNT` by the same amount,
for zero proof value. **Owner ruling R3-O-3's deliberate deviation: pin the
COUNTS instead.** `PINNED_CALLER_COUNTS` in `tools/room_resolve_census.py`
carries the measured figures, and `--check` fails on drift in EITHER
direction (a new caller is what the pin exists to catch; a removed caller
means the literal is stale and would stop catching the next one), with a
message naming the two-edit fix -- the script literal and this section, always
updated together.

| Name | Pinned production call sites |
|---|---|
| `CAN_SEE(` | 86 |
| `get_char_room_vis(` | 41 |

Measured at this commit by the method recorded verbatim beside
`PINNED_CALLER_COUNTS`: every `SOURCE_SUFFIXES` file under `src/` except
`src/tests/`, comment/string-masked with the tool's own masker, attributed
with the tool's own `attribute_lines`, counting every regex occurrence (per
occurrence, not per line) on a line whose attribution is NOT `file-scope`.
The file-scope exclusion is what drops the declarations and definition heads
(`src/utils.h:669`/`:670` and `src/combat/visibility.cpp:107`/`:527` for
`CAN_SEE`; `src/handler.h:509` and `src/combat/visibility.cpp:617` for
`get_char_room_vis`) while KEEPING real calls that live inside a macro body --
`src/script/spec_pro.cpp:2345` is one such `CAN_SEE(` call, and counting only
`function`-attributed lines would have silently lost it. Neither name occurs
anywhere in `src/tests/` today, so the production scoping is discipline
rather than a concession.

## Stayed-TODO taxonomy: the Wave R3 additions

The canonical taxonomy lives in `docs/superpowers/room-resolve-playbook.md`
("The stayed-TODO taxonomy"), which named four categories out of Wave R2. Wave
R3 adds two and widens one; they are recorded here because R3's rows cite them
and this is the file those rows live in.

- **`APPLY_SPELL-window`** (owner ruling R3-O-2) -- rows reachable with an
  unplaced caster through `affect_modify`'s APPLY_SPELL arm (producer P1, the
  login/rent-load window; see the dispatch-entry section above for why that
  arm is deliberately unguarded). 11 combat rows / 16 sites at R3. These are
  NOT covered by `dispatch-invariant` and are not silently left: they stay
  `TODO` as a named class, to be ruled together with the program's pending
  `RR-O-1` ruling, since both are rulings about the same login window.
- **`owner-punted`** (owner ruling R3-O-4) -- `src/combat/olog_hai.cpp ·
  get_random_target` (1 row / 2 sites) has zero production callers; the
  repository's own dead-code heuristic would delete it, but the owner ruled it
  is NOT deleted, NOT proven, and NOT to be removed: intent is unknown and the
  disposition is a future design decision. The row stays `TODO` with that
  reason, and no proof is to be written for it.
- **`intervening-relocation`** (Wave R3 Task 1c) -- a row whose every door IS a
  registered dispatch entry, but where a statement between one entry's guard and
  the site can move the actor, so the guard's `location_of(...) != NOWHERE`
  conclusion does not survive to the site. `src/combat/visibility.cpp ·
  target_from_word · EXIT(` (1 row / 2 sites, both visibility.cpp:1164) is the
  first: its `command_interpreter` door is fully dominated after Task 1c, but its
  `do_cast` door (src/combat/spell_pa.cpp:624, reachable with `TAR_DIR_NAME` set
  by the `freeze` spell) sits below `complete_delay(ch)` at
  src/combat/spell_pa.cpp:516, which re-enters `command_interpreter`
  (src/app/comm.cpp:2837) and from there dispatches arbitrary spec procs through
  `special()`; `appear(ch)` at src/combat/spell_pa.cpp:721 is a second such
  statement (`affect_from_char` -> `affect_remove` -> `affect_total` ->
  `affect_modify`'s APPLY_SPELL arm, which runs an arbitrary ASPELL with
  `caster == victim == ch`). This is NOT the T3d "guard does not dominate" shape,
  which was about textual order and is now closed; it is about what runs in
  between. It is the open question T2d's report flagged against the `do_cast`
  entry, and it needs an owner/coordinator ruling, not a classification decision.
- **`scale-flagged`** (widened by owner ruling R3-O-3) -- already carrying
  `CAN_GO` and `obj_to_room` from Wave R2, this category now also covers
  `src/combat/visibility.cpp`'s `CAN_SEE` (the `:578`/`:580` half; the `:121`
  entry-guard half drains normally, so the row splits) and
  `get_char_room_vis`. See "Caller-count pins (R3-O-3)" above for the
  compensating control these two carry while they wait.

## The two-room-macro rule

`IS_SUNLIT_EXIT`/`IS_SHADOWY_EXIT` each take **two** room-id arguments (the
current room and the adjacent room across a door). A row classifying either
macro's call site must state a proof covering **both** room-id arguments
separately -- proving only one leaves the other's validity unaddressed
(design doc section 6/O-13). (The macros' own unchecked `dir_option[door]`
dereference is a distinct, explicitly out-of-scope defect class -- design doc
section 6 -- these rows prove the room-id arguments only, and say so.)

## The line-split-call note

Every token pattern anchors on the callee name immediately followed by `(`,
matched per physical line: a call whose *arguments* wrap to a following line
(`` room_of(\n    ch) ``) still hits, but a genuinely split spelling (the
callee name and its `(` on two different physical lines, e.g.
`` room_of\n    (ch) ``) cannot, by this line-based design (carried from Task
2's docstring note in this file). A one-time real-tree grep for that second
shape (`grep -rnE "room_of\s*$" src --include="*.cpp"`) found **zero
matches** at this commit -- the shape does not exist in production today.

## An `LS1-ALLOW` annotation is NOT a proof

`tools/location_read_census.py`'s `LS1-ALLOW` annotation licenses
*representation access* -- whether a raw `->in_room`/`world[`/etc. spelling is
allowed to exist outside the Stage-1 Placement API at all. This ledger asks a
**different** question: *input validity* -- whether the id handed to a
resolver-reaching spelling can be proven in-range before it is dereferenced. A
site can be simultaneously `LS1-ALLOW`'d (representation access is fine) and
an unclassified `TODO` here (its input validity is still unproven) -- the two
gates ask two different questions and keep two different ledgers (design doc
section 1, the `world[` bullet -- review O-1/F-1, both spec reviews' top
finding).

## Known reconciliation blind spots

The gate's cross-checks are count-based, not identity-based, which leaves a
few structural blind spots recorded here rather than silently discovered
later (review-1 F-5/F-6/F-7/W-12):

- **The same-function site-swap limit.** Deleting one already-`GUARDED` site
  from a function and adding a new, unguarded site to the SAME function in
  the SAME edit needs no ledger edit at all if the function's per-key site
  count for that token does not change -- sharper than ordinary two-sided
  drift, since nothing here even changes a row's `Count`. This is a review
  concern, not a gate one: the gate can only ever see aggregate counts per
  `(file, function, token)` key, never individual call-site identity.
- **Cross-class count-shuffling between rows sharing a key is review
  territory.** Two rows for the same `(file, function, token)` key but
  different classes (e.g. one `PROVEN` site and one `TODO` site inside the
  same function, same token) can trade sites between themselves while their
  COMBINED total stays fixed, and the gate cannot tell which physical site
  moved where. A mixed-class key's rows should disambiguate which sites each
  row actually covers in their own proof text (R2 rule) -- the reviews check
  this by reading the source, not by rerunning the gate.
- **`#decl` conflates three different things**: a genuine forward
  declaration, a definition HEADER line the scanner's attribution missed
  (not yet inside the body), and a truly unattributable site. A reader must
  not assume every `#decl` row is a pure declaration -- see `PINNED_DECL_KEYS`
  above for why the `.cpp`-file case is pinned shut rather than left open to
  silently reclassify.
- **The caller-contract token-pinning policy.** A `caller-contract` proof
  for an id-TAKING function (one whose room-id argument comes from a caller,
  not a loop/table it owns itself) is only as strong as the claim that every
  caller is accounted for. Such a proof must either have the function's own
  name pinned as a token this gate scans for directly (so a NEW caller
  anywhere would itself need a ledger row), or enumerate every existing
  caller explicitly in the proof text -- the `world_room_vnum`/
  `dispatch_room_vnum` hook-dispatch pair (`STATIC_TOKEN_PATTERNS` above) is
  the precedent: pinning the dispatch alias itself as a token is what lets a
  future SECOND registered target surface as its own new site instead of
  silently inheriting the first target's proof.
- **Recorded scan-scope limits.** The scan is `<root>/src` only (via
  `_resolve_scan_targets`'s default) -- nothing outside `src/` is ever
  scanned or can carry a ledger row. `collect_define_bodies` keeps
  LAST-WINS on a duplicate macro name across scanned files (a later
  `#define NAME(...)` silently overwrites an earlier one in the body map
  `derive_macro_family` closes over) -- two files defining the same macro
  name differently is a latent blind spot this gate does not detect.

<!-- ROOM-RESOLVE-CLASSIFICATION -->
| Key | Count | Class | Kind | Proof |
| --- | --- | --- | --- | --- |
| `src/app/act_comm.cpp · do_gen_com · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_comm.cpp · do_gen_com · room_of(` | 3 | TODO | — | — |
| `src/app/act_comm.cpp · do_gsay · room_of(` | 1 | TODO | — | — |
| `src/app/act_comm.cpp · do_say · room_of(` | 2 | TODO | — | — |
| `src/app/act_info.cpp · do_affections · OUTSIDE(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_affections · SUN_PENALTY(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_exits · EXIT(` | 13 | TODO | — | — |
| `src/app/act_info.cpp · do_exits · IS_SUNLIT_EXIT(` | 4 | TODO | — | — |
| `src/app/act_info.cpp · do_exits · room_by_id_total(` | 2 | TODO | — | — |
| `src/app/act_info.cpp · do_exits · room_of(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_info · room_of(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_look · EXIT(` | 27 | TODO | — | — |
| `src/app/act_info.cpp · do_look · IS_DARK(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_look · IS_SHADOWY_EXIT(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_look · IS_SUNLIT_EXIT(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_look · SUN_PENALTY(` | 3 | TODO | — | — |
| `src/app/act_info.cpp · do_look · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_look · room_of(` | 26 | TODO | — | — |
| `src/app/act_info.cpp · do_map · room_of(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_search · room_of(` | 2 | TODO | — | — |
| `src/app/act_info.cpp · do_small_map · room_of(` | 1 | TODO | — | — |
| `src/app/act_info.cpp · do_who · room_of(` | 2 | TODO | — | — |
| `src/app/act_info.cpp · perform_immort_where · room_by_id_total(` | 4 | TODO | — | — |
| `src/app/act_info.cpp · perform_immort_where · room_of(` | 6 | TODO | — | — |
| `src/app/act_info.cpp · perform_mortal_where · room_of(` | 8 | TODO | — | — |
| `src/app/act_info.cpp · show_room_weather · room_of(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · check_simple_move · EXIT(` | 2 | TODO | — | — |
| `src/app/act_move.cpp · check_simple_move · room_by_id_total(` | 4 | TODO | — | — |
| `src/app/act_move.cpp · check_simple_move · room_of(` | 4 | TODO | — | — |
| `src/app/act_move.cpp · do_close · EXIT(` | 14 | TODO | — | — |
| `src/app/act_move.cpp · do_close · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_enter · EXIT(` | 7 | TODO | — | — |
| `src/app/act_move.cpp · do_enter · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_enter · room_of(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_leave · EXIT(` | 4 | TODO | — | — |
| `src/app/act_move.cpp · do_leave · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_leave · room_of(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_lock · EXIT(` | 10 | TODO | — | — |
| `src/app/act_move.cpp · do_lock · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_move · EXIT(` | 8 | TODO | — | — |
| `src/app/act_move.cpp · do_move · room_by_id_total(` | 3 | TODO | — | — |
| `src/app/act_move.cpp · do_move · room_of(` | 10 | TODO | — | — |
| `src/app/act_move.cpp · do_open · EXIT(` | 15 | TODO | — | — |
| `src/app/act_move.cpp · do_open · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · do_pull · room_by_id_total(` | 2 | TODO | — | — |
| `src/app/act_move.cpp · do_unlock · EXIT(` | 11 | TODO | — | — |
| `src/app/act_move.cpp · do_unlock · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · msdp_room_update_impl · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_move.cpp · msdp_room_update_impl · room_of(` | 7 | TODO | — | — |
| `src/app/act_move.cpp · perform_move_mount · EXIT(` | 2 | TODO | — | — |
| `src/app/act_move.cpp · perform_move_mount · room_by_id_total(` | 3 | TODO | — | — |
| `src/app/act_move.cpp · perform_move_mount · room_of(` | 6 | TODO | — | — |
| `src/app/act_move.cpp · set_blood_trail · room_of(` | 4 | TODO | — | — |
| `src/app/act_obj1.cpp · do_butcher · room_of(` | 2 | TODO | — | — |
| `src/app/act_obj1.cpp · do_get · room_of(` | 2 | TODO | — | — |
| `src/app/act_obj1.cpp · get_from_room · room_of(` | 3 | TODO | — | — |
| `src/app/act_obj1.cpp · perform_get_from_room · room_of(` | 2 | TODO | — | — |
| `src/app/act_obj2.cpp · do_blowout · room_of(` | 1 | TODO | — | — |
| `src/app/act_obj2.cpp · do_blowout · world[` | 1 | TODO | — | — |
| `src/app/act_obj2.cpp · do_drink · room_of(` | 4 | TODO | — | — |
| `src/app/act_obj2.cpp · do_eat · room_of(` | 1 | TODO | — | — |
| `src/app/act_obj2.cpp · do_light · room_of(` | 1 | TODO | — | — |
| `src/app/act_obj2.cpp · do_light · world[` | 1 | TODO | — | — |
| `src/app/act_obj2.cpp · do_pour · room_of(` | 3 | TODO | — | — |
| `src/app/act_offe.cpp · can_bear_skill · room_of(` | 1 | TODO | — | — |
| `src/app/act_offe.cpp · do_bash · EXIT(` | 19 | TODO | — | — |
| `src/app/act_offe.cpp · do_bash · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_offe.cpp · do_bash · room_of(` | 1 | TODO | — | — |
| `src/app/act_offe.cpp · do_flee · EXIT(` | 5 | TODO | — | — |
| `src/app/act_offe.cpp · do_flee · room_by_id_total(` | 3 | TODO | — | — |
| `src/app/act_offe.cpp · do_flee · room_of(` | 3 | TODO | — | — |
| `src/app/act_offe.cpp · do_hit · room_of(` | 1 | TODO | — | — |
| `src/app/act_offe.cpp · do_kick · room_of(` | 1 | TODO | — | — |
| `src/app/act_offe.cpp · do_rescue · room_of(` | 1 | TODO | — | — |
| `src/app/act_othe.cpp · do_fish · room_of(` | 1 | TODO | — | — |
| `src/app/act_othe.cpp · do_knock · EXIT(` | 10 | TODO | — | — |
| `src/app/act_wiz.cpp · do_echo · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_purge · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_purge · room_of(` | 7 | TODO | — | — |
| `src/app/act_wiz.cpp · do_register · room_by_id_total(` | 3 | TODO | — | — |
| `src/app/act_wiz.cpp · do_register · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_rehash · room_by_id_total(` | 3 | TODO | — | — |
| `src/app/act_wiz.cpp · do_show · room_by_id_total(` | 6 | TODO | — | — |
| `src/app/act_wiz.cpp · do_show · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_stat_character · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_stat_object · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_stat_room · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_stat_room · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_zone · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · do_zreset · room_of(` | 1 | TODO | — | — |
| `src/app/act_wiz.cpp · find_target_room · room_by_id_total(` | 5 | TODO | — | — |
| `src/app/boards.cpp · find_board · room_of(` | 1 | TODO | — | — |
| `src/app/comm.cpp · act_impl · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/comm.cpp · act_impl · room_of(` | 1 | TODO | — | — |
| `src/app/comm.cpp · clean_expose_elements · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/comm.cpp · msdp_update · OUTSIDE(` | 1 | TODO | — | — |
| `src/app/comm.cpp · msdp_update · room_of(` | 3 | TODO | — | — |
| `src/app/comm.cpp · send_to_outdoor · OUTSIDE(` | 1 | TODO | — | — |
| `src/app/comm.cpp · send_to_outdoor · room_of(` | 1 | TODO | — | — |
| `src/app/comm.cpp · send_to_room_except_impl · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/comm.cpp · send_to_room_except_two_impl · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/comm.cpp · send_to_room_impl · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/comm.cpp · send_to_sector · OUTSIDE(` | 1 | TODO | — | — |
| `src/app/comm.cpp · send_to_sector · room_of(` | 1 | TODO | — | — |
| `src/app/comm.cpp · zone_is_populated · room_of(` | 1 | TODO | — | — |
| `src/app/db_boot.cpp · record_crime · room_of(` | 1 | TODO | — | — |
| `src/app/handler.cpp · extract_char · room_by_id_total(` | 2 | TODO | — | — |
| `src/app/handler.cpp · extract_char · room_of(` | 3 | TODO | — | — |
| `src/app/handler.cpp · extract_char · world[` | 1 | TODO | — | — |
| `src/app/interpre.cpp · special · room_by_id_total(` | 4 | TODO | — | — |
| `src/app/objsave.cpp · Emergency_save · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/objsave.cpp · do_rent · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/objsave.cpp · gen_receptionist · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/objsave.cpp · gen_receptionist · room_of(` | 1 | TODO | — | — |
| `src/app/protocol.cpp · broadcast_weather_msdp_update · OUTSIDE(` | 1 | TODO | — | — |
| `src/app/protocol.cpp · broadcast_weather_msdp_update · room_of(` | 1 | TODO | — | — |
| `src/app/shop.cpp · closing_time · EXIT(` | 14 | TODO | — | — |
| `src/app/shop.cpp · closing_time · room_of(` | 1 | TODO | — | — |
| `src/app/shop.cpp · has_already · room_by_id_total(` | 2 | TODO | — | — |
| `src/app/shop.cpp · opening_time · EXIT(` | 4 | TODO | — | — |
| `src/app/shop.cpp · shopping_buy · room_by_id_total(` | 1 | TODO | — | — |
| `src/app/shop.cpp · shopping_list · room_by_id_total(` | 2 | TODO | — | — |
| `src/app/utility.cpp · log_death_trap · room_of(` | 2 | TODO | — | — |
| `src/combat/clerics.cpp · do_mental · room_of(` | 1 | TODO | — | — |
| `src/combat/fight.cpp · death_cry · room_by_id_total(` | 1 | GUARDED | RR Wave R3 Task 3p (fight.cpp:927) | `death_cry_cursor.retarget(room_by_id_total(was_in)->dir_option[door]->to_room);` at src/combat/fight.cpp:946, where `was_in = location_of(ch);` (:910) was unguarded and the only dominating test is `if (CAN_GO(ch, door))` at :945 — NOT a location guard: `CAN_GO`'s own first line is `room_data* r = room_of(ch);` (src/entity/environment_utils.cpp:139), so with `was_in == NOWHERE` it answers off the ROOM-0 fallback and can legitimately return 1, after which :946 resolves NOWHERE a second time and the render cursor is retargeted to one of room 0's neighbours — the death cry is then broadcast into an unrelated room, plus two negative-room mudlogs per passable door. Both this row and its `get_corpse_desc` sibling come from ONE raw_kill() invocation, one statement apart (fight.cpp:982 `death_cry(dead_man);`, :983 `make_corpse(dead_man, ...)` -> make_physical_corpse:749 -> get_corpse_desc), and NONE of raw_kill()'s FOURTEEN production call sites tree-wide (re-grepped at this commit: act_move.cpp:400/:423/:830, act_offe.cpp:262, fight.cpp:1126/:1151/:1180/:2137/:3284/:3350, limits.cpp:1425/:1618, script.cpp:1653, spec_pro.cpp:3277) guards `dead_man`'s placement. The tree documents the unplaced case as LEGITIMATE rather than hypothetical: src/entity/placement.cpp:396-399 and src/entity/include/rots/entity/render_cursor.h:121 both record that death_cry()'s `ScopedRenderLocation` window is deliberately permitted to open with `was_in == NOWHERE`, "for a character who really is nowhere", which is exactly why no proof kind can close either row. ABSENT BEHAVIOR (stated before the guard was written): the per-door broadcast is skipped entirely for an unplaced character. Nothing else is lost — the opening `act(cry_msg, FALSE, ch, 0, 0, TO_ROOM)` at :909 is ALREADY a no-op in that state, since comm.cpp's `act_impl` takes neither its `ch && location_of(ch) != NOWHERE` arm nor its `obj` arm and returns at `if (!to)`. THE GUARD: `if (was_in == NOWHERE) { return; }` at fight.cpp:927, immediately after the read. RED-FIRST: `DeathCryTest.SkipsThePerDoorBroadcastWhenTheDyingCharacterIsNowhere` (src/tests/load_room_placement_tests.cpp) failed against the unguarded body with the real degrade path visible — seven `world[] called for negative room number.` mudlogs followed by the cry arriving in the bystander's room — and its own in-test POSITIVE CONTROL (a PLACED victim, who must still be heard through the per-door loop) passed in the same run, so the assertion cannot pass vacuously. |
| `src/combat/fight.cpp · die · room_of(` | 1 | TODO | — | — |
| `src/combat/fight.cpp · exp_with_modifiers · room_of(` | 2 | TODO | — | — |
| `src/combat/fight.cpp · find_door · EXIT(` | 8 | TODO | — | — |
| `src/combat/fight.cpp · get_corpse_desc · room_of(` | 3 | GUARDED | RR Wave R3 Task 3p (fight.cpp:568) | All three sites are on one logical condition at src/combat/fight.cpp:568 (`room_of(ch)->sector_type == SECT_WATER_NOSWIM \|\| ... SECT_WATER_SWIM \|\| ... SECT_UNDERWATER`), with no guard anywhere in `get_corpse_desc` (:414-579). Both this row and its `get_corpse_desc` sibling come from ONE raw_kill() invocation, one statement apart (fight.cpp:982 `death_cry(dead_man);`, :983 `make_corpse(dead_man, ...)` -> make_physical_corpse:749 -> get_corpse_desc), and NONE of raw_kill()'s FOURTEEN production call sites tree-wide (re-grepped at this commit: act_move.cpp:400/:423/:830, act_offe.cpp:262, fight.cpp:1126/:1151/:1180/:2137/:3284/:3350, limits.cpp:1425/:1618, script.cpp:1653, spec_pro.cpp:3277) guards `dead_man`'s placement. The tree documents the unplaced case as LEGITIMATE rather than hypothetical: src/entity/placement.cpp:396-399 and src/entity/include/rots/entity/render_cursor.h:121 both record that death_cry()'s `ScopedRenderLocation` window is deliberately permitted to open with `was_in == NOWHERE`, "for a character who really is nowhere", which is exactly why no proof kind can close either row. ABSENT BEHAVIOR (stated before the guard was written): for an unplaced character the three reads take ROOM 0's `sector_type` (plus three negative-room mudlogs) and the corpse is described as "floating here" whenever room 0 happens to be watery; the guard makes the wording fall to the default "lying here" instead. THE GUARD: a leading `location_of(ch) != NOWHERE &&` term on the same condition, so the three resolves are short-circuited away for an unplaced character and the site count is unchanged (the `limits.cpp:813` idiom — the sentinel test guards the resolves on its own line). RED-FIRST: `GetCorpseDescTest.UsesTheLyingHereWordingWhenTheDeadCharacterIsNowhere` (src/tests/load_room_placement_tests.cpp) failed against the unguarded body with the real degrade path visible — three `world[] called for negative room number.` mudlogs and the description "The rancid corpse of *a Human* is floating here." — and its own in-test POSITIVE CONTROL (a PLACED victim in a `SECT_WATER_SWIM` room, who must still get the water wording) passed in the same run. Scope note: the neighbouring `obj_to_room(corpse, location_of(character))` at fight.cpp:814 hands the same unproven id to `src/entity/containment.cpp`'s own still-TODO `obj_to_room` row (RR Wave R2's medium-confidence refusal); that obligation is untouched here and is why two negative-room mudlogs remain in the green test run. |
| `src/combat/fight.cpp · group_gain · room_of(` | 1 | PROVEN | entry-guard | `for (auto* character : rots::entity::occupants(room_of(dead_man)))` at src/combat/fight.cpp:1289. The dominating guard is a TWO-STEP test on a DIFFERENT variable than the site's: `if (location_of(killer) == NOWHERE) return;` at :1262 excludes the sentinel for `killer`, and `if (location_of(killer) != location_of(dead_man)) return;` at :1265 transfers that exclusion to `dead_man`. Both dominate :1289, and :1268-:1286 in between are only local-container construction plus `push_back`/`insert` calls — nothing relocates either character. (Census advisory OVERTURNED in part: `occupant-loop? (line 1289)` flags the SITE ITSELF as its own proof, and `entry-guard? (line 1262)` names a guard on the wrong character; the :1265 transfer step, which is what makes the proof, appears in neither.) Sentinel exclusion at :1262 + :1265; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/fight.cpp · perform_drop · room_of(` | 2 | TODO | — | — |
| `src/combat/fight.cpp · raw_kill · room_by_id_total(` | 1 | PROVEN | dominating-resolve | `save_char(dead_man, room_by_id_total(r_mortal_start_room[race])->number, 0);` at src/combat/fight.cpp:1003. The id is NOT a character location: `r_mortal_start_room[]` is filled once at boot by `check_start_rooms()` (src/world/db_world.cpp:804), whose `if ((r_mortal_start_room[tmp] = real_room(mortal_start_room[tmp])) < 0) r_mortal_start_room[tmp] = 0;` at :814-815 is the PRODUCING resolve ruling R3-C-1 demands — a successful `real_room()` whose failure arm is CLAMPED to 0 rather than left to degrade. Every element is therefore either a `real_room()` result (in [0, top_of_world] by this ledger's own `src/world/db_world.cpp · real_room · room_by_id_total(` loop-bound row) or the literal `0`: the array can hold neither NOWHERE nor an out-of-range value, so BOTH halves are established without any `location_of` test, and append-only room allocation keeps them valid for the process lifetime. The line's own in-file comment (fight.cpp:990-1002) records the same derivation as an LS-3a O-2 rider. Recorded, not proven away (and out of this ledger's scope): `race = GET_RACE(dead_man)` (:989) is not bounds-checked against `MAX_RACES` here — contrast limits.cpp:1003, which is — so an out-of-range race would read past the array; that is an ARRAY-index defect, not a room-id one. |
| `src/combat/limits.cpp · affect_update_person · room_of(` | 1 | TODO | — | — |
| `src/combat/limits.cpp · affect_update_room · room_by_id_total(` | 4 | PROVEN | entry-guard | All four sites (src/combat/limits.cpp:1522, :1525, :1528, :1541) resolve `roomnum`, assigned at :1521 as `roomnum = room->dir_option[direction]->to_room;`. That assignment is dominated by a two-step guard in the same `if`/`else if` chain: `if (!(room->dir_option[direction])) { ... }` at :1518 (exit non-null) and `else if (room->dir_option[direction]->to_room != NOWHERE)` at :1520 (sentinel exclusion). Nothing between :1521 and :1541 rewrites `roomnum` or the exit. Sentinel exclusion at :1520; in-range via the exit-target invariant: `renum_world` (src/world/db_world.cpp:844-848) rewrites every non-NOWHERE `dir_option[]->to_room` through `real_room()` at world load, and that field's only FIVE runtime writers tree-wide (re-grepped at this commit: db_world.cpp:797, a raw vnum normalized by :848 before any read; db_world.cpp:848 itself; db_world.cpp:1957, `real_room()` of the room's own vnum, always found; src/olc/shaperom.cpp:309-312, `tmp = real_room(...); if (tmp == -1) tmp = NOWHERE;`; src/script/script.cpp:1035/:1059-1060, guarded by `tmpint != NOWHERE`) all preserve it — so a non-NOWHERE `to_room` is in [0, top_of_world], plus append-only room allocation. (The `room` parameter itself arrives as a `room_data*` from `affect_update()`'s `tmplist->ptr.room`, not as an id, so no resolver reaches it and it is not this row's business.) |
| `src/combat/limits.cpp · check_idling · room_by_id_total(` | 1 | PROVEN | entry-guard | `save_char(character, room_by_id_total(character->specials.was_in_room)->number, 0);` at src/combat/limits.cpp:600, dominated by `if (character && !IS_NPC(character) && (character->specials.was_in_room != NOWHERE))` at :595, inside the `timer > 28` block opened at :594. Census advisory OVERTURNED (right kind, WRONG guard): it points at :576, which opens the `timer > 8` block and asserts `was_in_room == NOWHERE` — the OPPOSITE of what this site needs. Nothing between :595 and :600 rewrites the field (:599's `char_to_room(character, character->specials.was_in_room)` reads it; :601 clears it afterwards). In-range: `specials.was_in_room` has exactly EIGHT writers tree-wide (re-grepped at this commit) — FIVE write the literal `NOWHERE` (comm.cpp:1307, handler.cpp:662, limits.cpp:601, entity_lifecycle.cpp:823, entity_lifecycle.cpp:872) and THREE write `location_of(x)` (fight.cpp:2063, fight.cpp:2832, limits.cpp:577); this corrects the census's own "six NOWHERE / the other two" split, which is internally inconsistent and is really 5 and 3. The field's value set is therefore {NOWHERE} plus {character locations}, so excluding NOWHERE at :595 leaves a character location, and in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. The field is never persisted or reloaded (zero `was_in_room` references anywhere under src/persist/ or any save codec, re-grepped), so no stale across-boot rnum can enter it. |
| `src/combat/limits.cpp · finalize_new_character_start_state · room_by_id_total(` | 1 | PROVEN | entry-guard | `ch->specials2.load_room = room_by_id_total(start_room_rnum)->number;` at src/combat/limits.cpp:1010, dominated by an explicit TWO-SIDED range test `if (start_room_rnum < 0 \|\| start_room_rnum > top_of_world) return;` at :1007 — the value having been read at :1006 from `r_mortal_start_room[race]`, whose index is itself bounds-checked at :1003 (`if (race < 0 \|\| race >= MAX_RACES) return;`). This is the one row in this wave's census-B set that needs no both-halves caveat: :1007 is a real range check, not a sentinel test, so it establishes in-range DIRECTLY rather than inheriting it from the write side. Recorded, not a defect: the upper bound is `top_of_world`, which is conservative relative to the program spec's own `world[i].number >= 0` validity definition (it can reject a legitimately created extension room above `top_of_world` — a behavior question, not a safety one). |
| `src/combat/limits.cpp · point_update · room_by_id_total(` | 3 | TODO | — | — |
| `src/combat/limits.cpp · update_bleed_tracks · room_by_id_total(` | 1 | PROVEN | loop-bound | `tmproom = room_by_id_total(roomnum);` at src/combat/limits.cpp:916, the first statement of `for (roomnum = 0; roomnum <= top_of_world; roomnum++)` at :915. `roomnum` is the loop's own induction variable and is never modified in the body (:918-923 touch only `tmproom->bleed_track[]`), so `roomnum` stays in [0, top_of_world] at every evaluation of the site. Byte-for-byte the same shape as this ledger's `src/world/weather.cpp · age_bleed_tracks · room_by_id_total(` row. |
| `src/combat/limits.cpp · update_room_tracks · room_by_id_total(` | 1 | PROVEN | loop-bound | `tmproom = room_by_id_total(roomnum);` at src/combat/limits.cpp:899, the first statement of `for (roomnum = 0; roomnum <= top_of_world; roomnum++)` at :898; the body (:901-906) touches only `tmproom->room_track[]`, so the induction variable is never modified and `roomnum` stays in [0, top_of_world] at every evaluation. Sibling of this ledger's `src/world/weather.cpp · age_room_tracks · room_by_id_total(` row. |
| `src/combat/mage.cpp · different_zone · room_by_id_total(` | 2 | TODO | — | — |
| `src/combat/mage.cpp · loclife_add_rooms · room_by_id_total(` | 1 | PROVEN | caller-contract | `exptr = room_by_id_total(room.number)->dir_option[dirarray[dmp]];` at mage.cpp:240, no in-function guard; `room` is a by-value `loclife_coord` parameter. Exactly two production callers tree-wide (re-grepped `grep -rn loclife_add_rooms src`), both inside `spell_locate_living`: (a) mage.cpp:471, whose `new_room.number = location_of(caster)` (:469) is sentinel-excluded by that function's entry guard `if (location_of(caster) == NOWHERE) return;` at mage.cpp:458 -- in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. (b) mage.cpp:482, `roomlist[tmp]`, whose `.number` this same function wrote at mage.cpp:249 from `exptr->to_room` under the `(exptr->to_room != NOWHERE)` filter at mage.cpp:243. `tmp` is always inside `[0, roomnum)` there: mage.cpp:273/:274 increment `*roomnum` and the returned `count` together, so `spell_locate_living`'s `notscanned` bookkeeping (mage.cpp:471, then `notscanned += -1 + ...` at :482) equals the number of not-yet-scanned entries in `[0, roomnum)` exactly, and mage.cpp:477's selection loop stops at the `num`-th such entry with `num <= notscanned - 1` (mage.cpp:476). The whole-array zero-init at mage.cpp:462 (`.number = 0`) is a belt-and-braces fallback that this bookkeeping makes unreachable. in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. Adjacent, explicitly out-of-scope defect recorded rather than pursued: mage.cpp:477's selection loop has no upper bound on `tmp` and would index past `roomlist[254]` if that bookkeeping were ever violated -- an array-bounds defect, not a room-id one. |
| `src/combat/mage.cpp · random_exit · room_by_id_total(` | 7 | PROVEN | entry-guard | the 7 sites that resolve the function's own `room` parameter -- mage.cpp:893, mage.cpp:894 (FOUR occurrences on that one physical line), mage.cpp:896 (the INNER `room_by_id_total(room)` of the nested pair on that line) and mage.cpp:899 -- are each dominated by `random_exit`'s own two-sided entry guard at mage.cpp:886 `if ((room < 0) \|\| (room > top_of_world)) return NOWHERE;`. That single test gives BOTH halves locally (sentinel excluded by `< 0`, range by `<= top_of_world`), so no M-1 appeal is needed, and nothing between mage.cpp:886 and mage.cpp:899 assigns `room`. Split from the row below per the ledger's mixed-class-key rule: the eighth site rests on a different dominating test. |
| `src/combat/mage.cpp · random_exit · room_by_id_total(` | 1 | PROVEN | entry-guard | the 1 remaining site is the OUTER resolve of the nested pair at mage.cpp:896 (`romfl = room_by_id_total(room_by_id_total(room)->dir_option[tmp]->to_room)->room_flags;`), whose id is the EXIT's `to_room`, not `room`. Dominated by the enclosing `if` at mage.cpp:894, whose first term is `(room_by_id_total(room)->dir_option[tmp]->to_room != NOWHERE)`, evaluated before the body at mage.cpp:896 runs; the null test at mage.cpp:893 dominates both. Sentinel exclusion at mage.cpp:894; in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. |
| `src/combat/mage.cpp · spell_beacon · room_by_id_total(` | 1 | PROVEN | entry-guard | `room_data* to_room = room_by_id_total(oldaf->modifier);` at mage.cpp:1283, dominated by the two-sided LS-3b O-7 read-arm guard at mage.cpp:1276 `if (oldaf->modifier < 0 \|\| oldaf->modifier > top_of_world) { ... return; }` -- the site sits in that `if`'s own `else` arm, so the guard cannot be bypassed. Both halves come from that one test locally (sentinel excluded by `< 0`, range by `<= top_of_world`), so no M-1 appeal is needed. Already regression-pinned, unusually for this program: `SpellBeaconTest.ReadArmRefusesToReturnWhenTheStoredModifierIsNegativeInsteadOfPlacingTheCasterNowhere`, `SpellBeaconTest.ReadArmStillRefusesToReturnWhenTheStoredModifierExceedsTopOfWorld` and `SpellBeaconTest.RoundTripsToAnInRangeStoredRoomUnchanged` (mage_tests.cpp:847/:880/:911) drive exactly this guard's two refusal arms and its pass-through arm. |
| `src/combat/mage.cpp · spell_beacon · room_of(` | 2 | TODO | — | — |
| `src/combat/mage.cpp · spell_black_arrow · SUN_PENALTY(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_blaze · room_of(` | 3 | PROVEN | dispatch-invariant | All 3 sites sit in the room arm `if (!victim && !obj)` (mage.cpp:2197) and resolve the CASTER's own room: `first_occupant(room_of(caster))` at mage.cpp:2205, `room_affected_by_spell(room_of(caster), SPELL_BLAZE)` at mage.cpp:2234, and `affect_to_room(room_of(caster), &af)` at mage.cpp:2240. The arm's only other test is `if (!caster) return;` (mage.cpp:2198) -- a null test, never a placement test. (i) REGISTRY ENTRIES: `spell_blaze` is entered only through `skills[90].spell_pointer` (registered at spell_pa.cpp:1131), and exactly two of the six tree-wide `spell_pointer` doors can enter this arm -- `do_cast`'s dispatch (spell_pa.cpp:900), guarded at src/combat/spell_pa.cpp:499 (registry entry `do_cast`), and `do_use`'s ITEM_STAFF dispatch (act_othe.cpp:819, which passes `victim = 0, obj = 0` literally), guarded at src/app/act_othe.cpp:806 (registry entry `do_use`). The other four doors CANNOT reach these sites: every one passes a NON-NULL `victim` and this arm requires `!victim` -- `do_use`'s ITEM_WAND dispatch (act_othe.cpp:839, `tmp_char`), `affect_update_room`'s room-affect re-cast (limits.cpp:1498, `tmpch`), and `affect_modify`'s APPLY_SPELL pair (entity_lifecycle.cpp:2440/:2442, `victim == caster == ch`). Each argument list was re-read at this commit, so this row is NOT `APPLY_SPELL-window` (owner ruling R3-O-2) -- the login-window door is blocked by the arm's own shape, not by an exhaustion argument. `cast_mass_spell` (mystic.cpp:1010) is not a door either: it is handed only `spell_regeneration`/`spell_vitality`/`spell_insight` (mystic.cpp:1031/:1041/:1051) and passes a non-null `group_member` (mystic.cpp:1019) regardless. (ii) ACTOR: `caster`, the ASPELL's first parameter -- the identical `char_data*` both live doors pass as argument 1, i.e. `ch`, the actor each of those two registry entries names and guards. Every one of the 3 sites resolves THAT actor's location; none is victim/target-derived, so R3-C-5 does not bite. (iii) DIRECT CALLERS: NONE. A tree-wide grep for `spell_blaze` finds only the declaration (spells.h:440), the nullptr table-cell comment (consts.cpp:547), the registration (spell_pa.cpp:1131), the definition (mage.cpp:2187) and one test-tier row (spell_registry_tests.cpp:85) -- zero production call sites, so there is no unregistered door to prove or split off. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/mage.cpp · spell_blink · room_by_id_total(` | 1 | GUARDED | RR Wave R3 Task 2p (mage.cpp:944) | NOT provable as written, so converted rather than proved. Before this wave `if (IS_SET(room_by_id_total(room)->room_flags, NO_TELEPORT))` at mage.cpp:944 ran unconditionally -- including on the two paths that had just established `room == NOWHERE` (the walk's `if (room == NOWHERE) { fail = 1; break; }` at mage.cpp:935-937 and the post-walk `if (room == NOWHERE) fail = 1;` at mage.cpp:941), five lines before `fail` is first consulted at mage.cpp:949. ABSENT BEHAVIOR, stated: on that path `room_data::operator[]` mudlogged `world[] called for negative room number.` at LEVEL_GOD (db_world.cpp:2082-2085) and returned `world[0]`, whose `NO_TELEPORT` flag then decided a `fail` that was already 1. PRESENT BEHAVIOR: the test is now `if (!fail && IS_SET(...))` -- strictly narrowing and outcome-preserving, because on every `fail == 0` path the new term is a no-op, on every `fail == 1` path the body only re-sets `fail = 1`, and `room` is read nowhere else before mage.cpp:949 (its next readers, mage.cpp:955/:961, are inside the `else` arm). RED-FIRST test `MageProcTest.BlinkDoesNotResolveARoomForAnUnplacedVictimBeforeFailing` (mage_tests.cpp), which fails against the pre-guard body with that exact mudlog on stderr and asserts BOTH halves (the absent mudlog and the still-delivered failure message), so it cannot pass by the function doing nothing. Reachable exactly when `location_of(victim) == NOWHERE` at mage.cpp:926: `random_exit` returns either its own already-range-checked argument (mage.cpp:886/:904) or an exit `to_room` it has tested `!= NOWHERE` (mage.cpp:894/:899), so the walk cannot introduce the sentinel; and `victim` is always the caster here (`TAR_SELF_ONLY`, i.e. `targets == 32 == 1 << 5` -- consts.cpp:519 against `skill_data::targets`, spells.h:366, and types.h:203; the body substitutes the caster for a null `victim` at mage.cpp:922-923). |
| `src/combat/mage.cpp · spell_cone_of_cold · EXIT(` | 7 | PROVEN | dispatch-invariant | All 7 sites are `EXIT(caster, digit)` (utils.h:721, which expands to `room_of((caster))->dir_option[digit]`) in the DIRECTIONAL arm, reached only after `if (victim) { ... return; }` (mage.cpp:1544) falls through: mage.cpp:1572 (twice), :1578, :1579, :1584, :1590 and :1603. The arm's only other test is the direction-range check at mage.cpp:1569, which returns when `digit` is negative or not below `NUM_OF_DIRS` -- a `dir_option` INDEX test, not a room test. (The `room_by_id_total(` occurrence sharing mage.cpp:1603 belongs to the separate, already-PROVEN `spell_cone_of_cold · room_by_id_total(` row, whose id is the exit's `to_room`; this row is the `room_of(caster)` half of that same line.) (i) REGISTRY ENTRIES: `spell_cone_of_cold` is entered only through `skills[93].spell_pointer` (registered at spell_pa.cpp:1134), and exactly two of the six tree-wide `spell_pointer` doors can enter this arm -- `do_cast`'s dispatch (spell_pa.cpp:900), guarded at src/combat/spell_pa.cpp:499 (registry entry `do_cast`), and `do_use`'s ITEM_STAFF dispatch (act_othe.cpp:819, `victim = 0, obj = 0` literally), guarded at src/app/act_othe.cpp:806 (registry entry `do_use`). The other four doors CANNOT reach these sites: every one passes a NON-NULL `victim` and this arm requires `!victim` -- `do_use`'s ITEM_WAND dispatch (act_othe.cpp:839, `tmp_char`), `affect_update_room`'s room-affect re-cast (limits.cpp:1498, `tmpch`), and `affect_modify`'s APPLY_SPELL pair (entity_lifecycle.cpp:2440/:2442, `victim == caster == ch`). Each argument list was re-read at this commit, so this row is NOT `APPLY_SPELL-window` (owner ruling R3-O-2) -- the login-window door is blocked by the arm's own shape, not by an exhaustion argument. `cast_mass_spell` (mystic.cpp:1010) is not a door either: it is handed only `spell_regeneration`/`spell_vitality`/`spell_insight` (mystic.cpp:1031/:1041/:1051) and passes a non-null `group_member` (mystic.cpp:1019) regardless. (ii) ACTOR: `caster`, the ASPELL's first parameter -- the identical `char_data*` both live doors pass as argument 1, i.e. `ch`, the actor each entry names and guards. All 7 sites resolve THAT actor's location; none is victim/target-derived (R3-C-5 does not bite). (iii) DIRECT CALLERS: NONE IN PRODUCTION. A tree-wide grep for `spell_cone_of_cold` finds the declaration (spells.h:422), the nullptr table-cell comment (consts.cpp:554), the registration (spell_pa.cpp:1134), the definition (mage.cpp:1540) and two test-tier calls (spell_registry_tests.cpp:88, mage_tests.cpp:747, the latter passing a non-null victim) -- zero production call sites, so there is no unregistered door. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/mage.cpp · spell_cone_of_cold · room_by_id_total(` | 1 | PROVEN | entry-guard | `for (auto* tmpch : rots::entity::occupants(room_by_id_total(EXIT(caster, digit)->to_room)))` at mage.cpp:1603. The id THIS row proves is the exit's `to_room`; the `EXIT(` occurrence on the same physical line is a different id (`room_of(caster)`) and belongs to the separate `spell_cone_of_cold . EXIT(` row -- named here because the two rows share one line (the ledger's cross-class site-shuffle blind spot). Dominated by mage.cpp:1572 `if (!EXIT(caster, digit) \|\| (EXIT(caster, digit)->to_room == NOWHERE)) { ...; return; }`; the returns at mage.cpp:1580/:1587/:1593 only narrow the path further and nothing between mage.cpp:1572 and mage.cpp:1603 writes the exit. Sentinel exclusion at mage.cpp:1572; in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. OVERTURNS the census advisory `occupant-loop? (line 1603)`: the flagged loop is this very site's consumer, not its proof. |
| `src/combat/mage.cpp · spell_dark_bolt · SUN_PENALTY(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_earthquake · room_by_id_total(` | 4 | PROVEN | entry-guard | the 4 sites in the crack-from-an-existing-exit arm -- mage.cpp:1689 (THREE occurrences on that one physical line) and mage.cpp:1697 -- resolve `crack`, assigned `crack = cur_room->dir_option[DOWN]->to_room;` at mage.cpp:1683 under the dominating `if (cur_room->dir_option[DOWN] && (cur_room->dir_option[DOWN]->to_room != NOWHERE))` at mage.cpp:1682 and the redundant `if (crack != NOWHERE)` at mage.cpp:1684. Sentinel exclusion at mage.cpp:1682/:1684; in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. OVERTURNS the census advisory `entry-guard? (line 1639)`: mage.cpp:1639 guards the CASTER's location and says nothing about `crack` (it is the proof for the sibling `room_of(` row). Split from the row below per the mixed-class-key rule. |
| `src/combat/mage.cpp · spell_earthquake · room_by_id_total(` | 2 | PROVEN | dominating-resolve | the 2 sites in the `else` (create-a-crack) arm -- mage.cpp:1707 and mage.cpp:1709 -- resolve a `crack` produced three lines earlier by `crack = world.create_room(room_of(caster)->zone);` at mage.cpp:1704, and nothing between mage.cpp:1704 and mage.cpp:1709 reassigns it or destroys the room. in-range by construction: `room_data::create_room` (db_world.cpp:1965-2023) either reuses a free in-allocation slot (`place < TOTAL_LENGTH`, :1979-1984) or allocates a fresh `room_data_extension` and returns that block's first index (:1986-2007), then `dummy_room_data(new_room)` initializes the slot and `new_room->number`/`->zone` are stamped (:2011-2020) before it returns; every returned `place` is therefore a real, initialized, resolvable room id on every path -- there is no failure return to test. |
| `src/combat/mage.cpp · spell_earthquake · room_of(` | 4 | PROVEN | entry-guard | all 4 sites -- `cur_room = room_of(caster);` (mage.cpp:1641), `first_occupant(room_of(caster))` (mage.cpp:1666), `world.create_room(room_of(caster)->zone)` (mage.cpp:1704) and `room_of(caster)->create_exit(DOWN, crack)` (mage.cpp:1705) -- are dominated by the entry guard `if (location_of(caster) == NOWHERE) return;` at mage.cpp:1639, itself preceded by the null test at mage.cpp:1637. Sentinel exclusion at mage.cpp:1639; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Residual stated rather than hidden: between the guard and mage.cpp:1704/:1705 the occupant walk at mage.cpp:1666-1679 calls `apply_spell_damage(caster, tmpch, ...)` on OTHER occupants only (`tmpch != caster`, mage.cpp:1668), and `damage()`'s only character-relocating actions are on its `victim` argument -- the flee dispatches at fight.cpp:2049/:2057/:2062 and `die(victim, attacker, attacktype)` at fight.cpp:2078 -- so no path in this function relocates or extracts `caster`. The `ScopedRenderLocation` window at mage.cpp:1695 lives in the mutually exclusive sibling arm and closes at mage.cpp:1698, so no site in this row sits inside a cursor window. |
| `src/combat/mage.cpp · spell_earthquake · world[` | 4 | PROVEN | dominating-resolve | `world[crack].name` (mage.cpp:1708), `.description` (mage.cpp:1710), `.sector_type` (mage.cpp:1714) and `.room_flags` (mage.cpp:1715) all index the `crack` produced by `world.create_room(room_of(caster)->zone)` at mage.cpp:1704, in the same `else` arm, with no intervening reassignment. in-range by construction: `room_data::create_room` (db_world.cpp:1965-2023) either reuses a free in-allocation slot (`place < TOTAL_LENGTH`, :1979-1984) or allocates a fresh `room_data_extension` and returns that block's first index (:1986-2007), then `dummy_room_data(new_room)` initializes the slot and `new_room->number`/`->zone` are stamped (:2011-2020) before it returns; every returned `place` is therefore a real, initialized, resolvable room id on every path -- there is no failure return to test. These are the four `LS1-ALLOW: write` sites the design doc names as surviving raw `world[` indexing -- a worked example of the ledger's `an LS1-ALLOW annotation is NOT a proof` rule: the annotation licenses the spelling, this row supplies the input-validity proof. OVERTURNS the census advisory `entry-guard? (line 1639)`, which guards the caster's location and not `crack`. |
| `src/combat/mage.cpp · spell_expose_elements · OUTSIDE(` | 3 | PROVEN | dispatch-invariant | All 3 sites are `OUTSIDE(caster)` (utils.h:719, which expands to `room_of((caster))`) in the spec-selection switch: mage.cpp:2497 and :2501 (the PS_Arcane arm) and :2547 (the PS_Lightning arm). No test between function entry and any of them touches placement -- the three that exist are the null test at mage.cpp:2442 (`!victim` or `!caster` returns), `utils::is_pc(*victim)` (mage.cpp:2445) and `if (!valid_spec)` (mage.cpp:2462). (i) REGISTRY ENTRIES: `spell_expose_elements` is entered only through `skills[112].spell_pointer` (registered at spell_pa.cpp:1150). Its body requires a NON-NULL `victim` (the null test at mage.cpp:2442 returns when `!victim` or `!caster`), so the reachable doors are `do_cast`'s dispatch (spell_pa.cpp:900), guarded at src/combat/spell_pa.cpp:499 (registry entry `do_cast`), and `do_use`'s ITEM_WAND dispatch (act_othe.cpp:839, which passes `tmp_char`), guarded at src/app/act_othe.cpp:806 (registry entry `do_use`). `do_use`'s ITEM_STAFF dispatch (act_othe.cpp:819) passes `victim = 0` and returns at mage.cpp:2442. `affect_update_room` (limits.cpp:1498) passes `tmpch` -- itself a `first_occupant(room)` walk member, the GUARDED-PRIOR registry entry `affect_update_room` (limits.cpp:1493). `cast_mass_spell` (mystic.cpp:1010) is not a door: it is handed only `spell_regeneration`/`spell_vitality`/`spell_insight` (mystic.cpp:1031/:1041/:1051). THE APPLY_SPELL DOOR IS PROVABLY BLOCKED, and by a hard check rather than an exhaustion argument: `affect_modify`'s APPLY_SPELL pair (entity_lifecycle.cpp:2440/:2442) dispatches with `victim == caster == ch`, so a PC actor returns at `if (utils::is_pc(*victim))` (mage.cpp:2445) and an NPC actor returns at `if (!valid_spec)` (mage.cpp:2462) -- `utils::get_specialization` returns `PS_None` for EVERY NPC by its first statement (it returns `game_types::PS_None` when `is_npc(character)` holds, or when `character.profs` is NULL, entity_lifecycle.cpp:1631-1632), and `PS_None` is not among the six specs mage.cpp:2449-2451 admits. This row is therefore NOT `APPLY_SPELL-window` (owner ruling R3-O-2). (ii) ACTOR: `caster`, the ASPELL's first parameter -- the identical `char_data*` `do_cast` and `do_use` pass as argument 1 (`ch`, the actor each entry names and guards) and that `affect_update_room` passes as `tmpch`. The site resolves THAT actor's location, not `victim`'s: `victim` is used only for the `is_pc` test (mage.cpp:2445) and the `spec_data->exposed_target` store (mage.cpp:2469), so R3-C-5's target-derived exclusion does not apply. (iii) DIRECT CALLERS: NONE. A tree-wide grep for `spell_expose_elements` finds only the declaration (spells.h:491), the nullptr table-cell comment (consts.cpp:591), the definition (mage.cpp:2439), the registration (spell_pa.cpp:1150) and one test-tier row (spell_registry_tests.cpp:104) -- zero production call sites, so there is no unregistered door. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/mage.cpp · spell_expose_elements · room_of(` | 1 | PROVEN | dispatch-invariant | The single site is `const room_data& current_room = *room_of(caster);` at mage.cpp:2471, whose only consumer is `weather_info.sky[current_room.sector_type]` on the next line. It is the sibling of this file's `spell_expose_elements · OUTSIDE(` row and shares its guard analysis exactly: no test between function entry and mage.cpp:2471 touches placement -- the three that exist are the null test at mage.cpp:2442 (`!victim` or `!caster` returns), `utils::is_pc(*victim)` (mage.cpp:2445) and `if (!valid_spec)` (mage.cpp:2462). (i) REGISTRY ENTRIES: `spell_expose_elements` is entered only through `skills[112].spell_pointer` (registered at spell_pa.cpp:1150). Its body requires a NON-NULL `victim` (the null test at mage.cpp:2442 returns when `!victim` or `!caster`), so the reachable doors are `do_cast`'s dispatch (spell_pa.cpp:900), guarded at src/combat/spell_pa.cpp:499 (registry entry `do_cast`), and `do_use`'s ITEM_WAND dispatch (act_othe.cpp:839, which passes `tmp_char`), guarded at src/app/act_othe.cpp:806 (registry entry `do_use`). `do_use`'s ITEM_STAFF dispatch (act_othe.cpp:819) passes `victim = 0` and returns at mage.cpp:2442. `affect_update_room` (limits.cpp:1498) passes `tmpch` -- itself a `first_occupant(room)` walk member, the GUARDED-PRIOR registry entry `affect_update_room` (limits.cpp:1493). `cast_mass_spell` (mystic.cpp:1010) is not a door: it is handed only `spell_regeneration`/`spell_vitality`/`spell_insight` (mystic.cpp:1031/:1041/:1051). THE APPLY_SPELL DOOR IS PROVABLY BLOCKED, and by a hard check rather than an exhaustion argument: `affect_modify`'s APPLY_SPELL pair (entity_lifecycle.cpp:2440/:2442) dispatches with `victim == caster == ch`, so a PC actor returns at `if (utils::is_pc(*victim))` (mage.cpp:2445) and an NPC actor returns at `if (!valid_spec)` (mage.cpp:2462) -- `utils::get_specialization` returns `PS_None` for EVERY NPC by its first statement (it returns `game_types::PS_None` when `is_npc(character)` holds, or when `character.profs` is NULL, entity_lifecycle.cpp:1631-1632), and `PS_None` is not among the six specs mage.cpp:2449-2451 admits. This row is therefore NOT `APPLY_SPELL-window` (owner ruling R3-O-2). (ii) ACTOR: `caster`, the ASPELL's first parameter -- the identical `char_data*` `do_cast` and `do_use` pass as argument 1 (`ch`, the actor each entry names and guards) and that `affect_update_room` passes as `tmpch`. The site resolves THAT actor's location, not `victim`'s: `victim` is used only for the `is_pc` test (mage.cpp:2445) and the `spec_data->exposed_target` store (mage.cpp:2469), so R3-C-5's target-derived exclusion does not apply. (iii) DIRECT CALLERS: NONE. A tree-wide grep for `spell_expose_elements` finds only the declaration (spells.h:491), the nullptr table-cell comment (consts.cpp:591), the definition (mage.cpp:2439), the registration (spell_pa.cpp:1150) and one test-tier row (spell_registry_tests.cpp:104) -- zero production call sites, so there is no unregistered door. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/mage.cpp · spell_fireball · room_of(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_flash · room_of(` | 1 | PROVEN | entry-guard | `for (auto* tmpch : rots::entity::occupants(room_of(caster)))` at mage.cpp:689, dominated by the entry guard `if (location_of(caster) < 0) return;` at mage.cpp:682 (the `< 0` spelling of the sentinel test; `NOWHERE` is `-1`, rots/core/types.h). Only `get_mage_caster_level(caster)` (mage.cpp:687) runs between the guard and the site, and it relocates nothing. Sentinel exclusion at mage.cpp:682; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. OVERTURNS the census advisory `occupant-loop? (line 689)`: mage.cpp:689 IS this site -- the loop consumes the room this call resolves, so it cannot be its proof. |
| `src/combat/mage.cpp · spell_freeze · EXIT(` | 2 | TODO | — | — |
| `src/combat/mage.cpp · spell_lightning_bolt · OUTSIDE(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_lightning_strike · OUTSIDE(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_lightning_strike · room_of(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_locate_living · room_by_id_total(` | 2 | PROVEN | dominating-resolve | `occupants(room_by_id_total(roomlist[tmp].number))` at mage.cpp:491 and `room_by_id_total(roomlist[tmp].number)->name` at mage.cpp:497, both under `for (tmp = 0; (tmp < roomnum) && (bigcount < mobrange); tmp++)` (mage.cpp:490). Every `roomlist` entry in `[0, roomnum)` was PRODUCED by `loclife_add_rooms` at mage.cpp:249 from an `exptr->to_room` that its own `(exptr->to_room != NOWHERE)` filter at mage.cpp:243 had already excluded -- the filter-that-excluded-invalid-ids producer shape ruling R3-C-1 names as admissible for this kind -- and `(*roomnum)++` at mage.cpp:273 is the only writer of the entries this loop reads. in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. The caster's own room is deliberately EXCLUDED from `roomlist` (passed as `room_not` at mage.cpp:471/:482), so no site here resolves it. OVERTURNS BOTH census advisories on this row: `entry-guard? (line 458)` guards the caster's location, which neither site resolves, and `occupant-loop? (line 491)` is one of the two sites. Producer coverage exists: `MageProcTest.LocateLifeAddsReachableRoomsWithUpdatedCoordinates` and `MageProcTest.LocateLifeSkipsBlockedDuplicateAndExcludedRooms` (mage_tests.cpp:706/:751). |
| `src/combat/mage.cpp · spell_mist_of_baazunga · room_by_id_total(` | 4 | PROVEN | entry-guard | all 4 sites -- mage.cpp:2377, mage.cpp:2380, mage.cpp:2391 and mage.cpp:2396 -- resolve `roomnum`, assigned `roomnum = room->dir_option[direction]->to_room;` at mage.cpp:2375 inside the two nested dominating tests `if (room->dir_option[direction])` (mage.cpp:2373) and `if (room->dir_option[direction]->to_room != NOWHERE)` (mage.cpp:2374); `roomnum` is not reassigned again inside the arm. Sentinel exclusion at mage.cpp:2374; in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. The id is an EXIT destination, so this row is independent of the caster's own placement, which the sibling `spell_mist_of_baazunga . room_of(` row still carries. |
| `src/combat/mage.cpp · spell_mist_of_baazunga · room_of(` | 3 | TODO | — | — |
| `src/combat/mage.cpp · spell_relocate · room_by_id_total(` | 2 | PROVEN | loop-bound | `room = room_by_id_total(tmp);` at mage.cpp:1079 and mage.cpp:1117, each the first statement of `for (tmp = 0; tmp <= top_of_world; tmp++)` (headers at mage.cpp:1078 and mage.cpp:1116). `tmp` is the induction variable and the header's own bounds give `0 <= tmp <= top_of_world` at the top of every iteration -- both halves locally, no M-1 appeal. Neither body assigns `tmp` (mage.cpp:1080-1086 and mage.cpp:1118-1126 only read it) and the one `break` (mage.cpp:1125) leaves the loop rather than skipping the increment. OVERTURNS the census advisory `entry-guard? (line 1000)`, which guards `location_of(caster)` -- the sibling `room_of(` row's proof, not this one's. Completeness note: `top_of_world` is incremented by `create_room` (db_world.cpp:2021), so it tracks runtime extension rooms too; `tmp <= top_of_world` therefore stays inside the created set either way. |
| `src/combat/mage.cpp · spell_relocate · room_of(` | 1 | PROVEN | entry-guard | `zon_start = room_of(caster)->zone;` at mage.cpp:1003, dominated by the entry guard `if (location_of(caster) == NOWHERE) return;` at mage.cpp:1000 -- the function's first statement, with nothing between it and the site. Sentinel exclusion at mage.cpp:1000; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/mage.cpp · spell_reveal_life · room_of(` | 2 | PROVEN | entry-guard | `occupants(room_of(caster))` at mage.cpp:601 and `first_occupant(room_of(caster))` at mage.cpp:630, both dominated by the entry guard `if (location_of(caster) == NOWHERE) return;` at mage.cpp:593. Sentinel exclusion at mage.cpp:593; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Nothing between the guard and mage.cpp:630 relocates `caster`: the walk body (mage.cpp:603-620) calls only `new_saves_spell`, `stop_hiding` and `send_to_char`, and the pre-walk statements are `act`/`send_to_char`/`get_mage_caster_level` (mage.cpp:596-599) -- re-read to confirm no `char_to_room`/`char_from_room`/`extract_char` on `caster` anywhere in the function. OVERTURNS the advisory's `occupant-loop? (line 601)` half (mage.cpp:601 is one of the two sites); its `entry-guard? (line 593)` half is CONFIRMED. |
| `src/combat/mage.cpp · spell_searing_darkness · SUN_PENALTY(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_shout_of_pain · room_of(` | 1 | PROVEN | entry-guard | `first_occupant(room_of(caster))` in the walk header at mage.cpp:2127, dominated by the combined null-and-sentinel entry guard `if (!caster \|\| location_of(caster) == NOWHERE) return;` at mage.cpp:2121 -- the function's first statement. Only `number()`/`get_magic_power(caster)` (mage.cpp:2124) runs in between. Sentinel exclusion at mage.cpp:2121; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. The walk's `apply_spell_damage` calls (mage.cpp:2134/:2136) run strictly AFTER the resolve and only on `tmpch != caster` (mage.cpp:2129), so they cannot invalidate this site. |
| `src/combat/mage.cpp · spell_spear_of_darkness · SUN_PENALTY(` | 1 | TODO | — | — |
| `src/combat/mage.cpp · spell_summon · room_of(` | 3 | PROVEN | entry-guard | the 3 CASTER-derived sites -- `IS_SET(room_of(caster)->room_flags, NO_TELEPORT)` (mage.cpp:794) and `zone_table[room_of(caster)->zone].x/.y` (mage.cpp:812 and mage.cpp:813) -- are dominated by the entry guard `if (location_of(caster) == NOWHERE) return;` at mage.cpp:776. Sentinel exclusion at mage.cpp:776; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Nothing between the guard and mage.cpp:813 relocates `caster`: the intervening calls are `act`/`send_to_char`/`affected_by_spell` (mage.cpp:779-803), `different_zone(was_in, to_room)` (mage.cpp:808, a pure zone comparison) and `prohibit_item_stay_zone_move(victim, was_in)` (mage.cpp:809), whose body (fight.cpp:3942-3977 -- the worn-gear loop at :3945-3955 and the inventory/container walk at :3958-3976) moves only the VICTIM's objects. Split from the row below per the mixed-class-key rule: the other 2 sites resolve a different character's id. MATERIALLY OVERTURNS the census advisory `entry-guard? (line 776)` as a whole-row proof -- mage.cpp:776 dominates 3 of the row's 5 sites, not all 5. |
| `src/combat/mage.cpp · spell_summon · room_of(` | 2 | TODO | — | — |
| `src/combat/mage.cpp · spell_word_of_shock · room_of(` | 1 | PROVEN | entry-guard | `for (auto* tmpch : rots::entity::occupants(room_of(caster)))` at mage.cpp:2033, dominated by the entry guard `if (location_of(caster) < 0) return;` at mage.cpp:2028 (the `< 0` spelling; `NOWHERE` is `-1`). Only `get_mage_caster_level(caster)` (mage.cpp:2031) runs in between. Sentinel exclusion at mage.cpp:2028; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. OVERTURNS the census advisory `occupant-loop? (line 2033)`: mage.cpp:2033 IS the site. |
| `src/combat/mage.cpp · spell_word_of_sight · room_of(` | 2 | PROVEN | entry-guard | `occupants(room_of(caster))` at mage.cpp:1991 and `first_occupant(room_of(caster))` at mage.cpp:2018, both dominated by the entry guard `if (location_of(caster) == NOWHERE) return;` at mage.cpp:1982. Sentinel exclusion at mage.cpp:1982; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Nothing between the guard and mage.cpp:2018 relocates `caster`: the walk body (mage.cpp:1992-2012) calls only `new_saves_spell`, `stop_hiding` and `send_to_char`. Structurally identical to the `spell_reveal_life . room_of(` row above. OVERTURNS the advisory's `occupant-loop? (line 1991)` half (mage.cpp:1991 is one of the two sites); its `entry-guard? (line 1982)` half is CONFIRMED. |
| `src/combat/mystic.cpp · spell_divination · room_by_id_total(` | 1 | PROVEN | entry-guard | `const room_data& exit_room = *room_by_id_total(exit->to_room);` at mystic.cpp:638, dominated by `if (exit && exit->to_room != NOWHERE) {` at mystic.cpp:637 -- the site is that `if`'s first statement. The id is an EXIT destination, so neither the caster's placement nor the advisory's mystic.cpp:613 caster guard is involved. Sentinel exclusion at mystic.cpp:637; in-range via the exit-normalization invariant: all four tree-wide writers of a `dir_option[]->to_room` field store either a `real_room()` result or `NOWHERE` (db_world.cpp:797, the boot loader's raw VNUM, normalized in place by `renum_world` at db_world.cpp:844-848; db_world.cpp:1957, `create_exit`; shaperom.cpp:309-312, OLC, a `-1` miss mapped to `NOWHERE`; script.cpp:1060, `SCRIPT_CHANGE_EXIT_TO`, under its own `tmpint = real_room(curr->param[2])`/`tmpint != NOWHERE` pair at :1035/:1059), and `real_room()` (db_world.cpp:1732-1757) returns either a binary-search `mid` in `[0, top_of_world]` or `-1`. OVERTURNS the census advisory `entry-guard? (line 613)`. |
| `src/combat/mystic.cpp · spell_divination · room_of(` | 1 | PROVEN | entry-guard | `const room_data& cur_room = *room_of(caster);` at mystic.cpp:618, dominated by the combined null-and-sentinel entry guard `if (caster == NULL \|\| location_of(caster) == NOWHERE) return;` at mystic.cpp:613 -- the function's first statement, with only a local declaration in between. Sentinel exclusion at mystic.cpp:613; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/mystic.cpp · spell_haze · room_of(` | 2 | PROVEN | dispatch-invariant | Both sites sit in the room arm `if (!victim && !obj && !(caster->specials.fighting))` (mystic.cpp:1115) and resolve the CASTER's own room: `room_affected_by_spell(room_of(caster), SPELL_HAZE)` at mystic.cpp:1128 and `affect_to_room(room_of(caster), &af)` at mystic.cpp:1137. The arm's only other test is `if (!caster) { return; }` (mystic.cpp:1117) -- a null test, never a placement test. (i) REGISTRY ENTRIES: `spell_haze` is entered only through `skills[52].spell_pointer` (registered at spell_pa.cpp:1096), and exactly two of the six tree-wide `spell_pointer` doors can enter this arm -- `do_cast`'s dispatch (spell_pa.cpp:900), guarded at src/combat/spell_pa.cpp:499 (registry entry `do_cast`), and `do_use`'s ITEM_STAFF dispatch (act_othe.cpp:819, `victim = 0, obj = 0` literally), guarded at src/app/act_othe.cpp:806 (registry entry `do_use`). The other four doors CANNOT reach these sites: every one passes a NON-NULL `victim` and this arm requires `!victim` -- `do_use`'s ITEM_WAND dispatch (act_othe.cpp:839, `tmp_char`), `affect_update_room`'s room-affect re-cast (limits.cpp:1498, `tmpch`), and `affect_modify`'s APPLY_SPELL pair (entity_lifecycle.cpp:2440/:2442, `victim == caster == ch`). Each argument list was re-read at this commit, so this row is NOT `APPLY_SPELL-window` (owner ruling R3-O-2) -- the login-window door is blocked by the arm's own shape, not by an exhaustion argument. `cast_mass_spell` (mystic.cpp:1010) is not a door either: it is handed only `spell_regeneration`/`spell_vitality`/`spell_insight` (mystic.cpp:1031/:1041/:1051) and passes a non-null `group_member` (mystic.cpp:1019) regardless. (ii) ACTOR: `caster`, the ASPELL's first parameter -- the identical `char_data*` both live doors pass as argument 1, i.e. `ch`, the actor each entry names and guards. Both sites resolve THAT actor's location; neither is victim/target-derived (R3-C-5 does not bite). (iii) DIRECT CALLERS: NONE. A tree-wide grep for `spell_haze` finds only the declaration (spells.h:456), the nullptr table-cell comment (consts.cpp:475), the definition (mystic.cpp:1108), the registration (spell_pa.cpp:1096) and one test-tier row (spell_registry_tests.cpp:50) -- zero production call sites, so there is no unregistered door. Out-of-scope defect recorded, not pursued: mystic.cpp:1115 dereferences `caster->specials.fighting` BEFORE the null test at mystic.cpp:1117 -- a null-order bug unrelated to room ids. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/mystic.cpp · spell_poison · room_of(` | 2 | PROVEN | dispatch-invariant | Both sites sit in the room arm `if (!victim && !obj && !(caster->specials.fighting))` (mystic.cpp:1252) and resolve the CASTER's own room: `room_affected_by_spell(room_of(caster), SPELL_POISON)` at mystic.cpp:1264 and `affect_to_room(room_of(caster), &af)` at mystic.cpp:1271. The arm's only other test is `if (!caster) return;` (mystic.cpp:1254) -- a null test, never a placement test. (i) REGISTRY ENTRIES: `spell_poison` is entered through `skills[43].spell_pointer` (registered at spell_pa.cpp:1087), and exactly two of the six tree-wide `spell_pointer` doors can enter this arm -- `do_cast`'s dispatch (spell_pa.cpp:900), guarded at src/combat/spell_pa.cpp:499 (registry entry `do_cast`), and `do_use`'s ITEM_STAFF dispatch (act_othe.cpp:819, `victim = 0, obj = 0` literally), guarded at src/app/act_othe.cpp:806 (registry entry `do_use`). The other four doors CANNOT reach these sites: every one passes a NON-NULL `victim` and this arm requires `!victim` -- `do_use`'s ITEM_WAND dispatch (act_othe.cpp:839, `tmp_char`), `affect_update_room`'s room-affect re-cast (limits.cpp:1498, `tmpch`), and `affect_modify`'s APPLY_SPELL pair (entity_lifecycle.cpp:2440/:2442, `victim == caster == ch`). Each argument list was re-read at this commit, so this row is NOT `APPLY_SPELL-window` (owner ruling R3-O-2) -- the login-window door is blocked by the arm's own shape, not by an exhaustion argument. `cast_mass_spell` (mystic.cpp:1010) is not a door either: it is handed only `spell_regeneration`/`spell_vitality`/`spell_insight` (mystic.cpp:1031/:1041/:1051) and passes a non-null `group_member` (mystic.cpp:1019) regardless. (ii) ACTOR: `caster`, the ASPELL's first parameter -- the identical `char_data*` both live doors pass as argument 1, i.e. `ch`, the actor each entry names and guards. Both sites resolve THAT actor's location; neither is victim/target-derived (R3-C-5 does not bite). (iii) DIRECT CALLERS: EXACTLY ONE, and it cannot reach these sites. A tree-wide grep for `spell_poison` finds the declaration (spells.h:477), a second forward declaration (limits.cpp:1468, a declaration, not a call), the nullptr table-cell comment (consts.cpp:455), the definition (mystic.cpp:1246), the registration (spell_pa.cpp:1087), one test-tier row (spell_registry_tests.cpp:41), and the tree's only direct-by-name ASPELL call in production: `SPECIAL(snake)` at spec_pro.cpp:670, which passes `host->specials.fighting` as `victim` under the `host->specials.fighting &&` test at spec_pro.cpp:666 -- a NON-NULL victim, so the `!victim` room arm above is unreachable from it and the row does not split. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/mystic.cpp · spell_terror · room_of(` | 1 | PROVEN | entry-guard | `for (auto* tmpch : rots::entity::occupants(room_of(caster)))` at mystic.cpp:1309, dominated by the combined null-and-sentinel entry guard `if (!caster \|\| (location_of(caster) == NOWHERE)) return;` at mystic.cpp:1299. Only `send_to_char` and `get_mystic_caster_level`/`get_specialization` (mystic.cpp:1302-1308) run in between. Sentinel exclusion at mystic.cpp:1299; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Already regression-pinned: `MysticSpellTerror.ReturnsImmediatelyWhenCasterHasNoRoom` (mystic_tests.cpp:249) drives exactly this guard. OVERTURNS the advisory's `occupant-loop? (line 1309)` half (mystic.cpp:1309 IS the site); its `entry-guard? (line 1299)` half is CONFIRMED. |
| `src/combat/olog_hai.cpp · do_overrun · room_of(` | 1 | PROVEN | dispatch-invariant | `for (auto* tmpch : rots::entity::occupants(room_of(ch)))` at src/combat/olog_hai.cpp:565. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — the name the registry's own actor column carries — and this site resolves THAT actor's location (`room_of(ch)` is `room_by_id_total(location_of(ch))`); no victim/target id is read here, so R3-C-5 does not apply. (iii) DIRECT CALLERS: `do_overrun` is an `ACMD` (src/combat/olog_hai.cpp:524) and its ONLY door is the `cmd_info` table, `COMMANDO(245, POSITION_STANDING, do_overrun, ...)` at src/app/interpre.cpp:1908 — re-grepped tree-wide at this commit: no `set_combat_command` cell, no direct call from any other body, no fn-ptr storage. INTERVENING RELOCATION: the site sits inside the `for (dis = 0; dis <= total_moves; dis++)` loop whose tail relocates `ch` through `rots::combat::issue_command(combat_command::move, ch, ...)` at src/combat/olog_hai.cpp:572 — itself a registry entry (src/combat/combat_hooks.cpp:81, the `rots::combat::issue_command` registry entry (actor column `ch`)) guarding the same `ch` — and `do_move`'s relocation is the ADJACENT pair `char_from_room(ch); char_to_room(ch, to_room);` at src/app/act_move.cpp:807-808, which re-places on the very next statement, so no later iteration re-enters :565 unplaced. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/olog_hai.cpp · is_direction_valid · EXIT(` | 5 | PROVEN | dispatch-invariant | `EXIT(ch, cmd)` (`room_of((ch))->dir_option[cmd]`, src/utils.h:721) at src/combat/olog_hai.cpp:474, :477, :486, :488 and :495. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the registry's own actor column; `is_direction_valid(char_data* ch, int cmd)` (:463) resolves that same actor's location at all five sites — the `cmd` argument is a DOOR index, not a room id, and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: exactly ONE tree-wide — `if (!is_direction_valid(ch, cmd))` at src/combat/olog_hai.cpp:542, inside `ACMD(do_overrun)` (:524), forwarding do_overrun's own ACMD `ch`; `do_overrun`'s only door is `COMMANDO(245, ..., do_overrun, ...)` at src/app/interpre.cpp:1908 (no `set_combat_command` cell, no other direct call, re-grepped at this commit). The call sits at :542, ABOVE do_overrun's `for (dis...)` loop, so no relocation intervenes. Only the ROOM ID is proved here; the `dir_option[cmd]` dereference is the explicitly out-of-scope defect class (design doc section 6), and all five sites are in any case dominated by the `if (!room_of(ch)->dir_option[cmd])` early return at :465. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/olog_hai.cpp · is_direction_valid · room_of(` | 2 | PROVEN | dispatch-invariant | `if (!room_of(ch)->dir_option[cmd])` at src/combat/olog_hai.cpp:465 and `} else if (room_of(ch)->dir_option[cmd]->to_room == NOWHERE)` at :468. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the registry's own actor column; both sites resolve THAT actor's location, and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: exactly ONE tree-wide — src/combat/olog_hai.cpp:542 inside `ACMD(do_overrun)` (:524), forwarding do_overrun's own ACMD `ch`; do_overrun's only door is `COMMANDO(245, ..., do_overrun, ...)` at src/app/interpre.cpp:1908 (re-grepped tree-wide at this commit: no `set_combat_command` cell, no other direct call). The call sits above do_overrun's relocating loop, so no relocation intervenes. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/olog_hai.cpp · olog_hai::get_random_target · room_of(` | 2 | TODO | — | — |
| `src/combat/olog_hai.cpp · olog_hai::is_skill_valid · room_of(` | 1 | PROVEN | dispatch-invariant | `const room_data& room = *room_of(ch);` at src/combat/olog_hai.cpp:67. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — `is_skill_valid(char_data* ch, const int& skill_id)` (:55) resolves that same actor's location; the `skill_id` argument carries no id and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS (exhaustive, re-grepped tree-wide at this commit — FIVE, every one an `ACMD` body forwarding its own ACMD `ch` from the top of that body, before any relocation): src/combat/olog_hai.cpp:400 (`ACMD(do_cleave)` :388, `COMMANDO(244, ...)` at src/app/interpre.cpp:1906), :426 (`ACMD(do_smash)` :420, `COMMANDO(241, ...)` at :1900), :539 (`ACMD(do_overrun)` :524, `COMMANDO(245, ...)` at :1908), :591 (`ACMD(do_frenzy)` :585, `COMMANDO(242, ...)` at :1902), :616 (`ACMD(do_stomp)` :611, `COMMANDO(243, ...)` at :1904). None of the five has a `set_combat_command` cell or any other direct caller. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/olog_hai.cpp · olog_hai::room_target · room_of(` | 1 | PROVEN | dispatch-invariant | `for (victim = rots::entity::first_occupant(room_of(ch)); victim; victim = nxt_victim)` at src/combat/olog_hai.cpp:379. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — `room_target(char_data* ch, void (*skill_damage)(...))` (:374) resolves that actor's location; the `victim` cursor is DERIVED from the resolved room's occupant chain and is never itself resolved here, so R3-C-5's target-derived carve-out does not apply. (iii) DIRECT CALLERS (exhaustive, re-grepped tree-wide at this commit — FOUR, all `ACMD` bodies forwarding their own ACMD `ch`): src/combat/olog_hai.cpp:413 (`ACMD(do_cleave)` :388, `COMMANDO(244, ...)` at src/app/interpre.cpp:1906), :560 and :579 (`ACMD(do_overrun)` :524, `COMMANDO(245, ...)` at :1908), :628 (`ACMD(do_stomp)` :611, `COMMANDO(243, ...)` at :1904). No `set_combat_command` cell and no other direct caller for any of the three. INTERVENING RELOCATION: the :560 call re-enters on iterations 2+ after `issue_command(combat_command::move, ch, ...)` at :572 — itself a registry entry (src/combat/combat_hooks.cpp:81, the `rots::combat::issue_command` registry entry (actor column `ch`)) — whose `do_move` relocation is the ADJACENT `char_from_room(ch); char_to_room(ch, to_room);` pair at src/app/act_move.cpp:807-808, re-placing on the next statement. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · can_ch_blind · room_of(` | 1 | PROVEN | dispatch-invariant | `const room_data& room = *room_of(ch);` at src/combat/ranger.cpp:3192. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — `can_ch_blind(char_data* ch, int mana_cost)` (:3190) resolves that actor's location; no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — src/combat/ranger.cpp:3355, inside `ACMD(do_blinding)` (:3339), forwarding do_blinding's own ACMD `ch`; do_blinding's only door is `COMMANDO(238, POSITION_FIGHTING, do_blinding, ...)` at src/app/interpre.cpp:1894 (no `set_combat_command` cell, no other direct call). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · can_ch_mark · room_of(` | 1 | PROVEN | dispatch-invariant | `if (IS_SET(room_of(ch)->room_flags, PEACEROOM))` at src/combat/ranger.cpp:3013. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — `can_ch_mark(char_data* ch)` (:2994) resolves that actor's location; no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — src/combat/ranger.cpp:3115, inside `ACMD(do_mark)` (:3100), forwarding do_mark's own ACMD `ch`; do_mark's only door is `COMMANDO(237, POSITION_FIGHTING, do_mark, ...)` at src/app/interpre.cpp:1892 (no `set_combat_command` cell, no other direct call). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · can_ch_shoot · room_of(` | 1 | PROVEN | dispatch-invariant | `const room_data& room = *room_of(archer);` at src/combat/ranger.cpp:2287. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: the resolved id is `archer`, `can_ch_shoot(char_data* archer)`'s only parameter (:2267), which is bound at its single call site to the registry's `ch` — a rename, not a different character, and NOT a victim/target id (R3-C-5 does not apply). (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — `if (!can_ch_shoot(ch))` at src/combat/ranger.cpp:2548, inside `ACMD(do_shoot)` (:2533), forwarding do_shoot's own ACMD `ch`; do_shoot's only door is `COMMANDO(229, POSITION_FIGHTING, do_shoot, ...)` at src/app/interpre.cpp:1876 (no `set_combat_command` cell, no other direct call). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · can_harad_use_skill · room_of(` | 1 | PROVEN | dispatch-invariant | `const room_data& room = *room_of(ch);` at src/combat/ranger.cpp:3448. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — `can_harad_use_skill(char_data* ch, int mana_cost, int move_cost, int skill)` (:3446) resolves that actor's location; the remaining arguments carry no id and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS (exhaustive, re-grepped tree-wide at this commit — TWO, both `ACMD` bodies forwarding their own ACMD `ch`): src/combat/ranger.cpp:3540 (`ACMD(do_bendtime)` :3524, `COMMANDO(239, POSITION_FIGHTING, do_bendtime, ...)` at src/app/interpre.cpp:1896) and :3708 (`ACMD(do_windblast)` :3692, `COMMANDO(240, POSITION_FIGHTING, do_windblast, ...)` at :1898). Neither has a `set_combat_command` cell or any other direct caller. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · change_arrow_target · room_of(` | 1 | PROVEN | dispatch-invariant | `const room_data& room = *room_of(archer);` at src/combat/ranger.cpp:2427. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: the resolved id is `archer`, parameter 1 of `change_arrow_target(char_data* archer, char_data* victim, obj_data* arrow)` (:2425) — NOT the `victim` parameter, whose id is never resolved in this function (R3-C-5's carve-out is therefore not engaged); `archer` is the registry's `ch` under a rename, transitively (see (iii)). (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — src/combat/ranger.cpp:2509, inside `on_arrow_miss(char_data* archer, ...)` (:2502), which forwards its OWN `archer`; `on_arrow_miss`'s own single tree-wide caller is src/combat/ranger.cpp:2645, inside `ACMD(do_shoot)` (:2533), passing do_shoot's ACMD `ch`; do_shoot's only door is `COMMANDO(229, POSITION_FIGHTING, do_shoot, ...)` at src/app/interpre.cpp:1876 (no `set_combat_command` cell, no other direct call at either level). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · check_gather_conditions · room_of(` | 1 | PROVEN | dispatch-invariant | `switch (room_of(ch)->sector_type)` at src/combat/ranger.cpp:327. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch` — `check_gather_conditions(struct char_data* ch, int percent, int gather_type)` (:302) resolves that actor's location; the other two arguments carry no id and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — src/combat/ranger.cpp:413, inside `ACMD(do_gather_food)` (:366), forwarding do_gather_food's own ACMD `ch`; do_gather_food's only door is `COMMANDO(203, POSITION_STANDING, do_gather_food, ...)` at src/app/interpre.cpp:1823 (no `set_combat_command` cell, no other direct call). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · do_ambush · room_of(` | 1 | PROVEN | dispatch-invariant | `if (IS_SET(room_of(ch)->room_flags, PEACEROOM))` at src/combat/ranger.cpp:907. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`), plus src/combat/combat_hooks.cpp:81, the `rots::combat::issue_command` registry entry (actor column `ch`) for the `combat_command::ambush` cell, plus the two spec-proc doors proven in (iii). (ii) ACTOR: `ch` — the `ACMD(do_ambush)` actor (:889); the site resolves THAT actor, not the ambush target (R3-C-5 does not apply). (iii) DIRECT CALLERS (exhaustive, re-grepped tree-wide at this commit — FOUR doors, each proven): (a) the `cmd_info` table, `COMMANDO(87, POSITION_STANDING, do_ambush, ...)` at src/app/interpre.cpp:1597, i.e. ordinary `command_interpreter` dispatch; (b) `set_combat_command(combat_command::ambush, do_ambush)` at src/app/interpre.cpp:1962, i.e. the guarded `issue_command` entry; (c) src/script/spec_pro.cpp:1975, `do_ambush(host, ...)` inside `SPECIAL(mob_ranger)` (:1934), whose OWN entry guard `if (location_of(host) == NOWHERE) return 0;` at src/script/spec_pro.cpp:1950 dominates it and excludes the sentinel for `host`; (d) src/script/spec_pro.cpp:2052, inside `do_spec_ambush(char_data* host, char_data* tmpch)` (:2043), whose own two tree-wide callers — src/script/spec_pro.cpp:2146 and :2182 — both sit inside `SPECIAL(mob_ranger_new)` (:2084) BELOW its entry guard `if (location_of(host) == NOWHERE) return 0;` at src/script/spec_pro.cpp:2099, with nothing between that relocates `host` (`prog_do_hunter` at :2076 is a pure predicate; `do_hide` at :2129 moves no one). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · do_cover · room_of(` | 1 | PROVEN | dispatch-invariant | `tmproom = room_of(ch);` at src/combat/ranger.cpp:1804. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the `ACMD(do_cover)` actor (:1771); the site resolves THAT actor and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: `do_cover` is an `ACMD` whose ONLY door is the `cmd_info` table, `COMMANDO(216, POSITION_STANDING, do_cover, ...)` at src/app/interpre.cpp:1849 — re-grepped tree-wide at this commit: no `set_combat_command` cell, no direct call from any other body, no fn-ptr storage. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · do_gather_food · room_of(` | 1 | PROVEN | dispatch-invariant | `percent += sector_variables[room_of(ch)->sector_type];` at src/combat/ranger.cpp:393. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the `ACMD(do_gather_food)` actor (:366); the site resolves THAT actor and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: `do_gather_food` is an `ACMD` whose ONLY door is the `cmd_info` table, `COMMANDO(203, POSITION_STANDING, do_gather_food, ...)` at src/app/interpre.cpp:1823 — re-grepped tree-wide at this commit: no `set_combat_command` cell, no direct call from any other body, no fn-ptr storage. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · do_pick · EXIT(` | 9 | PROVEN | dispatch-invariant | `EXIT(ch, door)` (`room_of((ch))->dir_option[door]`, src/utils.h:721) at src/combat/ranger.cpp:601, :603, :605, :607, :609, :617, :618, :619 and :628. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the `ACMD(do_pick)` actor (:553); every one of the nine sites resolves THAT actor's location — `door` is a DOOR index returned by `find_door(ch, type, dir)` at :600, not a room id — and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: `do_pick` is an `ACMD` whose ONLY door is the `cmd_info` table, `COMMANDO(88, POSITION_STANDING, do_pick, ...)` at src/app/interpre.cpp:1600 — re-grepped tree-wide at this commit: no `set_combat_command` cell, no direct call from any other body, no fn-ptr storage. Only the ROOM ID is proved here; the `dir_option[door]` dereference is the explicitly out-of-scope defect class (design doc section 6). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · do_pick · room_by_id_total(` | 1 | PROVEN | entry-guard | `if ((back = room_by_id_total(other_room)->dir_option[rev_dir[door]]) != NULL)` at src/combat/ranger.cpp:629, dominated on the immediately preceding line by `if ((other_room = EXIT(ch, door)->to_room) != NOWHERE)` at :628 — the assignment and the sentinel test are one statement, so no intervening code can change `other_room`. Sentinel exclusion at :628; in-range via the exit-target invariant: `renum_world` (src/world/db_world.cpp:844-848) rewrites every non-NOWHERE `dir_option[]->to_room` through `real_room()` at world load, and that field's only FIVE runtime writers tree-wide (re-grepped at this commit: db_world.cpp:797, a raw vnum normalized by :848 before any read; db_world.cpp:848 itself; db_world.cpp:1957, `real_room()` of the room's own vnum, always found; src/olc/shaperom.cpp:309-312, `tmp = real_room(...); if (tmp == -1) tmp = NOWHERE;`; src/script/script.cpp:1035/:1059-1060, guarded by `tmpint != NOWHERE`) all preserve it — so a non-NOWHERE `to_room` is in [0, top_of_world], plus append-only room allocation. Scope note: this row proves the ROOM ID only; the `EXIT(` sites in the same function are a separate key and stay TODO, and the `dir_option[door]` dereference is the program spec's explicitly out-of-scope defect class. |
| `src/combat/ranger.cpp · do_scan · EXIT(` | 1 | PROVEN | entry-guard | `IS_SET(EXIT(character, dir)->exit_info, EX_NO_LOOK)` at src/combat/ranger.cpp:2886 (`EXIT(ch, door)` expands to `room_of((ch))->dir_option[door]`, src/utils.h:721 — the room id is what this row proves). `do_scan` opens with `if (!CAN_SEE(character)) { send_to_char(...); return; }` at :2833-2836, and `CAN_SEE(sub)` (the 1-arg overload, src/combat/visibility.cpp:107) opens with `if (location_of(sub) == NOWHERE) return 0;` at :109 — its FIRST statement, ahead of the blind/PLR_WRITING test at :111 and the `IS_SHADOW` shortcut at :114 — so a truthy `CAN_SEE(x)` is a sentinel exclusion for `x`. Census advisory OVERTURNED: `occupant-loop? (line 2871)` names the SIBLING row's site, a consumer rather than a proof, and never sees the real guard. CURSOR-WINDOW DISCLOSURE (mandatory per this ledger's render-window rule): the site sits inside a `ScopedRenderLocation` opened at :2866 with `is_in = location_of(character)` (:2858, itself proven by :2833) and retargeted at :2868 (back to `is_in`) and at :2889 (to `room_of(character)->dir_option[dir]->to_room`, dominated by the `CAN_GO(character, dir)` term evaluated FIRST on that same line :2886, hence non-NOWHERE) — so every value the cursor can hold across the window is a proven-valid room id. Sentinel exclusion at :2833; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. The retargeted values are additionally in-range by the exit-target invariant (src/world/db_world.cpp:844-848 `renum_world` normalization; see this ledger's `src/combat/limits.cpp · affect_update_room` row for the five-writer enumeration). |
| `src/combat/ranger.cpp · do_scan · room_of(` | 3 | PROVEN | entry-guard | All three sites — `occupants(room_of(character))` at src/combat/ranger.cpp:2871, `room_of(character)->dir_option[dir]->to_room == is_in` at :2886, and `scan_cursor.retarget(room_of(character)->dir_option[dir]->to_room)` at :2889 — share one proof with this function's `EXIT(` row: `if (!CAN_SEE(character)) { send_to_char(...); return; }` at :2833-2836, and `CAN_SEE(sub)` (the 1-arg overload, src/combat/visibility.cpp:107) opens with `if (location_of(sub) == NOWHERE) return 0;` at :109 — its FIRST statement, ahead of the blind/PLR_WRITING test at :111 and the `IS_SHADOW` shortcut at :114 — so a truthy `CAN_SEE(x)` is a sentinel exclusion for `x`. Census advisory OVERTURNED: `occupant-loop? (line 2871)` flags one of this row's OWN sites as if it were the proof. CURSOR-WINDOW DISCLOSURE: the `ScopedRenderLocation` opened at :2866 holds `is_in` (:2858, proven by :2833) and is retargeted only at :2868 (back to `is_in`) and :2889 (an exit target dominated by the `CAN_GO(character, dir)` term evaluated first on :2886), so the window's whole value set is proven. Sentinel exclusion at :2833; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/ranger.cpp · do_stalk · room_of(` | 1 | PROVEN | dispatch-invariant | `if (room_of(ch)->sector_type == SECT_WATER_NOSWIM)` at src/combat/ranger.cpp:1756. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the `ACMD(do_stalk)` actor (:1722); the site resolves THAT actor and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: `do_stalk` is an `ACMD` whose ONLY door is the `cmd_info` table, `COMMANDO(215, POSITION_STANDING, do_stalk, ...)` at src/app/interpre.cpp:1847 — re-grepped tree-wide at this commit: no `set_combat_command` cell, no direct call from any other body, no fn-ptr storage. (This row is the DISPATCH half of the `CAN_GO`/`CAN_SEE` contrast the R3 census records for this file: `CAN_GO` resolves `room_of(ch)` itself and so cannot serve as an entry guard, which is why `do_stalk` lands here while `do_scan` lands as an `entry-guard` via `CAN_SEE`.) in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · do_track · IS_WATER(` | 1 | PROVEN | entry-guard | `if (IS_WATER(location_of(ch)))` at src/combat/ranger.cpp:256 (`IS_WATER(room)` expands to three `room_by_id_total((room))` reads, src/utils.h:796, all on this one line and all on the same id), dominated six lines earlier by `if (!CAN_SEE(ch)) { send_to_char("You can't see to track!", ch); return; }` at :250-253, inside the SAME `if (subcmd == 0)` block (:249) that contains the site; the only statement in between is the `wtl`/`TARGET_TEXT` argument read at :254-255. `CAN_SEE(sub)` (the 1-arg overload, src/combat/visibility.cpp:107) opens with `if (location_of(sub) == NOWHERE) return 0;` at :109 — its FIRST statement, ahead of the blind/PLR_WRITING test at :111 and the `IS_SHADOW` shortcut at :114 — so a truthy `CAN_SEE(x)` is a sentinel exclusion for `x`. Sentinel exclusion at :250; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/ranger.cpp · do_whistle · room_by_id_total(` | 1 | PROVEN | loop-bound | MIXED-KEY SPLIT (this row plus the TODO row below; the `find_first_step`/`reset_zone` precedent, and each row names its own sites). This row covers ONE site: `rm = room_by_id_total(rm_num);` at src/combat/ranger.cpp:1676, the first statement of `for (rm_num = 0; rm_num <= top_of_world; rm_num++)` at :1675, with `rm_num` the induction variable, never modified in the body — so `rm_num` stays in [0, top_of_world] at every evaluation. The `ScopedRenderLocation` opened at :1674 is retargeted ONLY to these same loop-bound `rm_num` values (:1681), so the render window contributes no unproven id to THIS site (the window's INITIAL value does inherit :1649's obligation — that is the companion TODO row's business, not this one's). |
| `src/combat/ranger.cpp · do_whistle · room_by_id_total(` | 1 | PROVEN | dispatch-invariant | `zone_num = room_by_id_total(cur_room_num)->zone;` at src/combat/ranger.cpp:1649, where `cur_room_num = location_of(ch);` sits on the line immediately above (:1648) with nothing in between. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: `ch`, the `ACMD(do_whistle)` actor (:1642); `cur_room_num` is that actor's own location, and no victim id is read (R3-C-5 does not apply). (iii) DIRECT CALLERS: `do_whistle` is an `ACMD` whose ONLY door is the `cmd_info` table, `COMMANDO(179, POSITION_DEAD, do_whistle, ...)` at src/app/interpre.cpp:1779 — re-grepped tree-wide at this commit: no `set_combat_command` cell, no direct call from any other body, no fn-ptr storage. This is the DISPATCH half of the row Wave R3 Task 3p split; the sibling row's :1676 site is the `loop-bound` half. in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · get_corpse_tagged_arrows · room_of(` | 1 | PROVEN | caller-contract | `const room_data& room = *room_of(character);` at src/combat/ranger.cpp:2717 is the function's FIRST statement, so the dominating condition necessarily lives one frame up — which is why this row is `caller-contract` and not `entry-guard` (the census flagged the label choice as open and asked R3 to pick one; this ledger's `entry-guard` requires the branch to be in the SAME function). EXACTLY ONE production caller tree-wide: a whole-tree grep for `get_corpse_tagged_arrows` returns exactly two references, the definition at ranger.cpp:2715 and the call at ranger.cpp:2762 — the function is file-local with no declaration in any header, so no other caller can exist. That call sits inside `ACMD(do_recover)` and is dominated by `if (!CAN_SEE(character)) { send_to_char(...); return; }` at :2746-2749. `CAN_SEE(sub)` (the 1-arg overload, src/combat/visibility.cpp:107) opens with `if (location_of(sub) == NOWHERE) return 0;` at :109 — its FIRST statement, ahead of the blind/PLR_WRITING test at :111 and the `IS_SHADOW` shortcut at :114 — so a truthy `CAN_SEE(x)` is a sentinel exclusion for `x`. Nothing between :2746 and :2762 relocates `character` (:2752-2755 is a shadow test, :2757-2758 two carry-limit reads, :2760-2761 a vector declaration and the sibling call). Sentinel exclusion at :2746; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/ranger.cpp · get_room_tagged_arrows · room_of(` | 1 | PROVEN | caller-contract | `const room_data& room = *room_of(character);` at src/combat/ranger.cpp:2705, the function's first statement — identical shape to the `get_corpse_tagged_arrows` row above, and labelled `caller-contract` for the same reason. EXACTLY ONE production caller tree-wide (a whole-tree grep for `get_room_tagged_arrows` returns exactly two references: the definition at ranger.cpp:2703 and the call at ranger.cpp:2761; file-local, no header declaration): `ACMD(do_recover)`, dominated by `if (!CAN_SEE(character)) { send_to_char(...); return; }` at :2746-2749, with nothing between :2746 and :2761 that relocates `character`. `CAN_SEE(sub)` (the 1-arg overload, src/combat/visibility.cpp:107) opens with `if (location_of(sub) == NOWHERE) return 0;` at :109 — its FIRST statement, ahead of the blind/PLR_WRITING test at :111 and the `IS_SHADOW` shortcut at :114 — so a truthy `CAN_SEE(x)` is a sentinel exclusion for `x`. Sentinel exclusion at :2746; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/ranger.cpp · on_arrow_miss · room_of(` | 1 | PROVEN | dispatch-invariant | `const room_data& room = *room_of(archer);` at src/combat/ranger.cpp:2504. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`). (ii) ACTOR: the resolved id is `archer`, parameter 1 of `on_arrow_miss(char_data* archer, char_data* victim, obj_data* arrow)` (:2502) — NOT the `victim` parameter, whose id is never resolved here (R3-C-5's carve-out is therefore not engaged); `archer` is the registry's `ch` under a rename (see (iii)). (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — src/combat/ranger.cpp:2645, inside `ACMD(do_shoot)` (:2533), passing do_shoot's own ACMD `ch`; do_shoot's only door is `COMMANDO(229, POSITION_FIGHTING, do_shoot, ...)` at src/app/interpre.cpp:1876 (no `set_combat_command` cell, no other direct call). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/ranger.cpp · on_windblast_hit · EXIT(` | 5 | TODO | — | — |
| `src/combat/ranger.cpp · on_windblast_hit · room_by_id_total(` | 3 | PROVEN | entry-guard | All three sites (src/combat/ranger.cpp:3597, and :3599 twice) resolve `EXIT(ch, attempt)->to_room`, and all three are dominated by `if (res)` at :3596, where `res` was assigned on :3594 as `res = CAN_GO(ch, attempt) && !IS_SET(EXIT(ch, attempt)->exit_info, EX_NOFLEE) && !IS_SET(EXIT(ch, attempt)->exit_info, EX_NOWALK);`. `CAN_GO` (src/entity/environment_utils.cpp:137-143) returns 1 only when `r->dir_option[door] && (r->dir_option[door]->to_room != NOWHERE)` (:140), so a truthy `res` IS the sentinel exclusion for exactly the id these three sites resolve. WHAT THIS ROW DOES AND DOES NOT PROVE: the id here is the EXIT'S TARGET, not `ch`'s own room, so the proof is independent of `ch`'s placement — `CAN_GO` itself resolves `room_of(ch)` unguarded (environment_utils.cpp:139), which is this function's SIBLING rows' still-TODO obligation, not this one's. Nothing between :3594 and :3599 relocates `ch` or rewrites the exit (`check_simple_move` is not reached until :3604). Census advisory OVERTURNED: `occupant-loop? (line 3608)` names a different row's site entirely. Sentinel exclusion at :3594/:3596; in-range via the exit-target invariant: `renum_world` (src/world/db_world.cpp:844-848) rewrites every non-NOWHERE `dir_option[]->to_room` through `real_room()` at world load, and that field's only FIVE runtime writers tree-wide (re-grepped at this commit: db_world.cpp:797, a raw vnum normalized by :848 before any read; db_world.cpp:848 itself; db_world.cpp:1957, `real_room()` of the room's own vnum, always found; src/olc/shaperom.cpp:309-312, `tmp = real_room(...); if (tmp == -1) tmp = NOWHERE;`; src/script/script.cpp:1035/:1059-1060, guarded by `tmpint != NOWHERE`) all preserve it — so a non-NOWHERE `to_room` is in [0, top_of_world], plus append-only room allocation. |
| `src/combat/ranger.cpp · on_windblast_hit · room_of(` | 3 | TODO | — | — |
| `src/combat/ranger.cpp · on_windblast_success · room_of(` | 1 | PROVEN | entry-guard | `for (tmpch = rots::entity::first_occupant(room_of(ch)); tmpch; tmpch = tmpch_next)` at src/combat/ranger.cpp:3662, dominated by `if (location_of(ch) == NOWHERE) return;` at :3653-3654, with only three arithmetic/output statements in between (:3656 `power_level`, :3658 `dam_value`, :3660 a `send_to_char`). This is the ONE census advisory in `ranger.cpp` that survived falsification: `entry-guard? (line 3653)` is correct, complete, and points at the guard that really dominates. Sentinel exclusion at :3653; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/ranger.cpp · snuck_in · room_of(` | 1 | PROVEN | dominating-resolve | `for (auto* occ : rots::entity::occupants(room_of(ch)))` at src/combat/ranger.cpp:1870 is unguarded inside `snuck_in` (:1861), so the whole proof lives one frame up AND in another file — it is invisible from ranger.cpp alone. EXACTLY ONE production caller tree-wide (re-grepped at this commit: src/utils.h:93 is the declaration and src/persist/character_json.cpp:126's `"snuck_in"` is a flag-NAME string, not a call; the only call is src/app/act_move.cpp:821): `ACMD(do_move)`, where the call is dominated by `char_to_room(ch, to_room);` at act_move.cpp:808 — a PRODUCING placement of the kind ruling R3-C-1 requires, whose argument is itself proven: `to_room = EXIT(ch, cmd)->to_room;` at act_move.cpp:694 (the function's only write to `to_room`), sentinel-excluded at act_move.cpp:682 (`else if (EXIT(ch, cmd)->to_room == NOWHERE) { send_to_char(...); return; }`) and in-range by the exit-target invariant (src/world/db_world.cpp:844-848 `renum_world` normalization plus the five preserving writers enumerated in this ledger's `src/combat/limits.cpp · affect_update_room` row). Between :808 and :821 lie only `do_look(ch, ...)` (:809 — act_info.cpp:1165-1658 contains no `char_from_room`/`char_to_room`/`extract_char`/`special(`/command-dispatch call anywhere, re-grepped), a move-cost decrement (:810), and the sneak-visibility `if` at :811 whose ELSE arm (:820) is the one that reaches :821 — nothing detaches or relocates `ch`. In-range therefore holds BY CONSTRUCTION here (`location_of(ch) == to_room`, a proven id), not merely by inheritance from the write side. Census advisory OVERTURNED: `occupant-loop? (line 1870)` flags the site itself. Contrast the deliberately asymmetric sibling `snuck_out` (:1931), called from act_move.cpp:765 on the PRE-move side, which this proof does NOT cover. |
| `src/combat/ranger.cpp · snuck_out · room_of(` | 1 | PROVEN | dispatch-invariant | `for (auto* occ : rots::entity::occupants(room_of(ch)))` at src/combat/ranger.cpp:1931. (i) ENTRY: guarded at src/app/interpre.cpp:1126, the `command_interpreter` registry entry (actor column `ch`), plus src/combat/combat_hooks.cpp:81, the `rots::combat::issue_command` registry entry (actor column `ch`) for the `combat_command::move` cell. (ii) ACTOR: `ch` — `snuck_out(struct char_data* ch)` (:1927) resolves that actor's location; the `occ` cursor is derived from the resolved room's occupant chain and is never itself resolved, so R3-C-5 does not apply. (iii) DIRECT CALLERS: exactly ONE tree-wide (re-grepped at this commit) — `snuck_out(ch)` at src/app/act_move.cpp:765, inside `ACMD(do_move)` (:624), forwarding do_move's own `ch` and reached BEFORE do_move's own relocation (`char_from_room(ch); char_to_room(ch, to_room);` at src/app/act_move.cpp:807-808), so nothing relocates `ch` in between. `do_move`'s own doors, all proven: (a) the six `cmd_info` rows `COMMANDO(1..6, POSITION_STANDING, do_move, ...)` at src/app/interpre.cpp:1420/:1422/:1424/:1426/:1428/:1430; (b) `set_combat_command(combat_command::move, do_move)` at src/app/interpre.cpp:1975, i.e. the guarded `issue_command` entry; (c) three direct calls from other `ACMD` bodies, each forwarding its own guarded ACMD `ch` — src/app/act_offe.cpp:415 in `ACMD(do_flee)` (:336; `COMMANDO(9, ...)` at src/app/interpre.cpp:1436 and `COMMANDO(84, ...)` at :1591, plus `set_combat_command(combat_command::flee, do_flee)` at :1968), src/app/act_move.cpp:1369 and :1381 in `ACMD(do_enter)` (:1356; `COMMANDO(7, ...)` at src/app/interpre.cpp:1432), and src/app/act_move.cpp:1419 in `ACMD(do_leave)` (:1388; `COMMANDO(81, ...)` at src/app/interpre.cpp:1585). src/script/spec_pro.cpp:3191's `do_move(victim, ...)` is inside a `/* ... */` comment block and is not a call (confirmed against the gate's own comment/string masker). in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (which refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440, and otherwise spoofs only ids its callers supply), plus append-only room allocation. (R3-C-2's wording as CORRECTED by Wave R3 Task 1a and carried in this file's "The in-range half's standing citation" section — the `:369-391` span the wave's earlier T2p/T3p rows copied from the R3-C-2 draft is stale at this commit; every line cited here was re-read at this commit.) |
| `src/combat/spell_pa.cpp · do_cast · room_of(` | 1 | PROVEN | entry-guard | `if (IS_SET(room_of(ch)->room_flags, PEACEROOM))` at spell_pa.cpp:505, dominated by `do_cast`'s own entry guard `if (location_of(ch) == NOWHERE) { mudlog(...); return; }` at spell_pa.cpp:499 -- the RR Wave R3 Task 1b dispatch-invariant tripwire, landed as the registry entry `src/combat/spell_pa.cpp · do_cast`. This is `entry-guard`, NOT `dispatch-invariant`: since Task 1b the guard is INSIDE this function and dominates this site directly, so the proof no longer has to leave the body. Nothing runs between spell_pa.cpp:499 and :505 -- they are consecutive statements (the guard's closing brace at :503, a blank line at :504) -- so no relocation can intervene. The guard sits below the one earlier arm, `subcmd == -1` (spell_pa.cpp:476-486), which resolves no room and returns. NOTE for the reader who arrives here from Task 1b's placement decision: this site is explicitly NOT offered as a `dominating-resolve` for the `skills[].spell_pointer` dispatch at spell_pa.cpp:900 -- `room_of` is total and degrades silently, so an earlier unguarded resolve proves nothing about a later one (coordinator ruling R3-C-1), which is exactly why the tripwire was placed at :499 instead of at the dispatch. Sentinel exclusion by that entry guard's `location_of(...) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/combat/spell_pa.cpp · do_sense_magic · room_by_id_total(` | 2 | TODO | — | — |
| `src/combat/spell_pa.cpp · send_magic_room_message · room_of(` | 1 | PROVEN | entry-guard | `for (auto* receiver : rots::entity::occupants(room_of(caster)))` at spell_pa.cpp:69, dominated by the combined null-and-sentinel entry guard `if (caster == nullptr \|\| location_of(caster) < 0) return;` at spell_pa.cpp:64 -- the function's first statement; only `rots::text::truncate_at_null(message)` (spell_pa.cpp:67) runs in between. Sentinel exclusion at spell_pa.cpp:64; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Callers recorded for completeness even though the local guard makes them irrelevant: direct -- spell_pa.cpp:100 (`say_spell`) and spell_pa.cpp:662 (`do_cast`); transitively through `say_spell` -- spell_pa.cpp:789 plus ranger.cpp:3172/:3424/:3562/:3724. Existing coverage: `SpellParser.MagicRoomMessage*` (spell_pa_tests.cpp:91/:135). OVERTURNS the census advisory `occupant-loop? (line 69)`: spell_pa.cpp:69 IS the site. |
| `src/combat/visibility.cpp · CAN_SEE · room_of(` | 1 | PROVEN | entry-guard | MIXED-KEY SPLIT (owner ruling R3-O-3; this row plus the TODO row below — the single ledger key collapses TWO DIFFERENT OVERLOADS, which is why one proof could never cover it). This row covers ONE site: `room_data* r = room_of(sub);` at src/combat/visibility.cpp:121, inside the 1-arg `int CAN_SEE(struct char_data* sub)` at :107, dominated by `if (location_of(sub) == NOWHERE) return 0;` at :109 — the function's FIRST statement, ahead of the blind/`PLR_WRITING` test at :111 and the `IS_SHADOW` shortcut at :114. This is the site that makes the CAN_SEE-excludes-NOWHERE lemma work for five `ranger.cpp` rows landed in this same wave (`do_scan` x2, `do_track`, `get_room_tagged_arrows`, `get_corpse_tagged_arrows`). Census advisory partially confirmed: `entry-guard? (line 109)` is right for THIS site and covers only this one of the key's three; its companion `occupant-loop? (line 122)` is spurious — :122 is `bool room_is_dark = !r->light && ...`, an ordinary boolean assignment with no loop within twenty lines. Sentinel exclusion at :109; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. |
| `src/combat/visibility.cpp · CAN_SEE · room_of(` | 2 | TODO | — | — |
| `src/combat/visibility.cpp · generic_find · room_of(` | 1 | TODO | — | — |
| `src/combat/visibility.cpp · get_char_room_vis · room_of(` | 1 | TODO | — | — |
| `src/combat/visibility.cpp · get_obj_vis · room_of(` | 1 | TODO | — | — |
| `src/combat/visibility.cpp · target_from_word · EXIT(` | 2 | TODO | — | — |
| `src/combat/visibility.cpp · target_from_word · room_of(` | 1 | PROVEN | dispatch-invariant | The single site is `tmpobj = get_obj_in_list(word, room_of(ch)->contents);` at src/combat/visibility.cpp:1123, inside the `if (IS_SET(mask, TAR_OBJ_ROOM))` arm (visibility.cpp:1122-1131) -- the arm's own and only room read. (i) REGISTRY ENTRY: src/app/interpre.cpp:1119, the `command_interpreter` registry entry (actor column `ch`). That line number is Wave R3 Task 1c's: Task 1b had placed this entry's guard immediately above the ACMD dispatch, which did NOT dominate `target_parser` (T3d finding O-2); Task 1c moved it to the top of the position-check `else` block precisely so it dominates the target parse as well. NO INTERVENING RELOCATION on that path: between the guard (interpre.cpp:1119) and `target_parser(ch, ...)` (interpre.cpp:1130) there is no call at all -- only `may_not_perform = 0;` and three scalar assignments to `argument_info` -- and `target_parser` forwards the same `ch` straight into `target_from_word` (interpre.cpp:880/:883) with nothing between its own two calls but `target_from_word` itself, whose body only reads and fills the caller's `target_data` (plus a txt-block pool GET on the TAR_TEXT arms). (ii) ACTOR: `ch` -- `target_from_word`'s first parameter, which is `target_parser`'s `ch`, which is `command_interpreter`'s `ch`, the name the registry's own actor column carries. This site resolves THAT actor's location; the `t1` out-parameter the arm fills is the TARGET, but no target-derived id is READ here, so R3-C-5 does not bite. (iii) DIRECT CALLERS: an exhaustive tree-wide re-grep of `target_from_word(` at this commit finds exactly three production call sites -- src/app/interpre.cpp:880 and :883 (both inside `target_parser`, whose own sole production caller is `command_interpreter` at src/app/interpre.cpp:1130, likewise re-grepped; src/app/interpre.cpp:788 is the extern declaration, not a call) and src/combat/spell_pa.cpp:624 (inside `do_cast`). The two test-tier callers (src/tests/visibility_tests.cpp:86/:101) are not production doors. **The `do_cast` door provably cannot reach THIS site.** It calls `target_from_word(ch, arg, skills[spell_index].targets, &tmpwtl.targ2)`, and this arm requires `IS_SET(mask, TAR_OBJ_ROOM)` -- bit 8. Every one of the 162 initialised rows of `skills[]` (src/core/consts.cpp:400; MAX_SKILLS is 256, so the remaining 94 are zero-initialised) was decoded at this commit and NOT ONE sets bit 8; the only two rows that set any direction/object bit are `freeze` (`TAR_DIRECTION`) and `relocate` (`TAR_DIR_WAY`), both of which land in the direction arms at visibility.cpp:1162-1179, not in this one. The closed world holds at runtime too: `skill_data::targets` is never written anywhere in the tree -- the only runtime write to any `skill_data` member is `skills[skill_no].learn_diff = 10` (src/entity/entity_lifecycle.cpp:2164). So `do_cast`'s own guard (src/combat/spell_pa.cpp:499) is not load-bearing for this row, and neither is the intervening-relocation question that entry owes (see the sibling `target_from_word · EXIT(` row, which stays TODO for exactly that reason). Sentinel exclusion by the entry guard's `location_of(ch) == NOWHERE` refusal; in-range: the location field's only writers are `char_to_room` (src/entity/placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id -- mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real in `would_break_the_absence_invariant`, src/entity/placement.cpp:410-420, enforced from the constructor at :427 and from `retarget` at :440; spoofs only ids its callers supply), plus append-only room allocation. Line span per this file's "The in-range half's standing citation (R3-C-2)" section, which corrects R3-C-2's drafted `:369-391`; every line above re-read at this commit. |
| `src/db.h · #decl · world_room_vnum(` | 1 | DECL | — | — |
| `src/entity/char_utils_combat.cpp · get_real_stealth · room_of(` | 1 | PROVEN | caller-contract | no in-function guard; `room_data* r = room_of(ch);` at char_utils_combat.cpp:341 is unconditional. All 4 production callers tree-wide-verified: act_move.cpp:757 (perform_move's sneak-vs-flee check, `ch` still in its pre-move room -- `room_of(ch)` is already used unconditionally elsewhere in this same block, e.g. :769/:788); act_move.cpp:811 (same check, 3 lines after `char_to_room(ch, to_room)` at :808 -- dominating-resolve); ranger.cpp:813 (via `ambush_calculate_success`, reached from `do_ambush`'s direct dispatch and its `WAIT_STATE_FULL` delayed callback -- `extract_char` calls `abort_delay(ch)` at handler.cpp:512 BEFORE unplacing, so a pending delayed callback can never fire on an already-extracted character); ranger.cpp:1963 (via `hide_prof`, reached from `do_hide`'s direct/delayed dispatch under the same `abort_delay` protection, and from `snuck_in()`, called directly from act_move.cpp's movement flow where `room_of(ch)` is already unconditional). In-range via M-1 placement precondition (placement.cpp:369-395) + append-only room allocation. |
| `src/entity/containment.cpp · obj_from_room · room_by_id_total(` | 1 | PROVEN | caller-contract | no in-function guard; `room_by_id_total(object->in_room)` at containment.cpp:163 uses the object's own `in_room` field directly. All 7 production callers tree-wide-verified: act_obj1.cpp:295 (`perform_get_from_room`; every one of ITS 3 callers obtains `item` via a `room_of(ch)->contents` walk, act_obj1.cpp:322/335/347); ranger.cpp:2782 (explicit `if (arrow->in_room >= 0)` guard immediately above); shop.cpp:339 (`temp1` found via `get_obj_in_list_vis` over `shop_index[...].stock_room`'s own contents list, so its `in_room` equals that room); object_utils.cpp:412 (explicit `if (obj->in_room != NOWHERE)` guard immediately above); spec_pro.cpp:1304 (explicit `if (ferryobj->in_room != NOWHERE)` guard a few lines above); mobact.cpp:308 (`best_obj` found via a `room_of(ch)->contents` walk moments earlier at :285); script.cpp:1610 (the call is reached only via a ternary that evaluates `room_by_id_total(tmpobj->in_room)` solely in the `tmpobj->in_room >= 0` branch -- the only theoretical gap is the ternary's other arm firing on `tmprm->number == 0`, a low-priority residual risk of the same shape already accepted below for relocate_all_contents/relocate_all_occupants's ferry-data caveat). In-range via M-1 + append-only allocation. |
| `src/entity/containment.cpp · obj_to_room · room_by_id_total(` | 1 | TODO | — | — |
| `src/entity/containment.cpp · relocate_all_contents · room_by_id_total(` | 8 | PROVEN | caller-contract | sole production caller is spec_pro.cpp:1323 (`ferry_captain`), `relocate_all_contents(old_room, new_room)`. Both values are `ferry_captain_data[num].cabin_route[...]`, normalized via `real_room()` once at boot (spec_pro.cpp:1123-1124) -- a dominating-resolve-shaped in-range argument -- and guarded by containment.cpp:214-215's own `if (from_room == to_room) return;` (mirroring spec_pro.cpp:1312's `if (new_room != old_room)` at the caller) before any of the 8 `room_by_id_total(` sites (containment.cpp:217/:221/:223 x2/:225 x2/:226/:228) run. Residual risk: a corrupt `ferry_captain_data` entry whose `real_room()` returned -1 is not re-guarded at runtime; theoretical, contingent on a trusted static data table's own correctness, not believed exercised in practice. |
| `src/entity/environment_utils.cpp · CAN_GO · room_of(` | 1 | TODO | — | — |
| `src/entity/environment_utils.cpp · can_breathe · room_of(` | 1 | PROVEN | caller-contract | `room_data* r = room_of(ch);` at environment_utils.cpp:151, unconditional. Two production callers, both limits.cpp: `check_breathing`'s own `can_breathe(ch)` call (:1022, `check_breathing` defined at :1016 and invoked unconditionally per-character from `fast_update()`'s `for (character = character_list; ...)` loop, limits.cpp:1590/1633) and the asphyxiation affect-update's `can_breathe(i)` call (:1418, dominated by the already-unconditional `zone = room_of(i)->zone;` four lines above at :1414). Full verification of the shared `character_list`/`fast_update()` concern this row raised: `load_character()` (objsave.cpp:508-511) inserts `ch` into `character_list` one statement before `char_to_room()` places it, but `load_character()` is an ordinary synchronous function with no yield point between those two lines, and `fast_update()` is a separate top-level pulse call that cannot interleave inside it in this single-threaded game loop -- so `fast_update()` can never observe a character mid-load. `extract_char` (handler.cpp) similarly un-places (`char_from_room`, :589) before unlinking from `character_list` (:604+), but also runs to completion synchronously; the one path that could re-enter this concern -- `raw_kill()` called from inside `fast_update()`'s own loop at limits.cpp:1618 -- is immediately followed by `return;` (:1620), which exits the loop for that pulse entirely, so no further `character_list` iteration happens in the same call. In-range via M-1 + append-only allocation. |
| `src/entity/equipment.cpp · attach_equipment · room_of(` | 1 | PROVEN | entry-guard | guarded at src/entity/equipment.cpp:267 `if (location_of(ch) != NOWHERE)` dominating `room_of(ch)->light++;` at :268 (a one-line dominating `if`, distinct from the outer two-term guard at :214 covering a different, wider block). Sentinel exclusion at :267; in-range via M-1 (placement.cpp:369-395) + append-only allocation. |
| `src/entity/equipment.cpp · detach_equipment · room_of(` | 1 | PROVEN | entry-guard | guarded at src/entity/equipment.cpp:357 `if ((location_of(ch) != NOWHERE) && (obj->obj_flags.value[2] != 0) && (obj->obj_flags.value[3] != 0))` dominating `room_of(ch)->light--;` at :360. Sentinel exclusion at :357; in-range via M-1 + append-only allocation. |
| `src/entity/location_benchmark.cpp · locbench::<anon>::measure_iteration · room_by_id_total(` | 1 | PROVEN | caller-contract | `room_data *room = room_by_id_total(room_id);` at location_benchmark.cpp:237, no in-function guard. `room_id` is `measure_iteration`'s own plain `int` parameter; its sole caller is `run_benchmark()` in the same file, whose sole caller tree-wide is `src/tests/location_benchmark_tests.cpp` -- zero production callers exist. `room_id` is drawn from `run_benchmark`'s own `room_ids` vector (built from a test-supplied `room_id_base`, location_benchmark.cpp:439-442), and this same `room_id` was already used to place `occupancy` characters via `char_to_room()` at :227, 10 lines above -- dominating-resolve in substance, classified caller-contract per the test-only-caller structural note (the file lives under production-tier `src/entity/`, but every real caller is test code). |
| `src/entity/location_benchmark.cpp · locbench::<anon>::measure_lookup · room_by_id_total(` | 1 | PROVEN | caller-contract | `room_by_id_total(expected_room_id)` at location_benchmark.cpp:326; `expected_room_id` comes from the same `room_ids` vector `run_benchmark()` builds and passes down (:439-442), same test-only-caller chain as measure_iteration above -- zero production callers. |
| `src/entity/location_benchmark.cpp · locbench::<anon>::measure_lookup · room_of(` | 1 | PROVEN | caller-contract | `room_of(ch)` at location_benchmark.cpp:326, paired with the `room_by_id_total(` row above. `ch = pool[i].get()`, placed via `char_to_room(pool[i].get(), room_id)` at :317, 9 lines earlier in this same function with no intervening detach -- dominating-resolve in substance; classified caller-contract per the test-only-caller structural note shared with the sibling rows in this file. |
| `src/entity/location_benchmark.cpp · locbench::<anon>::measure_lookup · room_of(` | 1 | PROVEN | dominating-resolve | `room_accumulator += reinterpret_cast<uintptr_t>(room_of(ch.get()));` at location_benchmark.cpp:358, timed inside a walk over `pool` -- every member of `pool` was placed by this SAME function's own `char_to_room()` calls at :317 and is detached only later, at :367, strictly after this walk completes; every `ch.get()` this site resolves is therefore still placed by this function's own preceding action, the textbook dominating-resolve shape. |
| `src/entity/location_benchmark.cpp · locbench::<anon>::room_is_empty · room_by_id_total(` | 1 | PROVEN | caller-contract | `rots::entity::first_occupant(room_by_id_total(room_id))` at location_benchmark.cpp:98, `room_id` a plain parameter; called from `run_benchmark()`'s own fixture-hygiene checks with the same `room_ids`-vector provenance and test-only-caller chain as measure_iteration above. |
| `src/entity/placement.cpp · #decl · room_by_id_total(` | 1 | DECL | — | — |
| `src/entity/placement.cpp · #decl · room_of(` | 1 | DECL | — | — |
| `src/entity/placement.cpp · char_to_room · room_by_id_total(` | 1 | TODO | — | — |
| `src/entity/placement.cpp · detach_char_from_room · room_by_id_total(` | 1 | PROVEN | entry-guard | guarded at src/entity/placement.cpp:652 `if (ch->ls_location_id_ == NOWHERE) { return false; }` -- an early return strictly before the resolve at :658 `room_data* r = room_by_id_total(ch->ls_location_id_);`. Sentinel exclusion at :652; in-range via M-1 (:369-395) + append-only allocation. |
| `src/entity/placement.cpp · get_char_room · room_by_id_total(` | 1 | PROVEN | caller-contract | `room_by_id_total(room)->ls_first_occupant_` at placement.cpp:512, `room` a plain `int` parameter, no in-function guard. Sole production caller: spec_pro.cpp:430 (`SPECIAL(pet_shops)`), `get_char_room(buf, pet_room)` where `pet_room = location_of(ch)`. Corrects the mini-census's premise: per the `SPECIAL(cname)` macro (src/interpre.h:49-52), the parameter named `ch` inside a SPECIAL body is the SECOND parameter -- the acting character dispatching the command (matching `special()`'s own `ch`) -- not the special's `host` mob (first parameter). `special()` (interpre.cpp:1246-1264) normalizes `in_room` to `location_of(ch)` whenever passed `NOWHERE` and `return`s `FALSE` without ever reaching the room-occupant dispatch loop (interpre.cpp:1351-1352, `activate_char_special` -> `pet_shops`) when that normalized value is `NOWHERE` -- a documented, tested invariant (interpre.h:130-136's `InterpreSpecial.RemoteMode*`/`AbsentCharacterEarlyReturn*` coverage). Ten production call sites of `special()` exist tree-wide (re-grepped: act_offe.cpp:367, act_othe.cpp:1847, act_move.cpp:162/:342/:456/:460/:824, interpre.cpp:1098/:1101, act_comm.cpp:660); every one of the first nine omits the trailing `in_room` argument or passes `NOWHERE` explicitly (the default). The sole call site passing a real, non-default `in_room` is act_comm.cpp:660 (`do_shout`'s SCMD_YELL cross-zone broadcast), which supplies a loop-bound room id and only runs after this same function already calls `room_of(ch)` unconditionally at act_comm.cpp:656, independently proving `ch` placed there too. In-range via M-1 + append-only allocation. |
| `src/entity/placement.cpp · relocate_all_occupants · room_by_id_total(` | 8 | PROVEN | caller-contract | sole production caller is spec_pro.cpp:1322 (`ferry_captain`), `relocate_all_occupants(old_room, new_room)` -- same `old_room`/`new_room` values as `relocate_all_contents` above (real_room()-normalized ferry route data at boot, a dominating-resolve-shaped in-range argument), guarded by `if (from_room == to_room) return;` (placement.cpp:742) before any of the 8 `room_by_id_total(` sites (placement.cpp:745/:750/:752 x2/:754 x2/:755/:757) run. Same residual corrupt-ferry-data caveat as relocate_all_contents, low priority. |
| `src/entity/placement.cpp · room_of · room_by_id_total(` | 1 | RESOLVER-IMPL | — | — |
| `src/handler.h · #decl · room_by_id_total(` | 1 | DECL | — | — |
| `src/handler.h · #decl · room_of(` | 1 | DECL | — | — |
| `src/interpre.h · #ASSIGNROOM · ASSIGNROOM(` | 1 | DECL | — | — |
| `src/interpre.h · #ASSIGNROOM · world[` | 1 | DECL | — | — |
| `src/olc/shapemob.cpp · do_shape · room_of(` | 2 | TODO | — | — |
| `src/olc/shaperom.cpp · implement_room · room_by_id_total(` | 1 | PROVEN | entry-guard | guarded at src/olc/shaperom.cpp:234-238 `rnum = real_room(SHAPE_ROOM(ch)->room->number); if (rnum < 0) { ...; return; }` immediately before `real = room_by_id_total(rnum);` at :239. Sentinel exclusion (`rnum >= 0`) at :235; in-range because `real_room()`'s binary search bounds any found `mid` within `[0, top_of_world]`. |
| `src/olc/shapescript.cpp · shape_center_script · room_of(` | 1 | TODO | — | — |
| `src/olc/shapezon.cpp · extra_coms_zone · room_of(` | 1 | TODO | — | — |
| `src/olc/shapezon.cpp · shape_center_zone · room_of(` | 1 | TODO | — | — |
| `src/pathfind/graph.cpp · #VALID_EDGE · VALID_EDGE(` | 1 | DECL | — | — |
| `src/pathfind/graph.cpp · #VALID_EDGE · room_by_id_total(` | 1 | DECL | — | — |
| `src/pathfind/graph.cpp · find_first_step · VALID_EDGE(` | 2 | PROVEN | entry-guard | both call sites (`if (VALID_EDGE(src_room, curr_dir))` at graph.cpp:133, `if (VALID_EDGE(queue_head, curr_dir))` at :146) reach the macro's own internal `room_by_id_total(TOROOM(x, y))` behind the SAME macro expansion's short-circuit `&&` chain, graph.cpp:70: `(x)->dir_option[(y)] && (TOROOM(x, y) != NOWHERE) && (!IS_CLOSED(x, y)) && (!IS_MARKED(room_by_id_total(TOROOM(x, y))))`. Sentinel exclusion is the macro's own `TOROOM(x,y) != NOWHERE` term; in-range follows from the world loader's `real_room()` normalization of every `dir_option[].to_room` at creation time (db_world.cpp:1957/:1961, zone_load.cpp) plus append-only allocation. |
| `src/pathfind/graph.cpp · find_first_step · room_by_id_total(` | 1 | PROVEN | loop-bound | `UNMARK(room_by_id_total(curr_room))` at graph.cpp:122, bounded by `for (curr_room = 0; curr_room <= top_of_world; curr_room++)` at :121. |
| `src/pathfind/graph.cpp · find_first_step · room_by_id_total(` | 6 | PROVEN | entry-guard | the remaining 6 sites split from the row above (per the ledger's mixed-class-key rule): graph.cpp:124/:125 (`src_room = room_by_id_total(src)`, `target_room = room_by_id_total(target)`) are dominated by the early return at :112, `if` src or target is out of `[0, top_of_world]` then `log(...); return BFS_ERROR;`; graph.cpp:134/:135/:147/:148 (the four `room_by_id_total((int)TOROOM(...))` calls feeding `MARK`/`bfs_enqueue`) are each dominated by the immediately-preceding `VALID_EDGE(...)` check at :133/:146 (see the `VALID_EDGE(` row above for that guard's own sentinel/in-range reasoning). |
| `src/pathfind/graph.cpp · show_blood_trail · IS_WATER(` | 1 | PROVEN | entry-guard | guarded at src/pathfind/graph.cpp:281 `if (location_of(ch) == NOWHERE) { return 0; }`, dominating `IS_WATER(location_of(ch))` at :317. Sentinel exclusion at :281; in-range via M-1 + append-only allocation. |
| `src/pathfind/graph.cpp · show_blood_trail · room_of(` | 1 | PROVEN | entry-guard | same guard as above (graph.cpp:281), dominating `ch_room = room_of(ch);` at :285. |
| `src/persist/db_players.cpp · #decl · dispatch_room_vnum(` | 1 | DECL | — | — |
| `src/persist/db_players.cpp · save_char · dispatch_room_vnum(` | 1 | PROVEN | entry-guard | guarded one line above by `location_of(ch) != NOWHERE` — src/persist/db_players.cpp:1951; in-range follows from the M-1 placement precondition (placement.cpp:369-395) plus appends-only allocation — a `location_of() != NOWHERE` guard alone proves only the sentinel half |
| `src/script/mobact.cpp · one_mobile_activity · EXIT(` | 4 | TODO | — | — |
| `src/script/mobact.cpp · one_mobile_activity · room_by_id_total(` | 4 | TODO | — | — |
| `src/script/mobact.cpp · one_mobile_activity · room_of(` | 8 | TODO | — | — |
| `src/script/mudlle.cpp · int_tolist · room_by_id_total(` | 1 | TODO | — | — |
| `src/script/mudlle.cpp · int_tolist · room_of(` | 1 | TODO | — | — |
| `src/script/mudlle.cpp · int_tolist · world[` | 2 | TODO | — | — |
| `src/script/mudlle.cpp · service_commands · room_by_id_total(` | 3 | TODO | — | — |
| `src/script/script.cpp · get_room_param · room_of(` | 3 | TODO | — | — |
| `src/script/script.cpp · run_script · IS_SUNLIT(` | 1 | TODO | — | — |
| `src/script/script.cpp · run_script · room_by_id_total(` | 2 | TODO | — | — |
| `src/script/spec_ass.cpp · assign_rooms · ASSIGNROOM(` | 4 | TODO | — | — |
| `src/script/spec_pro.cpp · <anon>::choose_caster_target · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · ar_tarthalon · room_of(` | 3 | TODO | — | — |
| `src/script/spec_pro.cpp · block_exit_down · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · block_exit_east · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · block_exit_north · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · block_exit_south · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · block_exit_up · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · block_exit_west · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · dragon · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · ferry_captain · room_by_id_total(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · gatekeeper · EXIT(` | 18 | TODO | — | — |
| `src/script/spec_pro.cpp · gatekeeper · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · gatekeeper2 · EXIT(` | 13 | TODO | — | — |
| `src/script/spec_pro.cpp · gatekeeper2 · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · gatekeeper_no_knock · EXIT(` | 10 | TODO | — | — |
| `src/script/spec_pro.cpp · get_combat_spells · OUTSIDE(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · get_combat_spells · SUN_PENALTY(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · get_combat_spells · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · healing_plant · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · mob_magic_user_spec · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · mob_ranger · room_of(` | 2 | TODO | — | — |
| `src/script/spec_pro.cpp · mob_ranger_new · EXIT(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · mob_ranger_new · room_by_id_total(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · mob_ranger_new · room_of(` | 3 | TODO | — | — |
| `src/script/spec_pro.cpp · mob_warrior · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · pet_shops · room_by_id_total(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · thuringwethil · room_of(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · vampire_doorkeep · room_by_id_total(` | 2 | TODO | — | — |
| `src/script/spec_pro.cpp · vampire_huntress · room_by_id_total(` | 1 | TODO | — | — |
| `src/script/spec_pro.cpp · vampire_huntress · room_of(` | 3 | TODO | — | — |
| `src/script/spec_pro.cpp · vampire_killer · room_by_id_total(` | 4 | TODO | — | — |
| `src/script/spec_pro.cpp · vampire_killer · room_of(` | 4 | TODO | — | — |
| `src/script/spec_pro.cpp · vortex_elevator · room_of(` | 1 | TODO | — | — |
| `src/tests/act_info_format_tests.cpp · #decl · room_by_id_total(` | 29 | TEST-FIXTURE | — | — |
| `src/tests/act_info_format_tests.cpp · ActInfoDisplayCluster.DoLookCaseEightExitMarkShowsShadowyMarkerForOrc · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_info_format_tests.cpp · ActInfoPerception.DoLookDirectionFormatsTooDarkMessageWhenTargetRoomIsDark · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_info_format_tests.cpp · ActInfoWorldSocial.DoWhoZoneFilterIncludesOnlySameZoneOccupant · room_by_id_total(` | 6 | TEST-FIXTURE | — | — |
| `src/tests/act_info_format_tests.cpp · ActInfoWorldSocial.PerformMortalWhereListsSameZoneOccupantViaDescriptorList · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · #decl · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · DoEnterTest.RefusesWhenAlreadyIndoors · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · DoLeaveTest.RefusesWhenAlreadyOutside · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · DoOpenTest.AnnouncesToTheOtherSideOfAReciprocalDoor · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · DoPullTest.LeverInADifferentRoomAnnouncesRumblingAndTogglesBothSidesReciprocally · room_by_id_total(` | 11 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · DoPullTest.LeverInPullersOwnRoomTogglesDoorWithoutRumblingMessage · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/act_move_tests.cpp · SetBloodTrailTest.WritesConvertedRoomsBleedTrackSlotForNpcCaller · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · #decl · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · DoButcher.FindsTheCorpseInTheRoomWhenNoArgumentIsGiven · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · DoButcher.FindsTheNamedCorpseInTheRoomWhenAnArgumentIsGiven · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · DoButcher.ReportsNoCorpseWhenNoneIsPresentInTheRoom · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · DoGet.FindsAContainerAmongRoomContentsAndStopsReportingNoContainers · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · DoGet.ReportsNoContainersWhenNoneArePresentInTheRoom · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · GetFromRoom.ReportsNothingHereWhenTheRoomIsEmpty · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · GetFromRoom.WithAllArgumentPicksUpEveryVisibleObjectInTheRoom · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · PerformDropGold.CreatesTheGoldObjectInTheDroppersOwnRoom · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/act_obj1_tests.cpp · PerformGetFromRoom.MovesTheItemFromTheRoomIntoTheCharactersInventory · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · #decl · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoBlowout.ExtinguishesALitTorchFoundInTheRoomAndDecrementsRoomLight · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoDrink.DrinksGenericWaterWhenTheRoomHasTheDrinkWaterFlag · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoDrink.ReportsCannotFindWhenNoWaterFlagAndNoObjectAnywhere · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoEat.ReportsNothingToEatWhenNoFoodIsAnywhere · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoEat.TastesFoodFoundInTheRoomWhenNoneIsCarried · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoLight.LightsATorchFoundInTheRoomAndIncrementsRoomLight · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoLight.ReportsNothingToLightWhenNoTorchIsPresent · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoPour.PoursFromADrinkContainerFoundInTheRoomWhenNoWaterFlagIsSet · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/act_obj2_tests.cpp · DoPour.ReportsCannotFindWhenNoWaterFlagAndNoObjectAnywhere · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/act_othe_tests.cpp · #decl · room_by_id_total(` | 6 | TEST-FIXTURE | — | — |
| `src/tests/act_wiz_format_tests.cpp · ActWizInspection.StatRoomFormatsExitLineWithRealDestinationKeywordAndDescription · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/act_wiz_format_tests.cpp · ActWizWorldManip.DoRehashCollectsRoomsWithNonPermanentAffectsAndSkipsPermsOnlyRooms · room_by_id_total(` | 6 | TEST-FIXTURE | — | — |
| `src/tests/act_wiz_format_tests.cpp · ActWizWorldManip.FindTargetRoomResolvesToTheNamedTargetsOwnRoomNotTheCallers · room_by_id_total(` | 9 | TEST-FIXTURE | — | — |
| `src/tests/characterization_combat_tests.cpp · CharacterizationCombatTest.DamageTranscriptSeed42 · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/damage_test_context.h · #decl · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/interpre_account_menu_tests.cpp · <anon>::InterpreAccountMenu.IntroduceCharForAccountBackedCharactersAvoidsLegacyFilesAndKeepsFirstLoginState · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · #decl · room_by_id_total(` | 8 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · <anon>::release_spawned_follower · room_of(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · CleanExposeElementsGuard.StillCancelsForAMageWhoIsInARoomAndCannotSeeTheTarget · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · DeathCryTest.SkipsThePerDoorBroadcastWhenTheDyingCharacterIsNowhere · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · ExtractCharTeardown.ExtractingANowherePlacedCharacterLeavesEveryRoomChainIntact · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · GetCorpseDescTest.UsesTheLyingHereWordingWhenTheDeadCharacterIsNowhere · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomChain.FollowerAndOwnerLandTogetherWhenTheVnumHappensToEqualItsRnum · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomChain.FollowerLandsInTheSameRoomAsItsOwnerWhenLoadRoomHoldsAVnum · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomChain.RnumShapedLoadRoomSendsOwnerAndFollowerToTheStartRoomTogether · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomEndToEnd.RealCrashLoadPlacesPersistedFollowerWithItsOwner · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomPersistence.TheNowhereSavePathConvertsInRoomToAVnum · world_room_vnum(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomPersistence.TheRentStashChainPersistsTheRoomVnumNotItsRnum · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomRider.DeathSavePersistsTheRacialStartRoomVnumNotItsRnum · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomRider.PostLoginSaveLandsABuggedCharacterInTheRacialStartRoom · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomRider.PostLoginSaveOfABuggedCharacterPersistsThePlacedRoomThroughTheRealLoadCharacter · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomRider.RentLoadingALitLightBumpsOnlyTheRoomTheCharacterIsPlacedIn · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomRider.WizsetFileSavePersistsTheChannelVnumNotAResolvedRoomNumber · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadRoomRider.WizsetRoomFieldIsShadowedByRoomflagSoTheRoomArmIsUnreachable · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/load_room_placement_tests.cpp · LoadWindowDeath.MakeCorpseEmptiesWornGearIntoTheCorpseBeforeExtractCharCanSeeIt · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/location_benchmark_tests.cpp · LocationBenchmark.RunBenchmarkReportsExpectedShapeChecksumsAndRestoresWorldState · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/mage_tests.cpp · #decl · room_by_id_total(` | 9 | TEST-FIXTURE | — | — |
| `src/tests/mage_tests.cpp · MageHelpers.DifferentZoneReflectsCurrentWorldZoneNumbers · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/mage_tests.cpp · MageProcTest.LocateLifeAddsReachableRoomsWithUpdatedCoordinates · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/mage_tests.cpp · MageProcTest.LocateLifeSkipsBlockedDuplicateAndExcludedRooms · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/mage_tests.cpp · MageProcTest.RandomExitChoosesAmongEligibleExitsUsingQueuedRandomRolls · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/mage_tests.cpp · MageProcTest.RandomExitFallsBackToSameRoomWhenNoBlinkableExitsExist · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/mobact_tests.cpp · #decl · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · CharToRoomProducerOrder.PublishesEachArrivalAtTheTailOfTheChainItFinds · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · CharToRoomProducerOrder.RepublishingInADifferentSequenceProducesThatSequence · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · GetCharRoomVisOrdinal.ResolvesTheSecondMatchAndInversionFlipsTheSelection · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ListCharToCharOrder.EmitsOccupantsInChainOrderAndInversionFlipsTheOrder · room_by_id_total(` | 6 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · PointUpdateInvisibleAnchorGate.HeadVisibilityGatesDeliveryAndInversionFlipsIt · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · RecalcZonePowerTest.AggregatesNonNpcPowerByRaceAndExcludesNowhereCharacters · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · RecountLightRoomTest.AbsoluteRecomputeCountsWornAndGroundLightSourcesAndOverwritesStaleValue · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ReportZonePowerTest.EvilRaceCharacterDetectsMagiPowerDominationAndReturnsNegativeOne · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ReportZonePowerTest.EvilRaceCharacterDetectsWhitePowerDomination · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ReportZonePowerTest.GoodRaceCharacterDetectsDarkPowerDomination · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ReportZonePowerTest.GoodRaceCharacterReturnsZeroWhenNoSideDominates · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ZoneResetFollowOrdinal.SelectsTheNthMatchAndInversionFlipsTheSelection · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/occupant_order_tests.cpp · ZoneResetLastMobOrdinal.SelectsTheNthMatchAndInversionFlipsTheSelection · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · #decl · room_by_id_total(` | 7 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.DirectionValidationAllowsOpenExitToReachableRoom · room_of(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.DirectionValidationRejectsHiddenClosedExitWithoutHolylight · room_of(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.DirectionValidationRejectsMissingExit · room_of(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.DirectionValidationRejectsNowhereExit · room_of(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.DirectionValidationReportsClosedKeywordForVisibleDoor · room_of(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.RejectsSkillUseInPeaceRooms · room_of(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.RoomTargetSkipsAttackerAndMountedCreature · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiHelpers.RoomTargetVisitOrderFollowsChainOrderNotWhichCharacterIsWhich · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/olog_hai_tests.cpp · OlogHaiProcTest.ReturnsOriginalVictimWhenNoAlternateTargetsExist · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/pathfind_tests.cpp · #decl · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/pathfind_tests.cpp · FindFirstStep.ReturnsNoPathWhenTargetIsUnreachable · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/placement_tests.cpp · RoomOfTest.ReturnsTheFallbackPointerForANowhereCharacter · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/placement_tests.cpp · RoomOfTest.ReturnsTheFallbackPointerForANowhereCharacter · room_of(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/placement_tests.cpp · RoomOfTest.ReturnsTheSamePointerAsTheNestedFormForAPlacedCharacter · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/placement_tests.cpp · RoomOfTest.ReturnsTheSamePointerAsTheNestedFormForAPlacedCharacter · room_of(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/poison_notification_tests.cpp · #decl · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/protocol_tests.cpp · #decl · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/protocol_tests.cpp · <anon>::MSDPProtocol.BroadcastWeatherMsdpUpdateSendsIndoorAndOutdoorWeather · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/protocol_tests.cpp · <anon>::MSDPProtocol.MsdpUpdateEmitsIndoorAndOutdoorWeather · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/protocol_tests.cpp · <anon>::MSDPProtocol.RoomUpdateImplSetsRoomNameVnumExitsAndTerrainWhenLocationIsNegative · room_by_id_total(` | 5 | TEST-FIXTURE | — | — |
| `src/tests/protocol_tests.cpp · <anon>::ScopedMSDPTestRoomGuard.FailsTheTestWhenAnotherFixtureReplacedTheStringsItInstalled · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/ranger_tests.cpp · #decl · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · <anon>::GetRoomParam.Chn1RoomReturnsTheCharactersOwnRoomViaRoomOf · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · TriggerRoomEvent.BeforeEnterNeverConsultsAnOccupantAfterADenial · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · TriggerRoomEvent.BeforeEnterStopsAtTheFirstDenyingOccupantAndReturnsZero · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · TriggerRoomEvent.BeforeEnterWalksAllUnscriptedOccupantsAndReturnsOne · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · TriggerRoomEvent.EnterNeverConsultsAnOccupantAfterADenial · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · TriggerRoomEvent.EnterStopsAtTheFirstDenyingOccupantAndReturnsZero · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/script_tests.cpp · TriggerRoomEvent.EnterWalksAllUnscriptedOccupantsAndReturnsOne · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/shop_tests.cpp · #decl · room_by_id_total(` | 6 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · #decl · room_by_id_total(` | 35 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProFerryCaptain.LeavesADestinationCabinIntactWhenTheSourceCabinsChainIsEmpty · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProFerryCaptain.MovesTheFerryObjectToTheNextDockAndAdvancesTheRouteMarker · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProFerryCaptain.MovesTheWholeSourceChainIntoAnEmptyDestinationCabinAndTerminatesIt · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProFerryCaptain.PerformsNoLightOrZonePowerBookkeepingWhileRelocatingTheCabin · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProFerryCaptain.SplicesTheSourceCabinChainAheadOfTheDestinationsAndRestampsEveryMember · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProVampireKiller.FindsAndDispatchesMoveWhenRoom15398HasAnEligibleVictim · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/spec_pro_tests.cpp · SpecProVampireKiller.FindsAndDispatchesMoveWhenRoom15399HasAnEligibleVictim · room_by_id_total(` | 4 | TEST-FIXTURE | — | — |
| `src/tests/test_world.h · #decl · world[` | 3 | TEST-FIXTURE | — | — |
| `src/tests/zone_reset_guard_tests.cpp · ResetZoneTest.SkipsTheLCommandSettingLastMobFromZoneWhenTheWorldHasFewerThanSevenRooms · room_by_id_total(` | 3 | TEST-FIXTURE | — | — |
| `src/tests/zone_reset_guard_tests.cpp · ResetZoneTest.SkipsTheLCommandSettingLastMobWhenItsRoomArgFailedToResolveAtLoadTime · room_by_id_total(` | 1 | TEST-FIXTURE | — | — |
| `src/tests/zone_reset_guard_tests.cpp · ResetZoneTest.SkipsTheLCommandSettingLastObjWhenItsRoomArgFailedToResolveAtLoadTime · room_by_id_total(` | 2 | TEST-FIXTURE | — | — |
| `src/utils.h · #EXIT · EXIT(` | 1 | DECL | — | — |
| `src/utils.h · #EXIT · room_of(` | 1 | DECL | — | — |
| `src/utils.h · #IS_DARK · IS_DARK(` | 1 | DECL | — | — |
| `src/utils.h · #IS_DARK · room_by_id_total(` | 4 | DECL | — | — |
| `src/utils.h · #IS_LIGHT · IS_DARK(` | 1 | DECL | — | — |
| `src/utils.h · #IS_LIGHT · IS_LIGHT(` | 1 | DECL | — | — |
| `src/utils.h · #IS_SHADOWY_EXIT · IS_SHADOWY_EXIT(` | 1 | DECL | — | — |
| `src/utils.h · #IS_SHADOWY_EXIT · room_by_id_total(` | 2 | DECL | — | — |
| `src/utils.h · #IS_SUNLIT · IS_LIGHT(` | 1 | DECL | — | — |
| `src/utils.h · #IS_SUNLIT · IS_SUNLIT(` | 1 | DECL | — | — |
| `src/utils.h · #IS_SUNLIT_EXIT · IS_SUNLIT_EXIT(` | 1 | DECL | — | — |
| `src/utils.h · #IS_SUNLIT_EXIT · room_by_id_total(` | 5 | DECL | — | — |
| `src/utils.h · #IS_WATER · IS_WATER(` | 1 | DECL | — | — |
| `src/utils.h · #IS_WATER · room_by_id_total(` | 3 | DECL | — | — |
| `src/utils.h · #OUTSIDE · OUTSIDE(` | 1 | DECL | — | — |
| `src/utils.h · #OUTSIDE · room_of(` | 1 | DECL | — | — |
| `src/utils.h · #SUN_PENALTY · OUTSIDE(` | 1 | DECL | — | — |
| `src/utils.h · #SUN_PENALTY · SUN_PENALTY(` | 1 | DECL | — | — |
| `src/utils.h · #SUN_PENALTY · room_of(` | 2 | DECL | — | — |
| `src/utils.h · #decl · room_by_id_total(` | 1 | DECL | — | — |
| `src/utils.h · #decl · room_of(` | 1 | DECL | — | — |
| `src/world/db_world.cpp · #decl · world_room_vnum(` | 1 | DECL | — | — |
| `src/world/db_world.cpp · load_rooms · room_by_id_total(` | 9 | RESOLVER-IMPL | — | — |
| `src/world/db_world.cpp · load_rooms · world[` | 17 | RESOLVER-IMPL | — | — |
| `src/world/db_world.cpp · real_room · room_by_id_total(` | 2 | PROVEN | loop-bound | Tightened re-derivation (RR Wave R2 Task 2) of the rr2-census.md MEDIUM-confidence flag: the loop (db_world.cpp:1732-1757) maintains, by induction, `0 <= bot <= top_of_world` and `top <= top_of_world` at the top of every iteration (base case: `bot=0, top=top_of_world`). Since `bot <= top` whenever the loop body proceeds past its own `if (bot >= top) return -1;` check at :1746, `mid = (bot+top)/2` (:1741) is sandwiched in `[bot, top] ⊆ [0, top_of_world]` for the FIRST `room_by_id_total(mid)` call at :1744 every iteration. For the SECOND call (:1752, only reached if the first one's `->number != virt`): when the branch taken is `top = mid-1` (:1753), integer-division floor guarantees `mid <= top-1` whenever `bot < top` strictly (the only way to reach this branch, since `bot>=top` already returned), so the NEXT iteration's `bot` (unchanged) stays `<= mid <= top_of_world`; when the branch is `bot = mid+1` (:1755), the same `mid <= top-1` bound gives `bot <= top <= top_of_world`. The specific boundary the census worried about ("`bot` has just become `top+1`") cannot occur: `bot=mid+1` is proven `<= top`, never `top+1`. The mirror case (`top` going negative) is bounded too: `top=mid-1` can reach `-1` only when `mid=bot=0`, and the very next iteration's `mid=(0+(-1))/2` truncates to `0` in C++ (division-by-2 of `-1` truncates toward zero) — one more safe call at `mid=0` before `bot(0) >= top(-1)` terminates the loop. Every `room_by_id_total(mid)` call therefore receives `mid ∈ [0, top_of_world]`. |
| `src/world/db_world.cpp · recalc_zone_power · room_of(` | 3 | PROVEN | entry-guard | guarded at src/world/db_world.cpp:2152 `if (!IS_NPC(tmpch) && (location_of(tmpch) != NOWHERE)) { ... }`, dominating all three `room_of(tmpch)` uses inside the block (:2155, :2157, :2159). Sentinel exclusion at :2152; in-range via M-1 (placement.cpp:369-395) + append-only allocation. |
| `src/world/db_world.cpp · renum_world · room_by_id_total(` | 4 | PROVEN | loop-bound | bounded by `for (room = 0; room <= top_of_world; room++)` — src/world/db_world.cpp:844; room never exceeds the resolver's own valid domain |
| `src/world/db_world.cpp · report_zone_power · room_of(` | 1 | TODO | — | — |
| `src/world/db_world.cpp · room_data::create_exit · room_by_id_total(` | 2 | PROVEN | caller-contract | `room_data::create_exit(int dir, int room, char connect)` has exactly TWO production callers tree-wide (re-grepped): (a) mage.cpp:1705, `room_of(caster)->create_exit(DOWN, crack)`, where `crack` is the fresh return value of `world.create_room(...)` two lines earlier — a newly-allocated room id, always valid by construction (dominating-resolve); (b) the function's own self-recursive call at db_world.cpp:1961, `room_by_id_total(room)->create_exit(rev_dir[dir], this_room, FALSE)`, where `this_room = real_room(number)` (:1938) finds the CURRENT room by its own vnum — always found (dominating-resolve). The function's own `if (room < 0) room = this_room;` at :1940-1941 additionally substitutes any negative input with the same always-valid `this_room`. |
| `src/world/db_world.cpp · room_data::create_room · room_by_id_total(` | 3 | RESOLVER-IMPL | — | — |
| `src/world/db_world.cpp · room_data::operator[] · world[` | 1 | RESOLVER-IMPL | — | — |
| `src/world/db_world.cpp · rots::world::room_by_id_impl · world[` | 1 | RESOLVER-IMPL | — | — |
| `src/world/db_world.cpp · rots::world::room_by_id_total_impl · world[` | 1 | RESOLVER-IMPL | — | — |
| `src/world/db_world.cpp · setup_dir · room_by_id_total(` | 7 | PROVEN | caller-contract | `room` is `load_rooms`'s own loop counter `room_nr`, passed unmodified at its sole call site — src/world/db_world.cpp:726; `load_rooms` holds `room_nr` in [0, top_of_world) by construction |
| `src/world/db_world.cpp · setup_dir · world[` | 1 | PROVEN | caller-contract | `room` is `load_rooms`'s own loop counter `room_nr`, passed unmodified at its sole call site — src/world/db_world.cpp:726; `load_rooms` holds `room_nr` in [0, top_of_world) by construction |
| `src/world/db_world.cpp · show_tracks · IS_WATER(` | 1 | PROVEN | entry-guard | guarded at src/world/db_world.cpp:2280 `if (location_of(ch) == NOWHERE) return 0;`, dominating `IS_WATER(location_of(ch))` at :2308. Sentinel exclusion at :2280; in-range via M-1 + append-only allocation. |
| `src/world/db_world.cpp · show_tracks · room_of(` | 1 | PROVEN | entry-guard | same guard as above (db_world.cpp:2280), dominating `ch_room = room_of(ch);` at :2282. |
| `src/world/db_world.cpp · world_room_vnum · room_by_id_total(` | 1 | PROVEN | caller-contract | sole registered dispatch target of dispatch_room_vnum; its only caller guards location_of(ch) != NOWHERE at db_players.cpp:1951 |
| `src/world/weather.cpp · age_bleed_tracks · room_by_id_total(` | 1 | PROVEN | loop-bound | `for (roomnum = 0; roomnum <= top_of_world; roomnum++) { tmproom = room_by_id_total(roomnum); ... }` at weather.cpp:463-464. |
| `src/world/weather.cpp · age_room_tracks · room_by_id_total(` | 1 | PROVEN | loop-bound | identical loop shape at weather.cpp:477-478. |
| `src/world/weather.cpp · get_sun_level · room_by_id_total(` | 7 | PROVEN | caller-contract | no in-function guard; seven unconditional `room_by_id_total(room)->...` reads (weather.cpp:309 x2, :311, :332, :334, :336, :338). Exactly THREE production callers tree-wide (re-grepped): all three go through `do_power_of_arda(ch)` (`level = get_sun_level(location_of(ch));`, limits.cpp:1317), itself called from (a) act_move.cpp:194 and (b) act_move.cpp:363 — both inside `check_simple_move`/`perform_move_mount`, each of which unconditionally dereferences `ch`'s room strictly earlier in the SAME function (`room_from = room_of(ch);` at act_move.cpp:167, and the `EXIT(ch, dir)` macro's own internal `room_of(ch)` at act_move.cpp:336, both preceding their respective `do_power_of_arda(ch)` calls) — dominating-resolve; and (c) limits.cpp:1630, inside `fast_update()`'s `for (character_list...)` loop — the SAME `character_list`/single-threaded-pulse family already fully verified by `can_breathe`'s row above (src/entity/environment_utils.cpp), including that row's own audit of `raw_kill()`'s early `return;` inside this exact loop. In-range via M-1 + append-only allocation. |
| `src/world/weather.cpp · weather_to_char · OUTSIDE(` | 1 | PROVEN | caller-contract | `if (OUTSIDE(ch))` at src/world/weather.cpp:451 (`OUTSIDE(ch)` expands to `!IS_SET(room_of((ch))->room_flags, INDOORS)`, src/utils.h:719). No guard anywhere inside `weather_to_char` (:445-455). EXACTLY ONE production caller tree-wide (re-grepped at this commit: src/world/weather.cpp:181 and src/app/act_info.cpp:2216 are declarations, not calls): `ACMD(do_weather)`'s call at src/app/act_info.cpp:2221, dominated by `if (location_of(ch) == NOWHERE) return;` at src/app/act_info.cpp:2218 with nothing whatever in between. Sentinel exclusion at act_info.cpp:2218; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. PROVENANCE: this is one of the two rows RR Wave R2 deferred under its dispatch-pattern category; ruling R3-C-4 moves them out of that class, because the invariant here is a REAL caller guard, not the "a dispatched actor is placed" premise the deferral was about. |
| `src/world/weather.cpp · weather_to_char · room_of(` | 2 | PROVEN | caller-contract | Both sites — `sector_type = room_of(ch)->sector_type;` at src/world/weather.cpp:449 and `weather_type = weather_info.sky[room_of(ch)->sector_type];` at :450 — share the sibling `OUTSIDE(` row's proof: no guard inside `weather_to_char` (:445-455), EXACTLY ONE production caller tree-wide (`ACMD(do_weather)` at src/app/act_info.cpp:2221; weather.cpp:181 and act_info.cpp:2216 are declarations, not calls), dominated by `if (location_of(ch) == NOWHERE) return;` at src/app/act_info.cpp:2218 with nothing in between. Sentinel exclusion at act_info.cpp:2218; in-range: the location field's only writers are `char_to_room` (placement.cpp:548, whose own unconditional resolve at :564 is the program's RR-O-1 site and catches every invalid id — mudlog today, abort post-flip) and `ScopedRenderLocation` (refuses NOWHERE-over-real, :369-391; spoofs only ids its callers supply), plus append-only room allocation. Deferred by RR Wave R2 under the dispatch-pattern category; reclassified here per ruling R3-C-4. |
| `src/world/zone.cpp · reset_zone · room_by_id_total(` | 3 | GUARDED | RR Wave R2 Task 2 (zone.cpp:314/:342/:448) | Genuine gaps this task's re-audit confirmed (correcting the census's 5-site list, which mis-attributed 2 of its 5 sites — see the entry-guard row below): zone.cpp:314 ('L' sub-command 0) and :342 (sub-command 1) each use `ZCMD.arg2` unconditionally as the room id regardless of which half of the `if (ZCMD.arg2 >= 0 \|\| ZCMD.arg3 >= 0)` OR-gate fired, and zone_load.cpp's 'L'-command load-time normalization (`arg2 = real_room(arg2)`, a bare uncaptured assignment) never routes a failure through the full-command-disable check ('D'/'M'/'O' all do, via the `a`/`b` capture + `if (a<0\|\|b<0) command='*'` mechanism, renum_zone_one() zone_load.cpp:254-329) — so a malformed zone file's failed room reference (`ZCMD.arg2 == NOWHERE`) reaches `room_by_id_total` unguarded. zone.cpp:448 (sub-command 6) is a DIFFERENT root cause: `ZCMD.arg1` there is provably `== 6` (this switch's own case selector; zone_load.cpp never routes 'L' arg1 through `real_room()` at all), so the risk is `room_by_id_total(6)` silently reading one of `create_bulk()`'s trailing `EXTENSION_SIZE`(=50)-room dummy block whenever `top_of_world < 6` — never true for any real production world, but not formally guaranteed. All three now carry an early-skip guard (arg2==NOWHERE for the first two; `arg1 > top_of_world`, mirroring the 'D' command's own idiom, for the third), red-first tested in `src/tests/zone_reset_guard_tests.cpp`'s three `ResetZoneTest.*` cases (quoted failures in task-2-report.md) before landing. |
| `src/world/zone.cpp · reset_zone · room_by_id_total(` | 2 | PROVEN | entry-guard | zone.cpp:585 ('O', read-an-object) is guarded by `if (ZCMD.arg2 >= 0) { ... }` at :583 (a plain single-arg condition, not an OR across two different args) dominating the room use at :585 — since `NOWHERE == -1` (rots/core/types.h:107), `ZCMD.arg2 >= 0` excludes exactly the failed-resolve sentinel. zone.cpp:606 ('P', object-to-object) is guarded by the exhaustive `if (ZCMD.arg1 == NOWHERE \|\| ZCMD.arg3 < 0) { obj_to = obj; } else { ...room_by_id_total(ZCMD.arg1)... }` at :603-606 — by De Morgan, the else branch requires `ZCMD.arg1 != NOWHERE` specifically. This task's re-audit found BOTH sites already correctly guarded, correcting the census's premise that lumped them in with the three genuinely-unguarded sites above (the census's own illustrative citation, an OR-across-two-args guard, does not describe either of these two sites' actual single-arg guard shape). In-range: `real_room()`'s only two possible outputs are `-1` (excluded by these guards) or a `mid ∈ [0, top_of_world]` (per this file's own re-derived `real_room` loop-bound proof, above) — so excluding the sentinel here is equivalent to proving in-range, not merely "not the sentinel" the way a bare `location_of() != NOWHERE` guard would be. |
| `src/world/zone.cpp · reset_zone · room_by_id_total(` | 6 | PROVEN | entry-guard | the 'D' (door-state) command's six sites (zone.cpp:701, :703, :707, :710, :712, :714). zone.cpp:701/:703/:707 (`room_by_id_total(ZCMD.arg1)`): `ZCMD.arg1` for 'D' commands IS routed through the load-time full-disable mechanism (zone_load.cpp:313-314, `a = arg1 = real_room(arg1);`, captured into `a`), so a command that survives to be dispatched as 'D' is guaranteed to carry a non-negative, real_room()-resolved `arg1` — equivalent to `arg1 ∈ [0, top_of_world]` by the same real_room-contract argument as the row above; the runtime `if (ZCMD.arg1 > top_of_world) break;` at :697 is additional (redundant) insurance the census already noted, not the primary protection. zone.cpp:710/:712/:714 (`room_by_id_total(tmp)`): `tmp = room_by_id_total(ZCMD.arg1)->dir_option[ZCMD.arg2]->to_room;` (:707) is guarded by `if (tmp == NOWHERE) break;` (:708-709) immediately above, dominating all three; in-range follows from the SAME `dir_option[].to_room` real_room()-at-creation-time normalization already established and PROVEN elsewhere in this ledger (`src/pathfind/graph.cpp`'s `find_first_step · VALID_EDGE(` row, above: "in-range follows from the world loader's real_room() normalization of every dir_option[].to_room at creation time"). |
| `src/world/zone.cpp · reset_zone · room_of(` | 1 | PROVEN | entry-guard | zone.cpp:451 (`room_of(tmpmob)->zone == tmp2`) is dominated by `location_of(tmpmob) >= 0 &&` immediately before it in the same `if` condition (zone.cpp:450-451: `if (IS_NPC(tmpmob) && tmpmob->nr == ZCMD.arg3 && location_of(tmpmob) >= 0 && room_of(tmpmob)->zone == tmp2)`) — clean, sentinel exclusion in the same statement; in-range via M-1 + append-only allocation. |
| `src/world/zone.cpp · reset_zone · room_of(` | 1 | PROVEN | dominating-resolve | zone.cpp:495 (`for (auto* occ : rots::entity::occupants(room_of(mob)))`): `mob` is confirmed non-null by the guard at zone.cpp:468 (`if ((ZCMD.arg1 != 5 && ZCMD.arg1 != 10 && !mob) ...) { last_cmd = 0; break; }`), and was itself placed earlier in this SAME `reset_zone` invocation via one of three sources — the 'M' command's `char_to_room(mob, ZCMD.arg2)` (zone.cpp:572, dominating-resolve, trivially placed), or the 'L' command's sub-case 6 walk (zone.cpp:425-464; per its own guard at :451, `location_of(tmpmob) >= 0` dominates the loop's match test — sentinel exclusion there, in-range via M-1 + append-only allocation), or the 'L' command's sub-case 5 walk (zone.cpp:408-424, "Sets last_mob from the world"). Sub-case 5's own loop filters ONLY on `IS_NPC(tmpmob) && tmpmob->nr == ZCMD.arg3` — unlike sub-case 6, it carries no `location_of(tmpmob)` check of its own, so its placement guarantee rests entirely on the `character_list`-membership argument: `load_character()` (objsave.cpp:508-511) inserts a character into `character_list` one statement before `char_to_room()` places it, a narrow transient window in which a `character_list` member could read `NOWHERE` — but the game loop is single-threaded and `load_character()` runs to completion in one call with no yield point between those two lines, and `reset_zone()` itself is likewise a single synchronous call (this sub-case's own `for` loop never calls `extract_char`/`load_character` on any OTHER character while walking `character_list`), so `reset_zone()` can never observe that transient window mid-walk — the SAME family invariant this ledger's `can_breathe` row (src/entity/environment_utils.cpp, above) and this task's `get_sun_level`/`report_zone_power` rows (this file, above) already establish in full, extended here to sub-case 5's un-guarded walk rather than assumed. In-range via M-1 + append-only allocation for whichever of the three sources placed `mob`. |

<!-- ROOM-RESOLVE-TOKEN-COUNTS -->
| Token | Sites |
| --- | --- |
| `room_of(` | 321 |
| `room_by_id_total(` | 627 |
| `world[` | 34 |
| `world_room_vnum(` | 4 |
| `dispatch_room_vnum(` | 2 |
| `##` | 0 |
| `EXIT(` | 251 |
| `OUTSIDE(` | 14 |
| `SUN_PENALTY(` | 10 |
| `IS_DARK(` | 3 |
| `IS_LIGHT(` | 2 |
| `IS_SUNLIT(` | 2 |
| `IS_SUNLIT_EXIT(` | 6 |
| `IS_SHADOWY_EXIT(` | 2 |
| `IS_WATER(` | 4 |
| `ASSIGNROOM(` | 5 |
| `VALID_EDGE(` | 3 |

<!-- ROOM-RESOLVE-DISPATCH-ENTRIES -->
| Entry point (file · function) | Actor parameter | Guard literal | Status |
| --- | --- | --- | --- |
| `src/app/interpre.cpp · command_interpreter` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return; }` \|\| `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); } else {` | GUARDED |
| `src/combat/combat_hooks.cpp · rots::combat::issue_command` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return; }` | GUARDED |
| `src/olc/shapemob.cpp · shape_center` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return; }` | GUARDED |
| `src/combat/spell_pa.cpp · do_cast` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return; }` | GUARDED |
| `src/app/act_othe.cpp · do_use` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return; }` | GUARDED |
| `src/combat/mystic.cpp · cast_mass_spell` | `caster` | `if (location_of(caster) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return; }` | GUARDED |
| `src/app/interpre.cpp · activate_char_special` | `victim` | `if (location_of(victim) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return 0; }` | GUARDED |
| `src/app/interpre.cpp · activate_obj_special` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); return 0; }` | GUARDED |
| `src/script/mobact.cpp · mobile_activity` | `ch` | `if (location_of(ch) == NOWHERE) { mudlog( , NRM, LEVEL_IMPL, TRUE); continue; }` | GUARDED |
| `src/app/interpre.cpp · special` | `ch` | `if (in_room == NOWHERE) return FALSE;` | GUARDED-PRIOR |
| `src/script/mobact.cpp · one_mobile_activity` | `ch` | `if ((location_of(ch) < 0) \|\| (location_of(ch) > top_of_world)) {` | GUARDED-PRIOR |
| `src/combat/limits.cpp · affect_update_room` | `tmpch` | `for (tmpch = rots::entity::first_occupant(room); tmpch; tmpch = next_tmpch) {` | GUARDED-PRIOR |
