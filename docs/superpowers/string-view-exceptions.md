# `std::string_view` Migration Exceptions

This ledger records every production declaration that intentionally retains a C-string or
`const std::string&` contract after the repository-wide first pass. Each row is keyed by
owner file plus the census tool's normalized declaration; check mode also requires an allowed
reason and a nonempty, declaration-specific contract.

The census includes function parameters, scalar pointer constants, lookup tables, and
literal-backed `const char*`/`char const*` scalars at file or namespace scope. Literal-backed
locals, type members, runtime-initialized pointers, and nullable pointers are not inferred to be
immutable constants by this heuristic.

Permitted reasons are `nullable-state`, `retains-storage`, `binary-data`, `printf-varargs`,
`c-boundary`, `hot-path`, `abi-layout`, and `sentinel-table`. Inconvenience alone is not an
exception; a `hot-path` entry requires measured evidence that the view boundary causes a material
regression and a contract that preserves the equivalent bounded API.

| Owner file | Normalized declaration | Reason | Contract |
| --- | --- | --- | --- |
| `src/account_management.cpp` | `std::string encode_salt(const std::string&#38; random_bytes) {` | `binary-data` | Every random salt byte, including zero, is mapped modulo the 64-character crypt alphabet; truncating at a null byte would reduce salt entropy and length. |
| `src/account_management.cpp` | `std::string hex_encode(const std::string&#38; bytes) {` | `binary-data` | Each input byte is emitted as exactly two hexadecimal digits, so zero bytes and all bytes after them must remain observable. |
| `src/boards.cpp` | `bool read_i32(const std::string&#38; bytes, size_t* offset, int* value, std::string* error_message, std::string_view label) {` | `binary-data` | The legacy board decoder reads one little-endian 4-byte signed integer at *offset and advances that byte cursor by four. |
| `src/boards.cpp` | `bool skip_bytes(const std::string&#38; bytes, size_t* offset, size_t length, std::string* error_message, std::string_view label) {` | `binary-data` | The legacy board decoder bounds-checks and advances across an exact byte count, including the serialized 4-byte pointer slot in each 32-bit record. |
| `src/boards.cpp` | `bool read_text(const std::string&#38; bytes, size_t* offset, size_t length_including_nul, std::string* out, std::string* error_message, std::string_view label) {` | `binary-data` | The legacy board format supplies a byte count that includes its stored terminator; the decoder copies exactly length-1 bytes and advances across the full serialized field. |
| `src/boards.cpp` | `bool read_legacy_record(const std::string&#38; bytes, size_t* offset, BoardMessageData* message, std::string* error_message) {` | `binary-data` | One board record contains a fixed 28-byte 32-bit header followed by counted heading and message fields; embedded bytes must not shorten those offsets. |
| `src/boards.cpp` | `bool legacy_board_file_from_binary(const std::string&#38; bytes, BoardSaveData* data, std::string* error_message) {` | `binary-data` | The implementation decodes the two-integer board header and exactly num_of_msgs variable-length legacy records, then rejects any trailing byte. |
| `src/boards.h` | `bool legacy_board_file_from_binary(const std::string&#38; bytes, BoardSaveData* data, std::string* error_message = nullptr);` | `binary-data` | The public decoder accepts the complete legacy .boa byte image so callers can migrate counted records without C-string truncation. |
| `src/char_utils.cpp` | `bool is_null_or_empty(const char* a) {` | `nullable-state` | Returns true for either a null pointer or a pointer whose first byte is null; callers intentionally treat both legacy absence forms as empty. |
| `src/comm.cpp` | `void vsend_to_char(char_data* character, const char* format, ...) {` | `printf-varargs` | format is the final named parameter passed to va_start; the implementation forwards its conversion string and variadic arguments to the character output formatter. |
| `src/comm.h` | `void vsend_to_char(struct char_data* ch, const char* format, ...);` | `printf-varargs` | The declaration is a printf-style variadic character-output API whose final named format parameter establishes the va_list boundary. |
| `src/convert_plrobjs.cpp` | `bool legacy_plrobj_bytes_round_trip_losslessly(const std::string&#38; legacy_bytes) {` | `binary-data` | The strict-conversion probe decodes the complete legacy .obj image and compares its JSON round trip; bytes after an embedded zero can determine acceptance. |
| `src/db_players.cpp` | `bool read_i32_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, int* value, std::string* error_message, std::string_view label) {` | `binary-data` | The crime decoder reads the 4-byte little-endian field at record_offset plus an offsetof-derived field offset within crime_record_type. |
| `src/db_players.cpp` | `bool read_i16_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, sh_int* value, std::string* error_message, std::string_view label) {` | `binary-data` | The crime decoder reads the 2-byte little-endian field at record_offset plus an offsetof-derived field offset within crime_record_type. |
| `src/db_players.cpp` | `bool legacy_crime_file_from_binary(const std::string&#38; bytes, std::vector<crime_record_type>* records, std::string* error_message) {` | `binary-data` | The crime migration decoder requires a whole number of sizeof(crime_record_type) records and reads every field at its historical ABI offset. |
| `src/db_boot.cpp` | `void add_exploit_record(int recordtype, char_data* victim, int iIntParam, const char* chParam) {` | `nullable-state` | chParam is intentionally null for PK, death, level, birth, poison, regen-death, and retired records; only STAT, MOBDEATH, ACHIEVEMENT, and NOTE variants consume its text. |
| `src/db.h` | `void add_exploit_record(int, struct char_data*, int, const char*);` | `nullable-state` | The recordtype discriminator determines whether the fourth argument carries text or is null, so the public exploit-record API must represent both states. |
| `src/db.h` | `bool legacy_crime_file_from_binary(const std::string&#38; bytes, std::vector<crime_record_type>* records, std::string* error_message = nullptr);` | `binary-data` | The public crime migration API receives the complete legacy record array, whose size and ABI-derived offsets are validated before JSON conversion. |
| `src/exploits_json.cpp` | `bool read_i32_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, int* value, std::string* error_message, std::string_view label) {` | `binary-data` | The exploit decoder reads a 4-byte little-endian integer at an offsetof-derived position inside each 80-byte historical exploit_record. |
| `src/exploits_json.cpp` | `bool read_i16_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, sh_int* value, std::string* error_message, std::string_view label) {` | `binary-data` | The exploit decoder reads the 2-byte victim identifier at its offsetof-derived position inside each historical exploit_record. |
| `src/exploits_json.cpp` | `bool read_fixed_bytes_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, char* dest, size_t length, std::string* error_message, std::string_view label) {` | `binary-data` | The exploit decoder copies an exact fixed-width char-array field, including padding and missing terminators, from the historical record image. |
| `src/exploits_json.cpp` | `void write_fixed_bytes_at(std::string* bytes, size_t record_offset, size_t field_offset, const char* source, size_t length) {` | `binary-data` | The legacy exploit encoder copies exactly length bytes from a fixed-width struct field, including embedded nulls and any remaining field bytes. |
| `src/exploits_json.cpp` | `bool exploit_records_from_binary(const std::string&#38; bytes, std::vector<exploit_record>* records, std::string* error_message) {` | `binary-data` | The implementation requires a whole number of 80-byte exploit_record images and decodes every fixed-offset scalar and fixed-width text field. |
| `src/exploits_json.cpp` | `size_t bounded_field_length(const char* field, size_t field_size) {` | `binary-data` | Known corrupt legacy exploit fields can lack a terminator; field_size is the hard scan bound that prevents reading into the next struct field. |
| `src/exploits_json.cpp` | `bool fixed_string_fields_equal(const char* a, const char* b, size_t field_size) {` | `binary-data` | Both historical fixed-width fields are compared only within field_size so unterminated corrupt data is unequal without an out-of-bounds strcmp. |
| `src/exploits_json.h` | `bool exploit_records_from_binary(const std::string&#38; bytes, std::vector<exploit_record>* records, std::string* error_message = nullptr);` | `binary-data` | The public exploit decoder accepts the complete historical .exploits byte image, including null-containing fixed-width fields and ABI padding. |
| `src/handler.cpp` | `int isname_c_string(const char* query, const char* name_list, char full) {` | `hot-path` | The private helper receives already-validated terminated pointers from isname_nullable and walks their sentinels directly, avoiding four redundant length/first-null scans on the dominant lookup path while the public string_view overload retains bounded-input safety. |
| `src/handler.cpp` | `int isname_nullable(const char* query, const char* name_list, char full) {` | `nullable-state` | Returns no match when either query or name_list is null; otherwise delegates to the bounded legacy keyword matcher. |
| `src/handler.h` | `int isname_nullable(const char* query, const char* name_list, char full = 1);` | `nullable-state` | This compatibility entry point explicitly permits a missing query or name list and defines either missing pointer as a failed match. |
| `src/interpre.cpp` | `std::string format_account_character_name_for_display(const char* character_name) {` | `nullable-state` | A null character name formats as an empty display string; non-null names use the account display-capitalization path. |
| `src/interpre.cpp` | `bool account_links_character(const account::AccountData&#38; account_data, const char* character_name) {` | `nullable-state` | A null or empty candidate name is not linked; only a present name is normalized and compared with the account roster. |
| `src/interpre.cpp` | `AccountCharacterSessionMatch descriptor_or_character_matches_account( const descriptor_data* descriptor, const account::AccountData&#38; account_data, const char* character_name) {` | `nullable-state` | The candidate name can be null while evaluating partially initialized descriptors; null fails roster and ownership matching without constructing a view. |
| `src/interpre.cpp` | `void mudlog_account_event(struct descriptor_data* d, std::string_view action, const char* email_override = nullptr) {` | `nullable-state` | A null or empty email_override falls back first to the descriptor email and then to <unknown-account>; a present override takes precedence. |
| `src/json_utils.h` | `explicit JsonReader(const char* input) : JsonReader(std::string_view(input)) {` | `c-boundary` | Disambiguates construction from a string-literal argument, which is otherwise equally convertible to std::string_view and to the deleted std::string&& overload; immediately delegates to the string_view constructor with no separate storage or behavior. |
| `src/json_utils.h` | `explicit JsonReaderV2(const char* input) : JsonReaderV2(std::string_view(input)) {` | `c-boundary` | Disambiguates construction from a string-literal argument, which is otherwise equally convertible to std::string_view and to the deleted std::string&& overload; immediately delegates to the string_view constructor with no separate storage or behavior. |
| `src/legacy_salvage.h` | `inline bool fixed_width_field_has_no_nul(const char* field, size_t width) {` | `binary-data` | The recovery helper inspects exactly width bytes to identify a legacy char array with no in-bounds terminator, without reading adjacent storage. |
| `src/legacy_salvage.h` | `inline std::string sanitize_fixed_width_field(const char* field, size_t width) {` | `binary-data` | The recovery helper consumes exactly width bytes, stopping at an in-field null or sanitizing every byte when the corrupt field is unterminated. |
| `src/mail.cpp` | `bool read_i32(const std::string&#38; bytes, size_t* offset, long* value, std::string* error_message, std::string_view label) {` | `binary-data` | The legacy mail decoder reads the next 4-byte little-endian signed value and advances the cursor within a 100-byte block image. |
| `src/mail.cpp` | `bool read_fixed_cstring(const std::string&#38; bytes, size_t* offset, size_t field_size, std::string* out, std::string* error_message, std::string_view label) {` | `binary-data` | Mail names and text occupy fixed 16-, 56-, or 96-byte slots; the decoder advances across the entire slot while extracting text only to its first in-slot null. |
| `src/mail.cpp` | `bool decode_message_at(const std::string&#38; bytes, size_t header_offset, MailMessageData* message, std::string* error_message) {` | `binary-data` | A mail message follows 100-byte header/data blocks by numeric next_block links, so decoding must preserve block offsets, cycles, and bytes beyond embedded nulls. |
| `src/mail.cpp` | `bool legacy_mail_file_from_binary(const std::string&#38; bytes, MailStoreData* data, std::string* error_message) {` | `binary-data` | The implementation requires a whole number of 100-byte mail blocks, scans header blocks, and follows their exact on-disk link indices. |
| `src/mail.h` | `bool legacy_mail_file_from_binary(const std::string&#38; bytes, MailStoreData* data, std::string* error_message = nullptr);` | `binary-data` | The public mail migration API receives the complete block file so 100-byte alignment and linked data blocks remain addressable. |
| `src/objects_json.cpp` | `template <typename T> bool read_pod(const std::string&#38; bytes, size_t* offset, T* value, std::string* error_message, std::string_view label) {` | `binary-data` | This helper memcpy-copies exactly sizeof(T) bytes for padding-free legacy scalars and 20-byte alias keyword arrays, then advances the binary cursor. |
| `src/objects_json.cpp` | `uint32_t read_u32le(const std::string&#38; bytes, size_t offset) {` | `binary-data` | The low-level object decoder assembles four bytes at an already-bounds-checked offset into one little-endian unsigned value. |
| `src/objects_json.cpp` | `int32_t read_s32le(const std::string&#38; bytes, size_t offset) {` | `binary-data` | The low-level object decoder interprets the exact four bytes at an already-bounds-checked offset as a signed 32-bit value. |
| `src/objects_json.cpp` | `int16_t read_s16le(const std::string&#38; bytes, size_t offset) {` | `binary-data` | The low-level object decoder interprets the exact two bytes at an already-bounds-checked offset as a signed 16-bit value. |
| `src/objects_json.cpp` | `bool check_bounds(const std::string&#38; bytes, size_t offset, size_t length, std::string* error_message, std::string_view label) {` | `binary-data` | The binary parser verifies that offset plus the requested record length stays within the complete object-save byte image. |
| `src/objects_json.cpp` | `bool read_rent_info(const std::string&#38; bytes, size_t* offset, DecodedRentInfo* rent, std::string* error_message) {` | `binary-data` | The rent header is an exact 48-byte legacy 32-bit layout with explicit scalar offsets and two padding bytes that must still advance the cursor. |
| `src/objects_json.cpp` | `bool read_obj_file_elem(const std::string&#38; bytes, size_t* offset, DecodedObjFileElem* elem, std::string* error_message, std::string_view label) {` | `binary-data` | Each object occupies the documented 56-byte 32-bit layout; explicit offsets skip historical padding and avoid native-LP64 struct layout. |
| `src/objects_json.cpp` | `bool read_follower_file_elem(const std::string&#38; bytes, size_t* offset, DecodedFollowerFileElem* follower, std::string* error_message) {` | `binary-data` | Each follower occupies exactly 28 bytes containing seven little-endian 32-bit fields before the cursor advances. |
| `src/objects_json.cpp` | `bool read_object_record_or_sentinel(const std::string&#38; bytes, size_t* offset, ObjectRecord* record, bool* is_sentinel, std::string* error_message, std::string_view label) {` | `binary-data` | The parser consumes one complete 56-byte object record and recognizes the -17 item sentinel from its fixed-offset fields. |
| `src/objects_json.cpp` | `bool object_save_data_from_binary_impl( const std::string&#38; bytes, ObjectSaveData* data, bool allow_missing_follower_section, bool* accepted_missing_follower_section, std::string* error_message) {` | `binary-data` | The strict parser walks the complete rent, object, board-point, counted alias, follower, and sentinel sections and rejects unexpected trailing bytes. |
| `src/objects_json.cpp` | `bool try_read_complete_object_list(const std::string&#38; bytes, size_t* offset, std::vector<ObjectRecord>* records, std::string_view label) {` | `binary-data` | Recovery accepts a candidate object list only when whole 56-byte records through a -17 sentinel fit; failure leaves the caller's byte cursor unchanged. |
| `src/objects_json.cpp` | `bool recover_object_save_data_from_binary_impl( const std::string&#38; bytes, ObjectSaveData* data, int* dropped_partial_record_count, std::string* error_message) {` | `binary-data` | The recovery parser preserves the full byte image while salvaging complete legacy sections and reporting records dropped at a partial binary boundary. |
| `src/objects_json.cpp` | `bool object_save_data_from_binary(const std::string&#38; bytes, ObjectSaveData* data, std::string* error_message) {` | `binary-data` | The strict public decoder requires every legacy object-save section, including the follower sentinel, and rejects trailing bytes. |
| `src/objects_json.cpp` | `bool legacy_object_save_data_from_binary( const std::string&#38; bytes, ObjectSaveData* data, bool* accepted_missing_follower_section, std::string* error_message) {` | `binary-data` | The compatibility decoder consumes the complete legacy object-save image but may accept the historically missing follower section and report that condition. |
| `src/objects_json.cpp` | `bool recover_object_save_data_from_binary( const std::string&#38; bytes, ObjectSaveData* data, int* dropped_partial_record_count, std::string* error_message) {` | `binary-data` | The public recovery entry point salvages only complete binary records from a possibly truncated legacy image and returns the dropped-record count. |
| `src/objects_json.h` | `bool object_save_data_from_binary(const std::string&#38; bytes, ObjectSaveData* data, std::string* error_message = nullptr);` | `binary-data` | This API exposes strict decoding of the complete legacy object-save byte layout, including all sentinels and trailing-byte validation. |
| `src/objects_json.h` | `bool legacy_object_save_data_from_binary( const std::string&#38; bytes, ObjectSaveData* data, bool* accepted_missing_follower_section = nullptr, std::string* error_message = nullptr);` | `binary-data` | This API exposes the compatibility decode that distinguishes a complete image from the known missing-follower-section legacy variant. |
| `src/objects_json.h` | `bool recover_object_save_data_from_binary( const std::string&#38; bytes, ObjectSaveData* data, int* dropped_partial_record_count = nullptr, std::string* error_message = nullptr);` | `binary-data` | This API exposes bounded salvage of complete records from corrupt legacy bytes and optionally reports how many partial records were dropped. |
| `src/pkill.cpp` | `bool read_i32_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, int* value, std::string* error_message, std::string_view label) {` | `binary-data` | The pkill decoder reads a 4-byte little-endian scalar at an offsetof-derived position inside each historical PKILL record. |
| `src/pkill.cpp` | `bool read_u8_at(const std::string&#38; bytes, size_t record_offset, size_t field_offset, unsigned char* value, std::string* error_message, std::string_view label) {` | `binary-data` | The pkill decoder reads one raw level byte at an offsetof-derived position inside each historical PKILL record. |
| `src/pkill.cpp` | `bool legacy_pkill_file_from_binary(const std::string&#38; bytes, std::vector<PKILL>* records, std::string* error_message) {` | `binary-data` | The implementation requires a whole number of sizeof(PKILL) records and decodes every scalar from its historical ABI offset. |
| `src/pkill.h` | `bool legacy_pkill_file_from_binary(const std::string&#38; bytes, std::vector<PKILL>* records, std::string* error_message = nullptr);` | `binary-data` | The public pkill migration API receives the complete legacy record image so record-size and fixed-offset validation include all bytes. |
| `src/platform/include/rots/platform/log.h` | `void vmudlog(char type, const char* format, ...);` | `printf-varargs` | This forward declaration of the server's printf-style variadic logger is reachable via the platform header (spec §13); format is the required va_list anchor, the one definition lives in rots_log.cpp (utils.h no longer declares it). |
| `src/platform_compat.h` | `int rots_asprintf(char** out, const char* fmt, ...);` | `printf-varargs` | fmt is the final named parameter of the portable asprintf replacement and is required by its va_start-based formatting ABI. |
| `src/protocol.cpp` | `const char* ProtocolOutput(descriptor_t* apDescriptor, const char* apData, int* apLength) {` | `binary-data` | The implementation uses *apLength as the maximum readable protocol-buffer extent, may stop earlier at a text null, updates the output length, and returns either the input pointer or static transformed storage. |
| `src/protocol.cpp` | `const char* MXPCreateTag(descriptor_t* apDescriptor, const char* apTag) {` | `retains-storage` | When MXP is disabled or the tag is too long, the function returns the exact caller-owned apTag pointer unchanged; enabled short tags instead return overwritten static buffer storage. |
| `src/protocol.h` | `const char* ProtocolOutput(descriptor_t* apDescriptor, const char* apData, int* apLength);` | `binary-data` | The external protocol API pairs apData with mutable *apLength as its explicit processing bound and returns borrowed input or static transformed storage rather than taking ownership. |
| `src/protocol.h` | `const char* MXPCreateTag(descriptor_t* apDescriptor, const char* apTag);` | `retains-storage` | Callers depend on a borrowed input pointer being returned unchanged when no MXP wrapper is produced, while successful wrapping returns function-static storage. |
| `src/rots_crypt.cpp` | `const char* rots_crypt(const char* key, const char* setting) {` | `nullable-state` | The libc-compatible overload returns nullptr when either key or setting is null and delegates only two present strings to the bounded SHA-512 crypt implementation. |
| `src/rots_crypt.h` | `const char* rots_crypt(const char* key, const char* setting);` | `nullable-state` | The compatibility declaration preserves crypt-style null rejection for either input pointer before any string_view is formed. |
| `src/rots_log.cpp` | `void vmudlog(char type, const char* format, ...) {` | `printf-varargs` | format is the final named parameter used by va_start before vsnprintf builds the server log message; the logging-seam Task 2 rewire moved this one definition here from utility.cpp. |
| `src/safe_template.cpp` | `std::string expand_checked(const char* tmpl, std::initializer_list<Conv> expected, std::initializer_list<std::string_view> args, std::string_view fallback, std::string_view context) {` | `nullable-state` | A null template is represented as a malformed scan and returns the supplied fallback; a non-null template is scanned for the declared conversion prefix. |
| `src/safe_template.cpp` | `std::string expand_checked_one(const char* tmpl, const char* arg, std::string_view fallback, std::string_view context) {` | `nullable-state` | The template may be null and fall back, while a null %s argument is deliberately converted to the literal (null) to preserve historical sprintf output. |
| `src/safe_template.h` | `std::string expand_checked(const char* tmpl, std::initializer_list<Conv> expected, std::initializer_list<std::string_view> args, std::string_view fallback, std::string_view context);` | `nullable-state` | The checked-template API accepts a missing template as an explicit rejection case that returns fallback rather than dereferencing it. |
| `src/safe_template.h` | `std::string expand_checked_one(const char* tmpl, const char* arg, std::string_view fallback, std::string_view context);` | `nullable-state` | The one-argument API preserves two nullable states: a missing template selects fallback and a missing argument renders as (null). |
| `src/utility.cpp` | `char* str_dup(const char* source) {` | `nullable-state` | The allocator returns null immediately for a null source; a present source is copied into newly allocated legacy CREATE storage. |
| `src/utility.cpp` | `int rots_asprintf(char** out, const char* fmt, ...) {` | `printf-varargs` | fmt is passed to va_start and vsnprintf twice to size and fill the newly allocated output buffer. |
| `src/utility.cpp` | `int str_cmp_nullable(const char* first, const char* second) {` | `nullable-state` | Defines total ordering for optional strings: two nulls compare equal, null sorts before present text, and two present values use case-insensitive comparison. |
| `src/utility.cpp` | `int strn_cmp_nullable(const char* first, const char* second, int count) {` | `nullable-state` | Defines the same optional-string ordering as str_cmp_nullable, then applies the requested character limit only when both pointers are present. |
| `src/utils.h` | `char* str_dup(const char* source);` | `nullable-state` | Legacy callers may duplicate optional struct text, so the declaration preserves the rule that a null source produces a null result. |
| `src/utils.h` | `inline const char* nz(const char* p) {` | `nullable-state` | Converts a null optional text pointer to the literal (null), matching the historical glibc %s rendering used by converted formatting call sites. |
| `src/utils.h` | `int str_cmp_nullable(const char* first, const char* second);` | `nullable-state` | The compatibility comparator exposes null-equals-null and null-before-present ordering before delegating present strings to str_cmp. |
| `src/utils.h` | `int strn_cmp_nullable(const char* first, const char* second, int count);` | `nullable-state` | The bounded compatibility comparator orders null separately from empty and compares at most count characters only for two present strings. |
| `src/utils.h` | `bool is_null_or_empty(const char* a);` | `nullable-state` | The public helper intentionally collapses a null pointer and a present zero-length C string into the same true result. |

## Mutable gameplay buffers outside the read-only census

These declarations are intentionally absent from the read-only candidate census. They mutate
caller-provided storage and require a separate immutable-parser design rather than a cast or view
adapter.

- `int get_number(char** name);` advances and rewrites the caller's numbered-name cursor in place.
- `int find_all_dots(char* arg);` rewrites command arguments while classifying `all` forms.
- `char* one_argument(char* argument, char* first_arg);` tokenizes a command buffer and writes the
  extracted token into caller-owned storage.
- ACMD command handlers retain mutable `char* argument` parameters because many handlers split or
  rewrite the interpreter-owned command buffer in place.
